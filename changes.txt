create a modern user interface for the image  make the make the components reusable and each component put them to there own files
with full fnuctionality like hovering and animation
don't change the design

