refactor(dashboard): overhaul buyers, transporters, and products pages with sheet-based detail views

Buyers Page:
- Extract getRiskBadge helper into BuyerDataTable component
- Replace custom search/filter UI and HTML table with DataTable component
- Add riskStatus as filterable column with Clear/Risk/Restricted options
- Open BuyerSheet on row click instead of navigating to detail page
- Add Risk column to table with count badge display
- Reduce column sizes for better fit
- Remove unnecessary handleViewBuyer wrapper function
- Remove shadow-sm from stat cards for flatter design

Transporters Page:
- Replace TransporterI with lightweight TransporterDisplayI type
- Simplify table columns (company name, status, total drivers, active drivers, drivers with disputes)
- Replace complex cell renderers (fleet, performance, on-time rate) with simple text cells
- Add stats row (total, pending, active, suspended) with icons
- Open TransporterSheet on row click instead of navigating to detail page
- Add page header with description
- Simplify status badges to inline spans instead of Badge component
- Remove unused lucide icon imports
- Remove pagination props (handled internally by DataTable)

Products Page:
- Replace AdminProductPageSection with new DataTable-based layout
- Add stats row (total, active, hidden, flagged) with icons
- Add VisibilityBadge and RiskBadge inline components
- Open ProductSheet on row click for detail view
- Add dummy data with realistic Tanzanian marketplace products
- Add visibility and risk columns with badge renderers
- Add page header with governance description

DataTable Cleanup:
- Remove title prop from DataTable calls (no longer needed)
- Remove wrapper div from TransporterDataTable and BuyerDataTable
- Pass onView directly without intermediate wrapper handlers

Sheet Component:
- Increase z-index of SheetOverlay and SheetContent from 999999 to 10000000/10000001
- Ensures sheets render above sticky navbar (z-index 999999)

Navigation Fix:
- Fix Reviews & Moderation route from /dashboard/promotions to /dashboard/reviews
