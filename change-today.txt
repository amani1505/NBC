Here's a commit message for these changes:

```
feat: add BNPL/S2B badges, per-product delivery, and Buy Now flow

- Add isBnplEligible, isSaveToBuyEligible, totalReviews, and averageRating fields to product types (FlashDealProduct, AdminProductDetail, CustomerProductDetail)
- Display stacked BNPL (purple) and S2B (emerald) badges on FlashDealProductCard and ShopProductCard alongside existing discount badges
- Show payment eligibility badges (Buy Now Pay Later, Save to Buy) on single product page
- Replace inline hero banner carousel with extracted HeroBanner component driven by API heroProducts data
- Refactor checkout flow from 3-step (Checkout → Delivery → Payment) to 2-step (Delivery → Payment)
- Replace global delivery selection with per-product delivery selector (ProductDeliverySelector) supporting DELIVERY and SELF_PICKUP per item
- Replace CartItemRow with read-only CheckoutCartItemRow in checkout; remove clear cart button from checkout view
- Implement functional Buy Now button: adds to cart, initiates checkout, and redirects to checkout page
- Redesign ProductActions layout: inline Add to Cart + quantity selector + wishlist icon, full-width Buy Now button below
- Replace sold count with review count display on product cards
- Show temporary success message on coupon application; remove persistent coupon removal UI
- Fix cart service clearCart call signature to include confirmation message
```