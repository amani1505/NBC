fix(checkout): refactor S2B flow to skip delivery step and support flexible upfront payment

- Bump Docker image version to 0.1.8
- Refactor OrderDetailPageSection to use OrderItemDetailI type from my/items/:orderNumber endpoint
  - Map nested fields (orderContext, payment, seller) to UI components
  - Remove shipping cost display (not present in new schema)
  - Handle optional shippingAddress and deliverySelections with type casting
- Extract GroupRegistrationForm to separate module for better maintainability
- Update S2B payment flow:
  - Skip delivery step (items delivered after full payment)
  - Replace fixed plan selection with flexible upfront amount input
  - Move account selector and upfront input to same row for compact layout
  - Require upfront amount before checkout (validation added)
  - Pass accountNumber in s2bConfirm request
  - Comment out plan preview UI (to be re-enabled later)
- Update checkout step flow:
  - Initialize currentStep to 2 for installment flows (skip delivery)
  - Conditionally load delivery modes and addresses (not needed for S2B)
  - Validate step 2 for S2B/BNPL regardless of currentStep
  - Hide delivery step indicator when hideDelivery=true
- Update BNPLPaymentForm to use UniversalSelect for account selection
- Enhance UniversalSelect to support badge display (e.g., "Primary" account)
- Add s2bUpfrontAmount state and handler to checkout state management
- Update OrderItemDetailI type with new schema fields (orderContext, payment, seller)
- Add accountNumber field to S2bConfirmRequestI