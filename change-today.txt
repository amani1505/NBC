Here's the commit message for these changes:

---

**feat: redesign dashboard UI with admin-focused governance views**

**Dashboard (`DashboardPageSection`)**
- Completely replaced the generic merchant stats dashboard (revenue, orders, products, customers) with an admin governance dashboard
- New stats row: Total GMV, Total Orders, and Payouts at Risk
- New "Pending Issues" panel: Open Disputes, Merchants Pending Approval, Orders Stuck >24h, Failed Payment Callbacks
- New "System Health" panel: High Refund Rate Merchants, High Dispute Rate Merchants, Buyers Flagged for Risk
- New "Quick Actions" row with links to Disputes Queue, Merchant Applications, and Manual Payouts
- New "Recent Critical Activity" feed replacing the generic activity feed, showing governance events (approvals, flags, freezes, disputes) with actor/admin attribution, timestamps, and eye action buttons
- Removed `StatsCard`, `SalesChart`, `RecentOrders`, `ActivityFeed`, and `QuickActions` section components

**Buyers Page (`BuyersPageSection`)**
- Replaced `BuyerDataTable` component with an inline table built directly in the page section
- Added summary stats row: Total Buyers, Flagged Buyers, Orders in Dispute, Total Payments (TZS)
- Added search bar (filters by name, phone, email) and risk status dropdown filter (All / Clear / Risk / Restricted)
- Added `getRiskBadge` helper rendering styled pill badges for `CLEAR`, `RISK`, and `RESTRICTED` statuses, including `riskCount` overflow indicator
- Updated dummy data to use Tanzanian names, masked phone numbers, and `riskStatus`/`riskCount` fields aligned with the new type
- Table columns: Buyer Name, Phone, Total Orders, Total Spend (TZS), Risk, Action (eye icon)
- Removed `fetchAllBuyers` call from `useEffect` (API not yet ready; comment retained)

**Dashboard Routes (`DashboardRoutes.ts`)**
- Removed all merchant-role routes (Store Management, Payments, Marketing, Settings, Support)
- Scoped all remaining routes to `ROLE_ADMIN` only
- Renamed and reordered nav items to reflect admin governance structure: Dashboard → Buyers → Merchants → Transporters → Product & Catalogue → Orders → Payments & Payouts → Disputes & Returns → Reviews & Moderation → Analytics & Reports
- Renamed "Transporter Management" → "Transporters", "Buyer Management" → "Buyers", "Merchant Management" → "Merchants", "Product Catalogue" → "Product & Catalogue", "Promotions & CMS" → "Reviews & Moderation", "Reports" → "Analytics & Reports"
- Added new "Payments & Payouts" route (`/dashboard/payments-payouts`, icon: `Shield`)
- Removed "Platform Settings" route

**Dashboard Client Layout**
- Removed "Back to Site" link from the dashboard menu

**Sidebar (`Sidebar.tsx`)**
- Removed "Back to Site" link and footer text block from the sidebar footer section

**Types (`buyers.ts`)**
- Added `riskStatus: "CLEAR" | "RISK" | "RESTRICTED"` field to `BuyerI`
- Added optional `riskCount?: number` field to `BuyerI`

**Misc**
- Fixed syntax error in `order.provider.tsx`: removed stray backslash from `setLoading` initialization
