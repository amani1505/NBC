
give me the developer commit message fo this changes
diff --git a/commits.txt b/commits.txt
index 378a249..fee425d 100644
--- a/commits.txt
+++ b/commits.txt
@@ -605,80 +605,4 @@
     
     ╚════════════════════════════════════════════════════════════════════╝
 
- commits.txt                                        | 182 ------------
- docker-compose.yml                                 |   2 +-
- .../CollectionCenterPageSection.tsx                |  89 +++---
- .../SingleCollectionCenterPageSection.tsx          |  24 +-
- .../components/AllocateBIBsModal.tsx               |  63 +---
- .../components/CollectionCenterForm.tsx            |  35 ++-
- .../RegistrationPageSection.tsx                    |  32 +--
- .../RegistrationSideSheet.tsx                      | 320 +++++++++++++--------
- .../TicketsPageSection/TicketSideSheet.tsx         | 268 +++++++++++------
- .../TicketsPageSection/TicketsPagesection.tsx      | 111 ++++---
- src/app/(PageSections)/Landing/HomePageSection.tsx |   1 -
- .../shared/registration/MerchandiseSelection.tsx   |   4 +-
- .../shared/registration/ParticipantDetails.tsx     |   3 +-
- .../shared/registration/RegistrationForm.tsx       |  36 ++-
- src/components/ui/calendar-rac.tsx                 |  48 +++-
- src/components/ui/form-date-picker.tsx             |   3 +-
- src/contexts/collection-center.context.ts          |   1 +
- src/contexts/collection-center.context.tsx         |   1 +
- src/contexts/event-registration.context.tsx        |   1 +
- src/lib/api/base-api.ts                            |   3 +-
- src/providers/collection-center.provider.tsx       |  12 +
- src/providers/event-registration.provider.tsx      |   9 +
- src/services/collection-center.service.ts          |  20 ++
- src/services/event-registration.service.ts         |  12 +
- src/services/numbering.service.ts                  |  42 +++
- src/types/event-registration.ts                    |  32 ++-
- 26 files changed, 722 insertions(+), 632 deletions(-)
-
-╔════════════════════════════════════════════════════════════════════╗
-║ Commit: 539a83d639d2cf5eb8c53b7c17ecc05f78bbe1a8
-║ Short:  539a83d
-║ Date:   2026-03-26 05:21:54 (6 days ago)
-║ Author: Robinson.Anthony <robinson.anthony@nbc.co.tz>
-╠════════════════════════════════════════════════════════════════════╣
-║ Subject:
-║   ix(collection-centers): display BIB number chips with expand/collapse and safe optional chaining
-║
-║ Message:    
-    ║     - Replace rangeStart/rangeEnd fields with numbers[] and usedNumbersList[] in RangeAllocation
-      - Add ChipList component with expand/collapse for BIB number display in SingleCollectionCenterPageSection
-      - Show used and available BIB numbers as color-coded chips with progress bar and stats grid
-      - Move collection center filtering from client-side to server-side (pass eventId/search to API)
-      - Fix optional chaining on ticket.name, ticket.currency, ticket.imageUrl across checkout components
-      - Fix statusFilter TypeScript type to avoid explicit-any lint error
-    
-    ╚════════════════════════════════════════════════════════════════════╝
-
- .../CollectionCenterPageSection.tsx                |  18 +-
- .../SingleCollectionCenterPageSection.tsx          | 202 ++++++++++++++-------
- .../RegistrationPageSection.tsx                    |   5 +-
- .../DashboardTicketCheckoutPagesection.tsx         |  10 +-
- .../Sections/DashboardTicketPaymentDetails.tsx     |  14 +-
- .../Sections/OrderItemCard.tsx                     |   2 +-
- .../Sections/TicketPaymentDetails.tsx              |   2 +-
- src/services/numbering.service.ts                  |   4 +-
- 8 files changed, 163 insertions(+), 94 deletions(-)
-
-╔════════════════════════════════════════════════════════════════════╗
-║ Commit: 2cd456352530f0df8aff50adb382d0b976559c7c
-║ Short:  2cd4563
-║ Date:   2026-03-25 20:54:17 (7 days ago)
-║ Author: Robinson.Anthony <robinson.anthony@nbc.co.tz>
-╠════════════════════════════════════════════════════════════════════╣
-║ Subject:
-║   refactor(tickets): update ticket purchase types to nested structure and improve tickets table UI
-║
-║ Message:    
-    ║     - Add TicketPurchaseItemI (nested purchase/attendee/ticket), TicketPurchaseAttendeeI,
-      TicketPurchaseTicketI, BibAssignmentI, BibCollectionCenterI types
-      - Update ticket service, context, and provider to use TicketPurchaseItemI
-      - Rewrite TicketsPagesection columns to use nested accessors with attendee name/email, ticket
-      name, and softer status badge colors
-      - Add TicketSideSheet for viewing ticket details from the table
-      - Add View custom action to open the side sheet
-    
-    ╚════════════════════════════════════════════════════════════════════╝
-
+ 
\ No newline at end of file
diff --git a/src/app/(PageSections)/Auth/ForgotPasswordPageSection.tsx b/src/app/(PageSections)/Auth/ForgotPasswordPageSection.tsx
index dd866fd..f3cbec7 100644
--- a/src/app/(PageSections)/Auth/ForgotPasswordPageSection.tsx
+++ b/src/app/(PageSections)/Auth/ForgotPasswordPageSection.tsx
@@ -23,6 +23,7 @@ function ForgotPasswordPageSection() {
   const validationSchema = Yup.object({
     email: Yup.string()
       .email(tValidation("email.invalid"))
+      .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, tValidation('email.invalid'))
       .required(tValidation("email.required")),
   });
 
diff --git a/src/app/(PageSections)/Auth/SignupPageSection.tsx b/src/app/(PageSections)/Auth/SignupPageSection.tsx
index eebd436..ff08490 100644
--- a/src/app/(PageSections)/Auth/SignupPageSection.tsx
+++ b/src/app/(PageSections)/Auth/SignupPageSection.tsx
@@ -103,6 +103,7 @@ function SignupPageSection() {
       .required(tValidation('lastName.required')),
     email: Yup.string()
       .email(tValidation('email.invalid'))
+      .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, tValidation('email.invalid'))
       .required(tValidation('email.required')),
     phoneNumber: Yup.string()
       .required(tValidation('phoneNumber.required'))
diff --git a/src/app/(PageSections)/Dashboard/CollectionCenterPageSections/components/CollectionCenterForm.tsx b/src/app/(PageSections)/Dashboard/CollectionCenterPageSections/components/CollectionCenterForm.tsx
index 9d6dca1..2112a3e 100644
--- a/src/app/(PageSections)/Dashboard/CollectionCenterPageSections/components/CollectionCenterForm.tsx
+++ b/src/app/(PageSections)/Dashboard/CollectionCenterPageSections/components/CollectionCenterForm.tsx
@@ -112,7 +112,9 @@ const collectionCenterValidationSchema = Yup.object().shape({
 
   email: Yup.string()
     .trim()
-    .email("Must be a valid email")
+    .email("Please enter a valid email address")
+    .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address")
+
     .required("Email is required"),
 
   operatingHours: Yup.string()
@@ -211,15 +213,15 @@ const CollectionCenterForm: React.FC<CollectionCenterFormProps> = ({
     if (collectionCenterId && center && center.id) {
       const startDate = center.collectionStartDate
         ? (() => {
-            const d = new Date(center.collectionStartDate);
-            return new ICalendarDate(d.getFullYear(), d.getMonth() + 1, d.getDate());
-          })()
+          const d = new Date(center.collectionStartDate);
+          return new ICalendarDate(d.getFullYear(), d.getMonth() + 1, d.getDate());
+        })()
         : undefined;
       const endDate = center.collectionEndDate
         ? (() => {
-            const d = new Date(center.collectionEndDate);
-            return new ICalendarDate(d.getFullYear(), d.getMonth() + 1, d.getDate());
-          })()
+          const d = new Date(center.collectionEndDate);
+          return new ICalendarDate(d.getFullYear(), d.getMonth() + 1, d.getDate());
+        })()
         : undefined;
 
       setFormInitialValues({
@@ -749,8 +751,8 @@ const CollectionCenterForm: React.FC<CollectionCenterFormProps> = ({
                 {isSubmitting
                   ? "Saving..."
                   : collectionCenterId
-                  ? "Update Collection Center"
-                  : "Create Collection Center"}
+                    ? "Update Collection Center"
+                    : "Create Collection Center"}
               </Button>
             </div>
           </Form>
diff --git a/src/app/(PageSections)/Dashboard/EventPageSection/components/tabs/SponsorsTab.tsx b/src/app/(PageSections)/Dashboard/EventPageSection/components/tabs/SponsorsTab.tsx
index 87c6242..bf7e892 100644
--- a/src/app/(PageSections)/Dashboard/EventPageSection/components/tabs/SponsorsTab.tsx
+++ b/src/app/(PageSections)/Dashboard/EventPageSection/components/tabs/SponsorsTab.tsx
@@ -74,18 +74,18 @@ interface SponsorsTabProps {
 }
 
 const DISCOUNT_TYPE_OPTIONS = [
-  { value: "FULLY_FREE",    label: "Fully Free"    },
-  { value: "PERCENTAGE",    label: "Percentage"    },
-  { value: "FIXED_AMOUNT",  label: "Fixed Amount"  },
+  { value: "FULLY_FREE", label: "Fully Free" },
+  { value: "PERCENTAGE", label: "Percentage" },
+  { value: "FIXED_AMOUNT", label: "Fixed Amount" },
 ];
 
 const STATUS_VARIANT: Record<string, "default" | "secondary" | "destructive" | "outline"> = {
   PENDING_REVIEW: "secondary",
-  ACTIVE:         "default",
-  APPROVED:       "default",
-  REJECTED:       "destructive",
-  SUSPENDED:      "destructive",
-  EXPIRED:        "outline",
+  ACTIVE: "default",
+  APPROVED: "default",
+  REJECTED: "destructive",
+  SUSPENDED: "destructive",
+  EXPIRED: "outline",
 };
 
 function statusBadgeVariant(status: string): "default" | "secondary" | "destructive" | "outline" {
@@ -818,7 +818,7 @@ function InlineMerchandiseSelection({
   const getMerchandiseByCategory = (categoryName: string) =>
     availableMerchandise.find(
       (item) => item.categoryName.toLowerCase() === categoryName.toLowerCase() &&
-                item.isActive && item.availableQuantity > 0
+        item.isActive && item.availableQuantity > 0
     );
 
   const getVarietyOptions = (item: EventMerchandise | undefined) =>
@@ -860,8 +860,8 @@ function InlineMerchandiseSelection({
   if (!availableMerchandise || availableMerchandise.length === 0) return null;
 
   const tshirtMerchandise = getMerchandiseByCategory("T-Shirt");
-  const shortMerchandise  = getMerchandiseByCategory("Short");
-  const shoeMerchandise   = getMerchandiseByCategory("Shoe");
+  const shortMerchandise = getMerchandiseByCategory("Short");
+  const shoeMerchandise = getMerchandiseByCategory("Shoe");
 
   return (
     <div className="space-y-6">
@@ -913,7 +913,7 @@ const participantSchema = Yup.object({
   firstName: Yup.string().min(2).max(50).required("First name is required"),
   middleName: Yup.string().max(50).optional(),
   lastName: Yup.string().min(2).max(50).required("Last name is required"),
-  email: Yup.string().transform((v) => (v === "" ? undefined : v)).email("Invalid email").optional(),
+  email: Yup.string().transform((v) => (v === "" ? undefined : v)).email("Please enter a valid email address").matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address").optional(),
   phone: Yup.string()
     .required("Phone number is required")
     .test("valid-phone", "Please enter a valid phone number", (v) => !!v && isValidPhoneNumber(v)),
@@ -1011,7 +1011,7 @@ function RegisterAttendeesContent({
     validationSchema: participantSchema,
     validateOnChange: true,
     validateOnBlur: true,
-    onSubmit: () => {},
+    onSubmit: () => { },
   });
 
   const { errors, touched, values, setFieldValue, setFieldTouched, validateForm, resetForm } = formik;
@@ -1145,298 +1145,298 @@ function RegisterAttendeesContent({
           <TabsTrigger value="excel">Excel Upload</TabsTrigger>
         </TabsList>
 
-      {/* ── Manual Entry ── */}
-      <TabsContent value="manual">
-        <div className="space-y-5 pt-2">
-          {/* Stepper */}
-          {participantCount > 1 && (
-            <Stepper steps={participantCount} currentStep={currentStep} onStepClick={handleStepClick} />
-          )}
-
-          {/* Participant heading */}
-          <div className="flex items-center justify-between">
-            <div>
-              <h3 className="text-xl font-bold text-primary-950 mb-1">
-                {participantCount === 1 ? "Participant Details" : `Participant ${currentStep} of ${participantCount}`}
-              </h3>
-              <p className="text-sm text-muted-foreground">Please fill in all required information</p>
-            </div>
+        {/* ── Manual Entry ── */}
+        <TabsContent value="manual">
+          <div className="space-y-5 pt-2">
+            {/* Stepper */}
             {participantCount > 1 && (
-              <button type="button" onClick={removeCurrentParticipant}
-                className="text-sm text-destructive hover:underline">
-                Remove Participant {currentStep}
-              </button>
+              <Stepper steps={participantCount} currentStep={currentStep} onStepClick={handleStepClick} />
             )}
-          </div>
 
-          {/* Personal Information */}
-          <div className="bg-gray-50 dark:bg-gray-900 p-4 sm:p-6 rounded-lg space-y-4">
-            <h4 className="text-base font-semibold text-foreground">Personal Information</h4>
-            <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
-              <FormInput
-                label="First Name" placeholder="Enter first name" required
-                value={values.firstName}
-                onChange={(e) => setFieldValue("firstName", e.target.value)}
-                onBlur={() => setFieldTouched("firstName", true)}
-                error={touched.firstName && errors.firstName ? String(errors.firstName) : undefined}
-              />
-              <FormInput
-                label="Middle Name" placeholder="Enter middle name (optional)"
-                value={values.middleName}
-                onChange={(e) => setFieldValue("middleName", e.target.value)}
-                onBlur={() => setFieldTouched("middleName", true)}
-                error={touched.middleName && errors.middleName ? String(errors.middleName) : undefined}
-              />
-              <FormInput
-                label="Last Name" placeholder="Enter last name" required
-                value={values.lastName}
-                onChange={(e) => setFieldValue("lastName", e.target.value)}
-                onBlur={() => setFieldTouched("lastName", true)}
-                error={touched.lastName && errors.lastName ? String(errors.lastName) : undefined}
-              />
-              <FormInput
-                label="Email Address" placeholder="Enter email (optional)" type="email"
-                value={values.email}
-                onChange={(e) => setFieldValue("email", e.target.value)}
-                onBlur={() => setFieldTouched("email", true)}
-                error={touched.email && errors.email ? String(errors.email) : undefined}
-              />
-              <PhoneNumberInput
-                label="Phone Number" placeholder="Add phone number" required
-                value={values.phone}
-                onChange={(phone) => {
-                  setFieldValue("phone", phone);
-                  setFieldTouched("phone", true);
-                  setTimeout(() => {
-                    formik.validateField("phone");
-                    if (values.emergencyPhone) formik.validateField("emergencyPhone");
-                  }, 0);
-                }}
-                error={touched.phone && errors.phone ? String(errors.phone) : undefined}
-              />
-              <I18nProvider locale="en-GB">
-                <FormDatePicker
-                  label="Date of Birth"
-                  description="Participant must be at least 2 years old"
-                  required
-                  value={
-                    values.dateOfBirth
-                      ? (() => {
+            {/* Participant heading */}
+            <div className="flex items-center justify-between">
+              <div>
+                <h3 className="text-xl font-bold text-primary-950 mb-1">
+                  {participantCount === 1 ? "Participant Details" : `Participant ${currentStep} of ${participantCount}`}
+                </h3>
+                <p className="text-sm text-muted-foreground">Please fill in all required information</p>
+              </div>
+              {participantCount > 1 && (
+                <button type="button" onClick={removeCurrentParticipant}
+                  className="text-sm text-destructive hover:underline">
+                  Remove Participant {currentStep}
+                </button>
+              )}
+            </div>
+
+            {/* Personal Information */}
+            <div className="bg-gray-50 dark:bg-gray-900 p-4 sm:p-6 rounded-lg space-y-4">
+              <h4 className="text-base font-semibold text-foreground">Personal Information</h4>
+              <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
+                <FormInput
+                  label="First Name" placeholder="Enter first name" required
+                  value={values.firstName}
+                  onChange={(e) => setFieldValue("firstName", e.target.value)}
+                  onBlur={() => setFieldTouched("firstName", true)}
+                  error={touched.firstName && errors.firstName ? String(errors.firstName) : undefined}
+                />
+                <FormInput
+                  label="Middle Name" placeholder="Enter middle name (optional)"
+                  value={values.middleName}
+                  onChange={(e) => setFieldValue("middleName", e.target.value)}
+                  onBlur={() => setFieldTouched("middleName", true)}
+                  error={touched.middleName && errors.middleName ? String(errors.middleName) : undefined}
+                />
+                <FormInput
+                  label="Last Name" placeholder="Enter last name" required
+                  value={values.lastName}
+                  onChange={(e) => setFieldValue("lastName", e.target.value)}
+                  onBlur={() => setFieldTouched("lastName", true)}
+                  error={touched.lastName && errors.lastName ? String(errors.lastName) : undefined}
+                />
+                <FormInput
+                  label="Email Address" placeholder="Enter email (optional)" type="email"
+                  value={values.email}
+                  onChange={(e) => setFieldValue("email", e.target.value)}
+                  onBlur={() => setFieldTouched("email", true)}
+                  error={touched.email && errors.email ? String(errors.email) : undefined}
+                />
+                <PhoneNumberInput
+                  label="Phone Number" placeholder="Add phone number" required
+                  value={values.phone}
+                  onChange={(phone) => {
+                    setFieldValue("phone", phone);
+                    setFieldTouched("phone", true);
+                    setTimeout(() => {
+                      formik.validateField("phone");
+                      if (values.emergencyPhone) formik.validateField("emergencyPhone");
+                    }, 0);
+                  }}
+                  error={touched.phone && errors.phone ? String(errors.phone) : undefined}
+                />
+                <I18nProvider locale="en-GB">
+                  <FormDatePicker
+                    label="Date of Birth"
+                    description="Participant must be at least 2 years old"
+                    required
+                    value={
+                      values.dateOfBirth
+                        ? (() => {
                           const [y, m, d] = values.dateOfBirth.split("-");
                           return y && m && d ? new CalendarDate(+y, +m, +d) : null;
                         })()
-                      : null
-                  }
-                  onChange={(dv) => {
-                    if (dv) {
-                      setFieldValue("dateOfBirth", `${dv.year}-${String(dv.month).padStart(2,"0")}-${String(dv.day).padStart(2,"0")}`);
-                    } else {
-                      setFieldValue("dateOfBirth", "");
+                        : null
                     }
+                    onChange={(dv) => {
+                      if (dv) {
+                        setFieldValue("dateOfBirth", `${dv.year}-${String(dv.month).padStart(2, "0")}-${String(dv.day).padStart(2, "0")}`);
+                      } else {
+                        setFieldValue("dateOfBirth", "");
+                      }
+                    }}
+                    onBlur={() => setFieldTouched("dateOfBirth", true)}
+                    error={touched.dateOfBirth && errors.dateOfBirth ? String(errors.dateOfBirth) : undefined}
+                    maxValue={today(getLocalTimeZone()).subtract({ years: 2 })}
+                    className="w-full"
+                  />
+                </I18nProvider>
+                <FormSelect
+                  label="Gender" required
+                  options={[{ label: "Male", value: "MALE" }, { label: "Female", value: "FEMALE" }]}
+                  value={values.gender}
+                  onValueChange={(v) => { setFieldValue("gender", v); setTimeout(() => setFieldTouched("gender", true), 0); }}
+                  error={touched.gender && errors.gender ? String(errors.gender) : undefined}
+                />
+              </div>
+            </div>
+
+            {/* Location Information */}
+            <div className="bg-gray-50 dark:bg-gray-900 p-4 sm:p-6 rounded-lg space-y-4">
+              <h4 className="text-base font-semibold text-foreground">Location Information</h4>
+              <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
+                <CountriesSelect
+                  label="Country" required
+                  value={values.country}
+                  onChange={(v) => {
+                    setFieldValue("country", v);
+                    setTimeout(() => setFieldTouched("country", true), 0);
+                    if (v !== "Tanzania" && values.region) setFieldValue("region", "");
                   }}
-                  onBlur={() => setFieldTouched("dateOfBirth", true)}
-                  error={touched.dateOfBirth && errors.dateOfBirth ? String(errors.dateOfBirth) : undefined}
-                  maxValue={today(getLocalTimeZone()).subtract({ years: 2 })}
-                  className="w-full"
+                  error={touched.country && errors.country ? String(errors.country) : undefined}
                 />
-              </I18nProvider>
-              <FormSelect
-                label="Gender" required
-                options={[{ label: "Male", value: "MALE" }, { label: "Female", value: "FEMALE" }]}
-                value={values.gender}
-                onValueChange={(v) => { setFieldValue("gender", v); setTimeout(() => setFieldTouched("gender", true), 0); }}
-                error={touched.gender && errors.gender ? String(errors.gender) : undefined}
-              />
+                {values.country === "Tanzania" && (
+                  <FormSelect
+                    label="Region" placeholder="Select region" required
+                    options={TANZANIA_REGION_OPTIONS}
+                    value={values.region}
+                    onValueChange={(v) => { setFieldValue("region", v); setTimeout(() => setFieldTouched("region", true), 0); }}
+                    error={touched.region && errors.region ? String(errors.region) : undefined}
+                  />
+                )}
+                <FormInput
+                  label="City" placeholder="City"
+                  value={values.city}
+                  onChange={(e) => setFieldValue("city", e.target.value)}
+                  onBlur={() => setFieldTouched("city", true)}
+                />
+                <FormInput
+                  label="Zip Code" placeholder="Zip code"
+                  value={values.zipCode}
+                  onChange={(e) => setFieldValue("zipCode", e.target.value)}
+                  onBlur={() => setFieldTouched("zipCode", true)}
+                />
+                <FormInput
+                  label="Pace (min/km)" placeholder="e.g. 5"
+                  value={values.pace}
+                  onChange={(e) => setFieldValue("pace", e.target.value)}
+                  onBlur={() => setFieldTouched("pace", true)}
+                  error={touched.pace && errors.pace ? String(errors.pace) : undefined}
+                />
+              </div>
             </div>
-          </div>
 
-          {/* Location Information */}
-          <div className="bg-gray-50 dark:bg-gray-900 p-4 sm:p-6 rounded-lg space-y-4">
-            <h4 className="text-base font-semibold text-foreground">Location Information</h4>
-            <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
-              <CountriesSelect
-                label="Country" required
-                value={values.country}
-                onChange={(v) => {
-                  setFieldValue("country", v);
-                  setTimeout(() => setFieldTouched("country", true), 0);
-                  if (v !== "Tanzania" && values.region) setFieldValue("region", "");
-                }}
-                error={touched.country && errors.country ? String(errors.country) : undefined}
-              />
-              {values.country === "Tanzania" && (
-                <FormSelect
-                  label="Region" placeholder="Select region" required
-                  options={TANZANIA_REGION_OPTIONS}
-                  value={values.region}
-                  onValueChange={(v) => { setFieldValue("region", v); setTimeout(() => setFieldTouched("region", true), 0); }}
-                  error={touched.region && errors.region ? String(errors.region) : undefined}
+            {/* Emergency Contact */}
+            <div className="bg-gray-50 dark:bg-gray-900 p-4 sm:p-6 rounded-lg space-y-4">
+              <h4 className="text-base font-semibold text-foreground">Emergency Contact</h4>
+              <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
+                <PhoneNumberInput
+                  label="Emergency Phone" placeholder="Add phone number" required
+                  value={values.emergencyPhone}
+                  onChange={(phone) => {
+                    setFieldValue("emergencyPhone", phone);
+                    setFieldTouched("emergencyPhone", true);
+                    setTimeout(() => formik.validateField("emergencyPhone"), 0);
+                  }}
+                  error={touched.emergencyPhone && errors.emergencyPhone ? String(errors.emergencyPhone) : undefined}
                 />
-              )}
-              <FormInput
-                label="City" placeholder="City"
-                value={values.city}
-                onChange={(e) => setFieldValue("city", e.target.value)}
-                onBlur={() => setFieldTouched("city", true)}
-              />
-              <FormInput
-                label="Zip Code" placeholder="Zip code"
-                value={values.zipCode}
-                onChange={(e) => setFieldValue("zipCode", e.target.value)}
-                onBlur={() => setFieldTouched("zipCode", true)}
-              />
-              <FormInput
-                label="Pace (min/km)" placeholder="e.g. 5"
-                value={values.pace}
-                onChange={(e) => setFieldValue("pace", e.target.value)}
-                onBlur={() => setFieldTouched("pace", true)}
-                error={touched.pace && errors.pace ? String(errors.pace) : undefined}
-              />
+              </div>
             </div>
-          </div>
 
-          {/* Emergency Contact */}
-          <div className="bg-gray-50 dark:bg-gray-900 p-4 sm:p-6 rounded-lg space-y-4">
-            <h4 className="text-base font-semibold text-foreground">Emergency Contact</h4>
-            <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
-              <PhoneNumberInput
-                label="Emergency Phone" placeholder="Add phone number" required
-                value={values.emergencyPhone}
-                onChange={(phone) => {
-                  setFieldValue("emergencyPhone", phone);
-                  setFieldTouched("emergencyPhone", true);
-                  setTimeout(() => formik.validateField("emergencyPhone"), 0);
-                }}
-                error={touched.emergencyPhone && errors.emergencyPhone ? String(errors.emergencyPhone) : undefined}
-              />
+            {/* Ticket Selection */}
+            <div className="bg-gray-50 dark:bg-gray-900 p-4 sm:p-6 rounded-lg space-y-4">
+              <h4 className="text-base font-semibold text-foreground">Select Ticket Category</h4>
+              {loadingTickets ? (
+                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
+                  {[1, 2, 3, 4].map((i) => <div key={i} className="h-48 bg-muted animate-pulse rounded-lg" />)}
+                </div>
+              ) : ticketOptions.length === 0 ? (
+                <p className="text-sm text-muted-foreground">No tickets configured for this event.</p>
+              ) : (
+                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
+                  {ticketOptions.map((ticket) => (
+                    <TicketCard
+                      key={ticket.type}
+                      type={ticket.type}
+                      title={ticket.title}
+                      price={ticket.price}
+                      image={ticket.image || "/images/ticket-3.png"}
+                      description={ticket.description}
+                      isSelected={ticket.type in values.selectedTickets}
+                      onClick={() => setFieldValue("selectedTickets", { [ticket.type]: 1 })}
+                    />
+                  ))}
+                </div>
+              )}
             </div>
-          </div>
 
-          {/* Ticket Selection */}
-          <div className="bg-gray-50 dark:bg-gray-900 p-4 sm:p-6 rounded-lg space-y-4">
-            <h4 className="text-base font-semibold text-foreground">Select Ticket Category</h4>
-            {loadingTickets ? (
-              <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
-                {[1, 2, 3, 4].map((i) => <div key={i} className="h-48 bg-muted animate-pulse rounded-lg" />)}
-              </div>
-            ) : ticketOptions.length === 0 ? (
-              <p className="text-sm text-muted-foreground">No tickets configured for this event.</p>
-            ) : (
-              <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
-                {ticketOptions.map((ticket) => (
-                  <TicketCard
-                    key={ticket.type}
-                    type={ticket.type}
-                    title={ticket.title}
-                    price={ticket.price}
-                    image={ticket.image || "/images/ticket-3.png"}
-                    description={ticket.description}
-                    isSelected={ticket.type in values.selectedTickets}
-                    onClick={() => setFieldValue("selectedTickets", { [ticket.type]: 1 })}
-                  />
-                ))}
+            {/* Merchandise Selection */}
+            {eventMerchandise.length > 0 && (
+              <div className="bg-gray-50 dark:bg-gray-900 p-4 sm:p-6 rounded-lg">
+                <InlineMerchandiseSelection
+                  availableMerchandise={eventMerchandise}
+                  selectedMerchandise={values.selectedMerchandise}
+                  onChange={(merchandise) => setFieldValue("selectedMerchandise", merchandise)}
+                  eventId={eventId}
+                />
               </div>
             )}
-          </div>
-
-          {/* Merchandise Selection */}
-          {eventMerchandise.length > 0 && (
-            <div className="bg-gray-50 dark:bg-gray-900 p-4 sm:p-6 rounded-lg">
-              <InlineMerchandiseSelection
-                availableMerchandise={eventMerchandise}
-                selectedMerchandise={values.selectedMerchandise}
-                onChange={(merchandise) => setFieldValue("selectedMerchandise", merchandise)}
-                eventId={eventId}
-              />
-            </div>
-          )}
 
 
-          {/* Admin options (only on last step) */}
-          {isLastStep && (
-            <div className="space-y-3 pt-2 border-t">
-              <FormTextarea
-                label="Admin Note"
-                value={adminNote}
-                onChange={(e) => setAdminNote(e.target.value)}
-                placeholder="Optional note about this registration..."
-                className="min-h-[80px]"
-              />
-              <div className="flex items-center gap-2">
-                <Checkbox id="override-limit" checked={overrideLimitCheck}
-                  onCheckedChange={(c) => setOverrideLimitCheck(c === true)} />
-                <Label htmlFor="override-limit" className="text-muted-foreground font-normal cursor-pointer">
-                  Override attendee limit check
-                </Label>
+            {/* Admin options (only on last step) */}
+            {isLastStep && (
+              <div className="space-y-3 pt-2 border-t">
+                <FormTextarea
+                  label="Admin Note"
+                  value={adminNote}
+                  onChange={(e) => setAdminNote(e.target.value)}
+                  placeholder="Optional note about this registration..."
+                  className="min-h-[80px]"
+                />
+                <div className="flex items-center gap-2">
+                  <Checkbox id="override-limit" checked={overrideLimitCheck}
+                    onCheckedChange={(c) => setOverrideLimitCheck(c === true)} />
+                  <Label htmlFor="override-limit" className="text-muted-foreground font-normal cursor-pointer">
+                    Override attendee limit check
+                  </Label>
+                </div>
               </div>
-            </div>
-          )}
-
-          {/* Navigation buttons */}
-          <div className={`flex items-center pt-4 border-t ${currentStep > 1 ? "justify-between" : "justify-end"}`}>
-            {currentStep > 1 && (
-              <Button type="button" variant="outline" onClick={handlePrevious} className="gap-2">
-                <ChevronLeft className="w-4 h-4" /> Previous
-              </Button>
             )}
-            <div className="flex gap-2">
-              <Button type="button" variant="outline" onClick={onClose}>Cancel</Button>
-              {isLastStep ? (
-                <Button type="button" disabled={submitting} onClick={handleManualSubmit} className="gap-2">
-                  <UserPlus className="h-4 w-4" />
-                  {submitting ? "Registering..." : participantCount > 1 ? `Register ${participantCount} Participants` : "Register"}
-                </Button>
-              ) : (
-                <Button type="button" onClick={handleNext} className="gap-2">
-                  Next <ChevronRight className="w-4 h-4" />
+
+            {/* Navigation buttons */}
+            <div className={`flex items-center pt-4 border-t ${currentStep > 1 ? "justify-between" : "justify-end"}`}>
+              {currentStep > 1 && (
+                <Button type="button" variant="outline" onClick={handlePrevious} className="gap-2">
+                  <ChevronLeft className="w-4 h-4" /> Previous
                 </Button>
               )}
+              <div className="flex gap-2">
+                <Button type="button" variant="outline" onClick={onClose}>Cancel</Button>
+                {isLastStep ? (
+                  <Button type="button" disabled={submitting} onClick={handleManualSubmit} className="gap-2">
+                    <UserPlus className="h-4 w-4" />
+                    {submitting ? "Registering..." : participantCount > 1 ? `Register ${participantCount} Participants` : "Register"}
+                  </Button>
+                ) : (
+                  <Button type="button" onClick={handleNext} className="gap-2">
+                    Next <ChevronRight className="w-4 h-4" />
+                  </Button>
+                )}
+              </div>
             </div>
           </div>
-        </div>
-      </TabsContent>
-
-      {/* ── Excel Upload ── */}
-      <TabsContent value="excel">
-        <form onSubmit={handleExcelUpload} className="space-y-5 pt-2">
-          <div className="rounded-lg border border-dashed p-6 text-center space-y-3">
-            <FileSpreadsheet className="h-10 w-10 text-muted-foreground mx-auto" />
-            <div>
-              <p className="text-sm font-medium">Upload Excel Attendance File</p>
-              <p className="text-xs text-muted-foreground mt-1">
-                Use the official template — it includes dropdowns for ticket types and collection centers.
-              </p>
+        </TabsContent>
+
+        {/* ── Excel Upload ── */}
+        <TabsContent value="excel">
+          <form onSubmit={handleExcelUpload} className="space-y-5 pt-2">
+            <div className="rounded-lg border border-dashed p-6 text-center space-y-3">
+              <FileSpreadsheet className="h-10 w-10 text-muted-foreground mx-auto" />
+              <div>
+                <p className="text-sm font-medium">Upload Excel Attendance File</p>
+                <p className="text-xs text-muted-foreground mt-1">
+                  Use the official template — it includes dropdowns for ticket types and collection centers.
+                </p>
+              </div>
+              <Button type="button" variant="outline" size="sm"
+                onClick={handleDownloadTemplate} disabled={submitting} className="gap-2">
+                <Download className="h-4 w-4" /> Download Template
+              </Button>
             </div>
-            <Button type="button" variant="outline" size="sm"
-              onClick={handleDownloadTemplate} disabled={submitting} className="gap-2">
-              <Download className="h-4 w-4" /> Download Template
-            </Button>
-          </div>
 
-          <div className="space-y-2">
-            <label className="block text-sm font-medium text-foreground">
-              Excel File <span className="text-destructive ml-1">*</span>
-            </label>
-            <div className="flex items-center gap-3 p-3 rounded-md border bg-muted/20 cursor-pointer hover:bg-muted/40 transition-colors"
-              onClick={() => fileInputRef.current?.click()}>
-              <Upload className="h-4 w-4 text-muted-foreground shrink-0" />
-              <span className={`text-sm ${uploadFile ? "text-foreground" : "text-muted-foreground"}`}>
-                {uploadFile ? uploadFile.name : "Click to choose file (.xlsx, .xls)"}
-              </span>
+            <div className="space-y-2">
+              <label className="block text-sm font-medium text-foreground">
+                Excel File <span className="text-destructive ml-1">*</span>
+              </label>
+              <div className="flex items-center gap-3 p-3 rounded-md border bg-muted/20 cursor-pointer hover:bg-muted/40 transition-colors"
+                onClick={() => fileInputRef.current?.click()}>
+                <Upload className="h-4 w-4 text-muted-foreground shrink-0" />
+                <span className={`text-sm ${uploadFile ? "text-foreground" : "text-muted-foreground"}`}>
+                  {uploadFile ? uploadFile.name : "Click to choose file (.xlsx, .xls)"}
+                </span>
+              </div>
+              <input ref={fileInputRef} type="file" accept=".xlsx,.xls" className="hidden"
+                onChange={(e) => setUploadFile(e.target.files?.[0] ?? null)} />
             </div>
-            <input ref={fileInputRef} type="file" accept=".xlsx,.xls" className="hidden"
-              onChange={(e) => setUploadFile(e.target.files?.[0] ?? null)} />
-          </div>
 
-          <div className="flex justify-end gap-2 pt-2 border-t">
-            <Button type="button" variant="outline" onClick={onClose}>Cancel</Button>
-            <Button type="submit" disabled={submitting || !uploadFile} className="gap-2">
-              <Upload className="h-4 w-4" />
-              {submitting ? "Uploading..." : "Upload & Register"}
-            </Button>
-          </div>
-        </form>
-      </TabsContent>
+            <div className="flex justify-end gap-2 pt-2 border-t">
+              <Button type="button" variant="outline" onClick={onClose}>Cancel</Button>
+              <Button type="submit" disabled={submitting || !uploadFile} className="gap-2">
+                <Upload className="h-4 w-4" />
+                {submitting ? "Uploading..." : "Upload & Register"}
+              </Button>
+            </div>
+          </form>
+        </TabsContent>
 
       </Tabs>
     </div>
@@ -1485,7 +1485,7 @@ export function SponsorsTab({ eventId }: SponsorsTabProps) {
       "Provision Favor Sponsor",
       "xl",
       "Creates a new sponsor with an active favor sponsorship — no payment required.",
-      () => {}
+      () => { }
     );
   };
 
@@ -1614,7 +1614,7 @@ function ViewSponsorshipContent({
   useEffect(() => {
     sponsorService.getSponsorRegistrationConfig(sponsorship.id)
       .then((res) => setConfig((res?.data as SponsorRegistrationConfigI) ?? null))
-      .catch(() => {})
+      .catch(() => { })
       .finally(() => setLoading(false));
   }, [sponsorship.id]);
 
diff --git a/src/app/(PageSections)/Dashboard/EventRegistrationPageSection/GroupRegistrationForm.tsx b/src/app/(PageSections)/Dashboard/EventRegistrationPageSection/GroupRegistrationForm.tsx
index e44139d..ec9e039 100644
--- a/src/app/(PageSections)/Dashboard/EventRegistrationPageSection/GroupRegistrationForm.tsx
+++ b/src/app/(PageSections)/Dashboard/EventRegistrationPageSection/GroupRegistrationForm.tsx
@@ -143,56 +143,56 @@ const GroupRegistrationForm = ({ eventId }: GroupRegistrationFormProps) => {
   const handleOpenAttendeeModal = (member?: GroupMemberI) => {
     setEditingMember(member || null);
     openModal(
-     <CollectionCenterProvider>
-      <AttendeeForm
-        groupId={selectedGroupId}
-        member={member}
-        tickets={tickets}
-        eventMerchandise={eventMechandise?.data ?? []}
-        eventId={eventId}
-        onSuccess={async (newMemberId, selectedTicket, selectedMerchandise) => {
-          closeModal();
-          findAllGroupMembers(selectedGroupId);
-          setEditingMember(null);
-          // Auto-select the new member with their chosen ticket (new members only)
-          if (newMemberId && selectedTicket) {
-            formik.setFieldValue("selectedParticipants", [
-              ...formik.values.selectedParticipants,
-              newMemberId,
-            ]);
-            formik.setFieldValue("participantTickets", [
-              ...formik.values.participantTickets,
-              {
-                participantId: newMemberId,
-                ticketId: selectedTicket,
-                selectedMerchandise: selectedMerchandise ?? [],
-              },
-            ]);
-            // If there's an active registration, also add the member to it via BFF
-            if (registrationId) {
-              try {
-                await addMemberToGroupRegistration(registrationId, {
-                  groupMemberId: newMemberId,
-                  selectedTickets: { [selectedTicket]: 1 },
-                  selectedMerchandise: (selectedMerchandise ?? []).map((item) => ({
-                    quantity: item.quantity,
-                    collectionCenterId: item.collectionCenterId || "",
-                  })),
-                });
-              } catch (error) {
-                console.error("Failed to add member to registration:", error);
+      <CollectionCenterProvider>
+        <AttendeeForm
+          groupId={selectedGroupId}
+          member={member}
+          tickets={tickets}
+          eventMerchandise={eventMechandise?.data ?? []}
+          eventId={eventId}
+          onSuccess={async (newMemberId, selectedTicket, selectedMerchandise) => {
+            closeModal();
+            findAllGroupMembers(selectedGroupId);
+            setEditingMember(null);
+            // Auto-select the new member with their chosen ticket (new members only)
+            if (newMemberId && selectedTicket) {
+              formik.setFieldValue("selectedParticipants", [
+                ...formik.values.selectedParticipants,
+                newMemberId,
+              ]);
+              formik.setFieldValue("participantTickets", [
+                ...formik.values.participantTickets,
+                {
+                  participantId: newMemberId,
+                  ticketId: selectedTicket,
+                  selectedMerchandise: selectedMerchandise ?? [],
+                },
+              ]);
+              // If there's an active registration, also add the member to it via BFF
+              if (registrationId) {
+                try {
+                  await addMemberToGroupRegistration(registrationId, {
+                    groupMemberId: newMemberId,
+                    selectedTickets: { [selectedTicket]: 1 },
+                    selectedMerchandise: (selectedMerchandise ?? []).map((item) => ({
+                      quantity: item.quantity,
+                      collectionCenterId: item.collectionCenterId || "",
+                    })),
+                  });
+                } catch (error) {
+                  console.error("Failed to add member to registration:", error);
+                }
               }
             }
-          }
-        }}
-        onCancel={() => {
-          closeModal();
-          setEditingMember(null);
-        }}
-        addAttendeToGroup={addAttendeToGroup}
-        updateAttendeToGroup={updateAttendeToGroup}
-      />
-     </CollectionCenterProvider>,
+          }}
+          onCancel={() => {
+            closeModal();
+            setEditingMember(null);
+          }}
+          addAttendeToGroup={addAttendeToGroup}
+          updateAttendeToGroup={updateAttendeToGroup}
+        />
+      </CollectionCenterProvider>,
       member ? "Edit Attendee" : "Add Attendee",
       "xl",
       member
@@ -630,8 +630,8 @@ const GroupRegistrationForm = ({ eventId }: GroupRegistrationFormProps) => {
                         >
                           <div
                             className={`p-4 rounded-lg border transition-all duration-200 ${isSelected
-                                ? " border-primary-950"
-                                : "bg-white border-gray-200 hover:border-gray-300"
+                              ? " border-primary-950"
+                              : "bg-white border-gray-200 hover:border-gray-300"
                               }`}
                           >
                             {/* Participant Info Row */}
@@ -863,10 +863,14 @@ const baseAttendeeSchema = Yup.object({
   lastName: Yup.string().required("Last name is required"),
   email: Yup.string()
     .transform((val) => (val === "" ? undefined : val))
-    .email("Invalid email")
+    .email("Please enter a valid email address")
+    .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address")
     .optional(),
   phone: Yup.string().required("Phone number is required"),
-  dateOfBirth: Yup.string().required("Date of birth is required"),
+  dateOfBirth: Yup.date().required("Date of birth is required").max(
+      new Date(new Date().setFullYear(new Date().getFullYear() - 2)),
+      "Must be at least 2 years old",
+    ),
   gender: Yup.string().required("Gender is required"),
   country: Yup.string().required("Country is required"),
   region: Yup.string(),
diff --git a/src/app/(PageSections)/Dashboard/EventRegistrationPageSection/IndividualRegistrationForm.tsx b/src/app/(PageSections)/Dashboard/EventRegistrationPageSection/IndividualRegistrationForm.tsx
index bb01d7b..fb59832 100644
--- a/src/app/(PageSections)/Dashboard/EventRegistrationPageSection/IndividualRegistrationForm.tsx
+++ b/src/app/(PageSections)/Dashboard/EventRegistrationPageSection/IndividualRegistrationForm.tsx
@@ -58,7 +58,9 @@ const baseSchemaFields = {
 
   email: Yup.string()
     .transform((val) => (val === "" ? undefined : val))
-    .email("Invalid email address")
+    .email("Please enter a valid email address")
+    .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address")
+
     .optional(),
 
   phone: Yup.string()
@@ -130,7 +132,7 @@ const IndividualRegistrationForm = ({ eventId, supportsGroupRegistration = true
   const { eventTickets, findEventTickets } = useTicket();
   const { isCreateLoading, completeIndividualRegistration } = useEventRegistration();
   const { event } = useEvent();
-  const { eventMechandise, findAllEventMechadise} = useMerchandiseCategory();
+  const { eventMechandise, findAllEventMechadise } = useMerchandiseCategory();
 
   const ticketIdFromQuery = searchParams.get("ticketId");
 
@@ -438,54 +440,54 @@ const IndividualRegistrationForm = ({ eventId, supportsGroupRegistration = true
     };
   };
 
- const handleSimpleRegistrationSubmit = async (values: RegistrationData) => {
-  if (Object.keys(selectedTickets).length === 0) {
-    formik.setFieldTouched("selectedTicket", true);
-    return;
-  }
-
-  const participant: APIParticipant = {
-    firstName: values.firstName,
-    middleName: values.middleName || undefined,
-    lastName: values.lastName,
-    email: values.email || undefined,
-    phone: values.phone,
-    dateOfBirth: values.dateOfBirth || "",
-    gender: values.gender,
-    country: values.country,
-    region: values.region || undefined,
-    pace: values.pace || undefined,
-    emergencyPhone: values.emergencyPhone,
-    selectedTickets: selectedTickets,
-  };
+  const handleSimpleRegistrationSubmit = async (values: RegistrationData) => {
+    if (Object.keys(selectedTickets).length === 0) {
+      formik.setFieldTouched("selectedTicket", true);
+      return;
+    }
 
-  // ✅ FIX: use map instead of reduce (array expected)
-  if (
-    values.selectedMerchandise &&
-    Array.isArray(values.selectedMerchandise) &&
-    values.selectedMerchandise.length > 0
-  ) {
-    participant.selectedMerchandise = values.selectedMerchandise.map(
-      (merch) => ({
-        varietyId: merch.variantId || merch.merchandiseId,
-        quantity: merch.quantity,
-        collectionCenterId: merch.collectionCenterId || "0", // ✅ string
-      })
-    );
-  }
+    const participant: APIParticipant = {
+      firstName: values.firstName,
+      middleName: values.middleName || undefined,
+      lastName: values.lastName,
+      email: values.email || undefined,
+      phone: values.phone,
+      dateOfBirth: values.dateOfBirth || "",
+      gender: values.gender,
+      country: values.country,
+      region: values.region || undefined,
+      pace: values.pace || undefined,
+      emergencyPhone: values.emergencyPhone,
+      selectedTickets: selectedTickets,
+    };
 
-  const apiData: IndividualRegistationI = {
-    eventId: eventId,
-    participants: [participant],
-  };
+    // ✅ FIX: use map instead of reduce (array expected)
+    if (
+      values.selectedMerchandise &&
+      Array.isArray(values.selectedMerchandise) &&
+      values.selectedMerchandise.length > 0
+    ) {
+      participant.selectedMerchandise = values.selectedMerchandise.map(
+        (merch) => ({
+          varietyId: merch.variantId || merch.merchandiseId,
+          quantity: merch.quantity,
+          collectionCenterId: merch.collectionCenterId || "0", // ✅ string
+        })
+      );
+    }
 
-  const response: ApiResponseI<RegistrationResponse> =
-    await completeIndividualRegistration(apiData);
+    const apiData: IndividualRegistationI = {
+      eventId: eventId,
+      participants: [participant],
+    };
 
-  router.push(
-    `/dashboard/event-registration/${eventId}/${response.data?.registrationId}/ticket-checkout`
-  );
-};
+    const response: ApiResponseI<RegistrationResponse> =
+      await completeIndividualRegistration(apiData);
+
+    router.push(
+      `/dashboard/event-registration/${eventId}/${response.data?.registrationId}/ticket-checkout`
+    );
+  };
 
   const handleFinalSubmit = async () => {
     const finalRegistrations = [...registrations];
@@ -559,7 +561,7 @@ const IndividualRegistrationForm = ({ eventId, supportsGroupRegistration = true
           }
           ticketSelectionError={
             formik.submitCount > 0 &&
-            Object.keys(selectedTickets).length === 0
+              Object.keys(selectedTickets).length === 0
               ? "Please select at least one ticket"
               : undefined
           }
@@ -568,13 +570,12 @@ const IndividualRegistrationForm = ({ eventId, supportsGroupRegistration = true
         />
 
         <div
-          className={`flex items-center pt-6 border-t mt-8 ${
-            !supportsGroupRegistration ||
-            ticketQuantity === 1 ||
-            currentStep === 1
+          className={`flex items-center pt-6 border-t mt-8 ${!supportsGroupRegistration ||
+              ticketQuantity === 1 ||
+              currentStep === 1
               ? "justify-end"
               : "justify-between"
-          }`}
+            }`}
         >
           {supportsGroupRegistration && currentStep > 1 && (
             <Button
@@ -591,8 +592,8 @@ const IndividualRegistrationForm = ({ eventId, supportsGroupRegistration = true
 
           <div className="flex space-x-4">
             {!supportsGroupRegistration ||
-            ticketQuantity === 1 ||
-            isLastStep ? (
+              ticketQuantity === 1 ||
+              isLastStep ? (
               <Button
                 type="submit"
                 disabled={shouldDisableButtons}
diff --git a/src/app/(PageSections)/Dashboard/FeaturedPeoplePageSections/CreateFeaturedPersonPageSection.tsx b/src/app/(PageSections)/Dashboard/FeaturedPeoplePageSections/CreateFeaturedPersonPageSection.tsx
index 86fb4cc..05426ed 100644
--- a/src/app/(PageSections)/Dashboard/FeaturedPeoplePageSections/CreateFeaturedPersonPageSection.tsx
+++ b/src/app/(PageSections)/Dashboard/FeaturedPeoplePageSections/CreateFeaturedPersonPageSection.tsx
@@ -54,7 +54,9 @@ const featuredPersonValidationSchema = Yup.object().shape({
     .url("Must be a valid URL")
     .required("Image URL is required"),
   email: Yup.string()
-    .email("Must be a valid email")
+    .email("Please enter a valid email address")
+    .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address")
+
     .required("Email is required"),
   phone: Yup.string().required("Phone is required"),
   socialMediaLinks: Yup.string(),
@@ -190,186 +192,186 @@ function CreateFeaturedPersonContent() {
           >
             {({ values, errors, touched, setFieldValue, handleChange, handleBlur, isSubmitting, isValid }) => {
               return (
-              <Form className="space-y-4">
+                <Form className="space-y-4">
+
+                  <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
+                    {/* Event Selection */}
+                    <div className="space-y-2">
+                      <UniversalSelect
+                        id="eventId"
+                        options={eventOptions}
+                        value={values.eventId}
+                        onValueChange={(value) =>
+                          setFieldValue(
+                            "eventId",
+                            Array.isArray(value) ? value[0] : value
+                          )
+                        }
+                        placeholder="Select event"
+                        label="Event"
+                        required
+                      />
+                      {touched.eventId && errors.eventId && (
+                        <p className="text-sm text-red-500">{errors.eventId}</p>
+                      )}
+                    </div>
 
-                <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
-                  {/* Event Selection */}
-                  <div className="space-y-2">
-                    <UniversalSelect
-                      id="eventId"
-                      options={eventOptions}
-                      value={values.eventId}
-                      onValueChange={(value) =>
-                        setFieldValue(
-                          "eventId",
-                          Array.isArray(value) ? value[0] : value
-                        )
-                      }
-                      placeholder="Select event"
-                      label="Event"
+                    <FormInput
+                      id="name"
+                      name="name"
+                      label="Name"
+                      type="text"
+                      placeholder="Enter name"
+                      value={values.name}
+                      onChange={handleChange}
+                      onBlur={handleBlur}
+                      error={touched.name && errors.name ? errors.name : undefined}
                       required
                     />
-                    {touched.eventId && errors.eventId && (
-                      <p className="text-sm text-red-500">{errors.eventId}</p>
-                    )}
-                  </div>
 
-                  <FormInput
-                    id="name"
-                    name="name"
-                    label="Name"
-                    type="text"
-                    placeholder="Enter name"
-                    value={values.name}
-                    onChange={handleChange}
-                    onBlur={handleBlur}
-                    error={touched.name && errors.name ? errors.name : undefined}
-                    required
-                  />
-
-                  <FormInput
-                    id="title"
-                    name="title"
-                    label="Title"
-                    type="text"
-                    placeholder="Enter title"
-                    value={values.title}
-                    onChange={handleChange}
-                    onBlur={handleBlur}
-                    error={touched.title && errors.title ? errors.title : undefined}
-                    required
-                  />
-
-                  <FormInput
-                    id="organization"
-                    name="organization"
-                    label="Organization"
-                    type="text"
-                    placeholder="Enter organization"
-                    value={values.organization}
-                    onChange={handleChange}
-                    onBlur={handleBlur}
-                    error={touched.organization && errors.organization ? errors.organization : undefined}
-                    required
-                  />
+                    <FormInput
+                      id="title"
+                      name="title"
+                      label="Title"
+                      type="text"
+                      placeholder="Enter title"
+                      value={values.title}
+                      onChange={handleChange}
+                      onBlur={handleBlur}
+                      error={touched.title && errors.title ? errors.title : undefined}
+                      required
+                    />
 
-                  <FormInput
-                    id="email"
-                    name="email"
-                    label="Email"
-                    type="email"
-                    placeholder="Enter email"
-                    value={values.email}
-                    onChange={handleChange}
-                    onBlur={handleBlur}
-                    error={touched.email && errors.email ? errors.email : undefined}
-                    required
-                  />
+                    <FormInput
+                      id="organization"
+                      name="organization"
+                      label="Organization"
+                      type="text"
+                      placeholder="Enter organization"
+                      value={values.organization}
+                      onChange={handleChange}
+                      onBlur={handleBlur}
+                      error={touched.organization && errors.organization ? errors.organization : undefined}
+                      required
+                    />
 
-                  <FormInput
-                    id="phone"
-                    name="phone"
-                    label="Phone"
-                    type="text"
-                    placeholder="Enter phone"
-                    value={values.phone}
-                    onChange={handleChange}
-                    onBlur={handleBlur}
-                    error={touched.phone && errors.phone ? errors.phone : undefined}
-                    required
-                  />
+                    <FormInput
+                      id="email"
+                      name="email"
+                      label="Email"
+                      type="email"
+                      placeholder="Enter email"
+                      value={values.email}
+                      onChange={handleChange}
+                      onBlur={handleBlur}
+                      error={touched.email && errors.email ? errors.email : undefined}
+                      required
+                    />
 
-                  <FormInput
-                    id="displayOrder"
-                    name="displayOrder"
-                    label="Display Order"
-                    type="number"
-                    placeholder="Enter display order"
-                    value={values.displayOrder}
-                    onChange={handleChange}
-                    onBlur={handleBlur}
-                    error={touched.displayOrder && errors.displayOrder ? errors.displayOrder : undefined}
-                    required
-                  />
-                </div>
+                    <FormInput
+                      id="phone"
+                      name="phone"
+                      label="Phone"
+                      type="text"
+                      placeholder="Enter phone"
+                      value={values.phone}
+                      onChange={handleChange}
+                      onBlur={handleBlur}
+                      error={touched.phone && errors.phone ? errors.phone : undefined}
+                      required
+                    />
 
-                <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
-                  <FormTextarea
-                    id="biography"
-                    name="biography"
-                    label="Biography"
-                    placeholder="Enter biography"
-                    value={values.biography}
-                    onChange={handleChange}
-                    onBlur={handleBlur}
-                    error={touched.biography && errors.biography ? errors.biography : undefined}
-                    required
-                    rows={3}
-                  />
+                    <FormInput
+                      id="displayOrder"
+                      name="displayOrder"
+                      label="Display Order"
+                      type="number"
+                      placeholder="Enter display order"
+                      value={values.displayOrder}
+                      onChange={handleChange}
+                      onBlur={handleBlur}
+                      error={touched.displayOrder && errors.displayOrder ? errors.displayOrder : undefined}
+                      required
+                    />
+                  </div>
 
-                  <FormTextarea
-                    id="socialMediaLinks"
-                    name="socialMediaLinks"
-                    label="Social Media Links"
-                    placeholder="Enter social media links (comma separated)"
-                    value={values.socialMediaLinks}
-                    onChange={handleChange}
-                    onBlur={handleBlur}
-                    error={touched.socialMediaLinks && errors.socialMediaLinks ? errors.socialMediaLinks : undefined}
-                    rows={3}
-                  />
-                </div>
+                  <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
+                    <FormTextarea
+                      id="biography"
+                      name="biography"
+                      label="Biography"
+                      placeholder="Enter biography"
+                      value={values.biography}
+                      onChange={handleChange}
+                      onBlur={handleBlur}
+                      error={touched.biography && errors.biography ? errors.biography : undefined}
+                      required
+                      rows={3}
+                    />
 
-                <div className="grid grid-cols-1 md:grid-cols-2 gap-4 items-start">
-                  {/* Image Upload */}
-                  <div className="space-y-2">
-                    <Label>Featured Image *</Label>
-                    <ImageUploader
-                      onChange={(file) =>
-                        handleImageUpload(file, setFieldValue)
-                      }
-                      previewUrl={values.imageUrl}
-                      isUploading={isUploading}
-                      uploadStatus={uploadStatus}
-                      showTitle={false}
+                    <FormTextarea
+                      id="socialMediaLinks"
+                      name="socialMediaLinks"
+                      label="Social Media Links"
+                      placeholder="Enter social media links (comma separated)"
+                      value={values.socialMediaLinks}
+                      onChange={handleChange}
+                      onBlur={handleBlur}
+                      error={touched.socialMediaLinks && errors.socialMediaLinks ? errors.socialMediaLinks : undefined}
+                      rows={3}
                     />
-                    {touched.imageUrl && errors.imageUrl && (
-                      <p className="text-sm text-red-500">{errors.imageUrl}</p>
-                    )}
                   </div>
 
-                  {/* Is Primary Toggle */}
-                  <div className="flex items-center space-x-2 md:pt-8">
-                    <Switch
-                      id="isPrimary"
-                      checked={values.isPrimary}
-                      onCheckedChange={(checked) =>
-                        setFieldValue("isPrimary", checked)
-                      }
-                    />
-                    <Label htmlFor="isPrimary" className="cursor-pointer">
-                      Mark as Primary Featured Person
-                    </Label>
+                  <div className="grid grid-cols-1 md:grid-cols-2 gap-4 items-start">
+                    {/* Image Upload */}
+                    <div className="space-y-2">
+                      <Label>Featured Image *</Label>
+                      <ImageUploader
+                        onChange={(file) =>
+                          handleImageUpload(file, setFieldValue)
+                        }
+                        previewUrl={values.imageUrl}
+                        isUploading={isUploading}
+                        uploadStatus={uploadStatus}
+                        showTitle={false}
+                      />
+                      {touched.imageUrl && errors.imageUrl && (
+                        <p className="text-sm text-red-500">{errors.imageUrl}</p>
+                      )}
+                    </div>
+
+                    {/* Is Primary Toggle */}
+                    <div className="flex items-center space-x-2 md:pt-8">
+                      <Switch
+                        id="isPrimary"
+                        checked={values.isPrimary}
+                        onCheckedChange={(checked) =>
+                          setFieldValue("isPrimary", checked)
+                        }
+                      />
+                      <Label htmlFor="isPrimary" className="cursor-pointer">
+                        Mark as Primary Featured Person
+                      </Label>
+                    </div>
                   </div>
-                </div>
 
-                {/* Form Actions */}
-                <div className="flex gap-4 justify-end pt-2">
-                  <Button
-                    type="button"
-                    variant="outline"
-                    onClick={() => router.push("/dashboard/featured-people")}
-                  >
-                    Cancel
-                  </Button>
-                  <Button
-                    type="submit"
-                    disabled={isLoading || isUploading || isSubmitting || !isValid}
-                  >
-                    {isLoading || isSubmitting ? "Creating..." : "Create Featured Person"}
-                  </Button>
-                </div>
-              </Form>
+                  {/* Form Actions */}
+                  <div className="flex gap-4 justify-end pt-2">
+                    <Button
+                      type="button"
+                      variant="outline"
+                      onClick={() => router.push("/dashboard/featured-people")}
+                    >
+                      Cancel
+                    </Button>
+                    <Button
+                      type="submit"
+                      disabled={isLoading || isUploading || isSubmitting || !isValid}
+                    >
+                      {isLoading || isSubmitting ? "Creating..." : "Create Featured Person"}
+                    </Button>
+                  </div>
+                </Form>
               );
             }}
           </Formik>
diff --git a/src/app/(PageSections)/Dashboard/FeaturedPeoplePageSections/EditFeaturedPersonPageSection.tsx b/src/app/(PageSections)/Dashboard/FeaturedPeoplePageSections/EditFeaturedPersonPageSection.tsx
index 169efb9..77dcf54 100644
--- a/src/app/(PageSections)/Dashboard/FeaturedPeoplePageSections/EditFeaturedPersonPageSection.tsx
+++ b/src/app/(PageSections)/Dashboard/FeaturedPeoplePageSections/EditFeaturedPersonPageSection.tsx
@@ -51,7 +51,9 @@ const featuredPersonValidationSchema = Yup.object().shape({
   imageUrl: Yup.string()
     .url("Must be a valid URL")
     .required("Image URL is required"),
-  email: Yup.string().email("Must be a valid email").required("Email is required"),
+  email: Yup.string().email("Please enter a valid email address")
+    .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address")
+    .required("Email is required"),
   phone: Yup.string().required("Phone is required"),
   socialMediaLinks: Yup.string(),
   displayOrder: Yup.number()
diff --git a/src/app/(PageSections)/Dashboard/GroupPageSection.tsx/AttendeeForm.tsx b/src/app/(PageSections)/Dashboard/GroupPageSection.tsx/AttendeeForm.tsx
index ee70950..22cec06 100644
--- a/src/app/(PageSections)/Dashboard/GroupPageSection.tsx/AttendeeForm.tsx
+++ b/src/app/(PageSections)/Dashboard/GroupPageSection.tsx/AttendeeForm.tsx
@@ -4,6 +4,7 @@ import PhoneNumberInput from "@/components/ui/phone-number-input";
 import { FormSelect } from "@/components/ui/form-select";
 import CountriesSelect from "@/components/ui/countries-select";
 import FormDatePicker from "@/components/ui/form-date-picker";
+import { today, getLocalTimeZone } from "@internationalized/date";
 import { TANZANIA_REGION_OPTIONS } from "@/constants/tanzania-regions";
 import { GroupMemberI, } from "@/types/group";
 import { ApiResponseI } from "@/types/api";
@@ -26,10 +27,15 @@ interface AttendeeFormProps {
 const attendeeValidationSchema = Yup.object({
   firstName: Yup.string().required("First name is required"),
   lastName: Yup.string().required("Last name is required"),
-  email: Yup.string().email("Invalid email").optional(),
-  phone: Yup.string(),
-  dateOfBirth: Yup.string(),
-  gender: Yup.string(),
+  email: Yup.string().email("Please enter a valid email address")
+    .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address")
+    .optional(),
+  phone: Yup.string().required("Phone Number is Required"),
+  dateOfBirth: Yup.date().required("Date of Birth required")  .max(
+      new Date(new Date().setFullYear(new Date().getFullYear() - 2)),
+      "Must be at least 2 years old",
+    ),
+  gender: Yup.string().required("gender is required"),
   country: Yup.string().required("Country is required"),
   region: Yup.string().when("country", {
     is: "Tanzania",
@@ -45,7 +51,7 @@ const attendeeValidationSchema = Yup.object({
       return Number.isInteger(num) && num >= 3 && num <= 10;
     }
   ).optional(),
-  emergencyContactPhone: Yup.string().test(
+  emergencyContactPhone: Yup.string().required("Emergency Contact Phone is required").test(
     "different-from-phone",
     "Emergency phone must be different from phone number",
     function (value) {
@@ -148,6 +154,7 @@ const AttendeeForm = ({
 
         <PhoneNumberInput
           id="phone"
+          required
           label="Phone Number"
           value={formik.values.phone}
           onChange={(value) => formik.setFieldValue("phone", value)}
@@ -158,10 +165,12 @@ const AttendeeForm = ({
         <FormDatePicker
           id="dateOfBirth"
           label="Date of Birth"
+          required
           value={formik.values.dateOfBirth ? parseDate(formik.values.dateOfBirth) : undefined}
           onChange={(date: DateValue | null) => {
             formik.setFieldValue("dateOfBirth", date ? date.toString() : "");
           }}
+          maxValue={today(getLocalTimeZone()).subtract({ years: 2 })}
           error={formik.touched.dateOfBirth && formik.errors.dateOfBirth ? formik.errors.dateOfBirth : undefined}
         />
 
@@ -169,6 +178,7 @@ const AttendeeForm = ({
           id="gender"
           name="gender"
           label="Gender"
+          required
           value={formik.values.gender}
           onValueChange={(value) => formik.setFieldValue("gender", value)}
           options={genderOptions}
@@ -219,6 +229,7 @@ const AttendeeForm = ({
         <PhoneNumberInput
           id="emergencyContactPhone"
           label="Emergency Contact Phone"
+          required
           value={formik.values.emergencyContactPhone}
           onChange={(value) => formik.setFieldValue("emergencyContactPhone", value)}
           placeholder="Enter emergency contact phone"
diff --git a/src/app/(PageSections)/Dashboard/GroupPageSection.tsx/CreateGroupPageSection.tsx b/src/app/(PageSections)/Dashboard/GroupPageSection.tsx/CreateGroupPageSection.tsx
index ae36e6c..ce5e940 100644
--- a/src/app/(PageSections)/Dashboard/GroupPageSection.tsx/CreateGroupPageSection.tsx
+++ b/src/app/(PageSections)/Dashboard/GroupPageSection.tsx/CreateGroupPageSection.tsx
@@ -47,28 +47,29 @@ const getValidationSchema = (isAdminOrPartner: boolean) =>
       : Yup.string().optional(),
     ownerFirstName: isAdminOrPartner
       ? Yup.string()
-          .required("First name is required")
-          .min(2, "First name must be at least 2 characters")
-          .max(50, "First name must not exceed 50 characters")
+        .required("First name is required")
+        .min(2, "First name must be at least 2 characters")
+        .max(50, "First name must not exceed 50 characters")
       : Yup.string().optional(),
     ownerLastName: isAdminOrPartner
       ? Yup.string()
-          .required("Last name is required")
-          .min(2, "Last name must be at least 2 characters")
-          .max(50, "Last name must not exceed 50 characters")
+        .required("Last name is required")
+        .min(2, "Last name must be at least 2 characters")
+        .max(50, "Last name must not exceed 50 characters")
       : Yup.string().optional(),
     ownerEmail: isAdminOrPartner
       ? Yup.string()
-          .required("Email is required")
-          .email("Please enter a valid email address")
+        .required("Email is required")
+        .email("Please enter a valid email address")
+        .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address")
       : Yup.string().optional(),
     ownerPhone: isAdminOrPartner
       ? Yup.string()
-          .required("Phone number is required")
-          .test("valid-phone", "Please enter a valid phone number", (value) => {
-            if (!value) return false;
-            return isValidPhoneNumber(value);
-          })
+        .required("Phone number is required")
+        .test("valid-phone", "Please enter a valid phone number", (value) => {
+          if (!value) return false;
+          return isValidPhoneNumber(value);
+        })
       : Yup.string().optional(),
   });
 
@@ -234,7 +235,7 @@ function CreateGroupPageSection({
                       onBlur={formik.handleBlur}
                       error={
                         formik.touched.ownerFirstName &&
-                        formik.errors.ownerFirstName
+                          formik.errors.ownerFirstName
                           ? formik.errors.ownerFirstName
                           : undefined
                       }
@@ -249,7 +250,7 @@ function CreateGroupPageSection({
                       onBlur={formik.handleBlur}
                       error={
                         formik.touched.ownerLastName &&
-                        formik.errors.ownerLastName
+                          formik.errors.ownerLastName
                           ? formik.errors.ownerLastName
                           : undefined
                       }
diff --git a/src/app/(PageSections)/Dashboard/InsurancePageSections/ApplyInsuranceForm.tsx b/src/app/(PageSections)/Dashboard/InsurancePageSections/ApplyInsuranceForm.tsx
index eccda2d..0575816 100644
--- a/src/app/(PageSections)/Dashboard/InsurancePageSections/ApplyInsuranceForm.tsx
+++ b/src/app/(PageSections)/Dashboard/InsurancePageSections/ApplyInsuranceForm.tsx
@@ -22,7 +22,8 @@ const validationSchema = Yup.object({
     .required("Phone number is required"),
 
   email: Yup.string()
-    .email("Invalid email address")
+    .email("Please enter a valid email address")
+    .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address")
     .required("Email is required"),
   dateOfBirth: Yup.date()
     .nullable()
@@ -182,8 +183,8 @@ function ApplyInsuranceForm() {
                 value={
                   formik.values.dateOfBirth
                     ? parseDate(
-                        formik.values.dateOfBirth.toISOString().split("T")[0]
-                      )
+                      formik.values.dateOfBirth.toISOString().split("T")[0]
+                    )
                     : undefined
                 }
                 onChange={(date) =>
diff --git a/src/app/(PageSections)/Dashboard/PartnerManagementPageSection/CreatePartnerForm.tsx b/src/app/(PageSections)/Dashboard/PartnerManagementPageSection/CreatePartnerForm.tsx
index 3930b23..8c2d7c4 100644
--- a/src/app/(PageSections)/Dashboard/PartnerManagementPageSection/CreatePartnerForm.tsx
+++ b/src/app/(PageSections)/Dashboard/PartnerManagementPageSection/CreatePartnerForm.tsx
@@ -17,7 +17,7 @@ interface Partner {
   id?: string;
   name: string;
   description: string;
-  partnerType:  "EXTERNAL_PARTNER" | "INTERNAL_PARTNER" | "MAIN_PARTNER";
+  partnerType: "EXTERNAL_PARTNER" | "INTERNAL_PARTNER" | "MAIN_PARTNER";
   websiteUrl: string;
   logoUrl: string;
   industry: string;
@@ -52,7 +52,7 @@ const getPartnerSchema = () => {
       .required("Name is required"),
     description: Yup.string().required("Description is required"),
     partnerType: Yup.string()
-      .oneOf(["EXTERNAL_PARTNER", "INTERNAL_PARTNER","MAIN_PARTNER"])
+      .oneOf(["EXTERNAL_PARTNER", "INTERNAL_PARTNER", "MAIN_PARTNER"])
       .required("Partner type is required"),
     websiteUrl: Yup.string().url("Must be a valid URL").required("Website URL is required"),
     logoUrl: Yup.string().required("Logo is required"),
@@ -64,7 +64,7 @@ const getPartnerSchema = () => {
     country: Yup.string().required("Country is required"),
     contactPersonFirstName: Yup.string().required("First name is required"),
     contactPersonLastName: Yup.string().required("Last name is required"),
-    contactEmail: Yup.string().email("Invalid email address").required("Contact email is required"),
+    contactEmail: Yup.string().email("Please enter a valid email address").matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address").required("Contact email is required"),
     contactPhone: Yup.string().required("Contact phone is required"),
     notes: Yup.string().optional(),
   });
@@ -137,14 +137,14 @@ function PartnerForm({
         throw new Error("Failed to get upload URL");
       }
     } catch (error) {
-     throw error
+      throw error
     } finally {
       setUploadingLogo(false);
     }
   };
 
   const handleSubmit = async (values: ReturnType<typeof getInitialValues>) => {
-      console.log("Request Data",values)
+    console.log("Request Data", values)
     try {
       const requestData = {
         name: values.name,
@@ -357,78 +357,78 @@ function PartnerForm({
               <div className="border-t pt-4 mt-4">
                 <h3 className="text-md font-semibold mb-3">Contact Information</h3>
 
-           <div className="space-y-3">
-                 <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
-                   <FormInput
-                     label="First Name"
-                     placeholder="Enter first name"
-                     required
-                     value={values.contactPersonFirstName}
-                     onChange={(e) =>
-                       setFieldValue("contactPersonFirstName", e.target.value)
-                     }
-                     onBlur={() => setFieldTouched("contactPersonFirstName", true)}
-                     error={
-                       touched.contactPersonFirstName && errors.contactPersonFirstName
-                         ? errors.contactPersonFirstName
-                         : undefined
-                     }
-                   />
-
-                   <FormInput
-                     label="Last Name"
-                     placeholder="Enter last name"
-                     required
-                     value={values.contactPersonLastName}
-                     onChange={(e) =>
-                       setFieldValue("contactPersonLastName", e.target.value)
-                     }
-                     onBlur={() => setFieldTouched("contactPersonLastName", true)}
-                     error={
-                       touched.contactPersonLastName && errors.contactPersonLastName
-                         ? errors.contactPersonLastName
-                         : undefined
-                     }
-                   />
-                 </div>
-
-                   <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
-                  <FormInput
-                    label="Contact Email"
-                    placeholder="contact@example.com"
-                    type="email"
-                    required
-                    value={values.contactEmail}
-                    onChange={(e) =>
-                      setFieldValue("contactEmail", e.target.value)
-                    }
-                    onBlur={() => setFieldTouched("contactEmail", true)}
-                    error={
-                      touched.contactEmail && errors.contactEmail
-                        ? errors.contactEmail
-                        : undefined
-                    }
-                  />
-
-                  <FormInput
-                    label="Contact Phone"
-                    placeholder="Enter phone number"
-                    required
-                    value={values.contactPhone}
-                    onChange={(e) =>
-                      setFieldValue("contactPhone", e.target.value)
-                    }
-                    onBlur={() => setFieldTouched("contactPhone", true)}
-                    error={
-                      touched.contactPhone && errors.contactPhone
-                        ? errors.contactPhone
-                        : undefined
-                    }
-                  />
+                <div className="space-y-3">
+                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
+                    <FormInput
+                      label="First Name"
+                      placeholder="Enter first name"
+                      required
+                      value={values.contactPersonFirstName}
+                      onChange={(e) =>
+                        setFieldValue("contactPersonFirstName", e.target.value)
+                      }
+                      onBlur={() => setFieldTouched("contactPersonFirstName", true)}
+                      error={
+                        touched.contactPersonFirstName && errors.contactPersonFirstName
+                          ? errors.contactPersonFirstName
+                          : undefined
+                      }
+                    />
+
+                    <FormInput
+                      label="Last Name"
+                      placeholder="Enter last name"
+                      required
+                      value={values.contactPersonLastName}
+                      onChange={(e) =>
+                        setFieldValue("contactPersonLastName", e.target.value)
+                      }
+                      onBlur={() => setFieldTouched("contactPersonLastName", true)}
+                      error={
+                        touched.contactPersonLastName && errors.contactPersonLastName
+                          ? errors.contactPersonLastName
+                          : undefined
+                      }
+                    />
+                  </div>
+
+                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
+                    <FormInput
+                      label="Contact Email"
+                      placeholder="contact@example.com"
+                      type="email"
+                      required
+                      value={values.contactEmail}
+                      onChange={(e) =>
+                        setFieldValue("contactEmail", e.target.value)
+                      }
+                      onBlur={() => setFieldTouched("contactEmail", true)}
+                      error={
+                        touched.contactEmail && errors.contactEmail
+                          ? errors.contactEmail
+                          : undefined
+                      }
+                    />
+
+                    <FormInput
+                      label="Contact Phone"
+                      placeholder="Enter phone number"
+                      required
+                      value={values.contactPhone}
+                      onChange={(e) =>
+                        setFieldValue("contactPhone", e.target.value)
+                      }
+                      onBlur={() => setFieldTouched("contactPhone", true)}
+                      error={
+                        touched.contactPhone && errors.contactPhone
+                          ? errors.contactPhone
+                          : undefined
+                      }
+                    />
+                  </div>
                 </div>
-           </div>
 
-             
+
               </div>
 
               <FormInput
@@ -454,8 +454,8 @@ function PartnerForm({
                       ? "Updating..."
                       : "Creating..."
                     : isEdit
-                    ? "Update Partner"
-                    : "Create Partner"}
+                      ? "Update Partner"
+                      : "Create Partner"}
                 </Button>
               </div>
             </Form>
diff --git a/src/app/(PageSections)/Dashboard/ProductSchemaPageSection/components/DynamicForm.tsx b/src/app/(PageSections)/Dashboard/ProductSchemaPageSection/components/DynamicForm.tsx
index 222b430..76f02a2 100644
--- a/src/app/(PageSections)/Dashboard/ProductSchemaPageSection/components/DynamicForm.tsx
+++ b/src/app/(PageSections)/Dashboard/ProductSchemaPageSection/components/DynamicForm.tsx
@@ -56,7 +56,7 @@ const createYupSchema = (
     switch (property.type) {
       case 'string':
         if (property.format === 'email') {
-          yupType = Yup.string().email('Invalid email format');
+          yupType = Yup.string().email("Please enter a valid email address").matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address");
         } else if (property.format === 'date' || property.format === 'date-time') {
           yupType = Yup.date();
         } else if (property.enum) {
diff --git a/src/app/(PageSections)/Dashboard/RegistrationPageSection/RegistrationPageSection.tsx b/src/app/(PageSections)/Dashboard/RegistrationPageSection/RegistrationPageSection.tsx
index bdd5350..294c6f8 100644
--- a/src/app/(PageSections)/Dashboard/RegistrationPageSection/RegistrationPageSection.tsx
+++ b/src/app/(PageSections)/Dashboard/RegistrationPageSection/RegistrationPageSection.tsx
@@ -137,7 +137,7 @@ function RegistrationPageSection() {
   const { openModal, closeModal } = useModal();
 
   const [sheetOpen, setSheetOpen] = useState(false);
-  const [selectedRegistration, setSelectedRegistration] = useState<RegistrationResponseI | null>(null);
+  const [selectedRegistrationId, setSelectedRegistrationId] = useState<string | null>(null);
 
   const [adminRegistrationsData, setAdminRegistrationsData] = useState<ApiResponseI<RegistrationResponseI[]>>({});
   const [userRegistrationsData, setUserRegistrationsData] = useState<ApiResponseI<RegistrationListItemI[]>>({});
@@ -259,32 +259,16 @@ function RegistrationPageSection() {
   };
 
   const handleViewRegistration = (item: RegistrationResponseI | RegistrationListItemI) => {
-    const isFullData = "attendeeDetails" in item;
-    if (isFullData) {
-      setSelectedRegistration(item as RegistrationResponseI);
-    } else {
-      // User view: wrap RegistrationListItemI into a minimal RegistrationResponseI shape
-      const reg = item as RegistrationListItemI;
-      setSelectedRegistration({
-        registration: reg as unknown as RegistrationResponseI["registration"],
-        attendeeDetails: [],
-        registrationTotal: 0,
-        totalTickets: 0,
-        paymentStatus: reg.status,
-      });
-    }
+    const id = "attendeeDetails" in item
+      ? (item as RegistrationResponseI).registration?.id
+      : (item as RegistrationListItemI).id;
+    setSelectedRegistrationId(id != null ? String(id) : null);
     setSheetOpen(true);
   };
 
   const handleSheetRefresh = async () => {
     if (isAdmin || isPartner) {
-      const response = await fetchAdminRegistrations(filters);
-      if (selectedRegistration && response?.data) {
-        const updated = response.data.find(
-          (r) => r.registration?.id === selectedRegistration.registration?.id
-        );
-        if (updated) setSelectedRegistration(updated);
-      }
+      await fetchAdminRegistrations(filters);
     } else {
       await fetchUserRegistrations();
     }
@@ -304,7 +288,7 @@ function RegistrationPageSection() {
 
   const getCustomActions = (item: RegistrationResponseI) => {
     const status = item.registration?.status;
-    if (status !== "COMPLETED") return [];
+    if (status !== "COMPLETED" || item.paymentStatus === "PAID") return [];
     return [
       {
         id: "send-control-number",
@@ -527,18 +511,7 @@ function RegistrationPageSection() {
   if (isAdmin || isPartner) {
     return (
       <div>
-        {/* Header */}
-        <div className="flex items-center justify-between mb-[var(--dash-header-mb)]">
-          <h1 className="text-[var(--dash-heading)] font-semibold text-primary-950">
-            {isAdmin ? "All Registrations" : "Event Registrations"}
-          </h1>
-          {!isAdmin && (
-            <Link href="/events">
-              <Button>Register For Event</Button>
-            </Link>
-          )}
-        </div>
-
+    
         {/* Data Table with Server-side Pagination */}
         <DataTable
           data={allAdminRegistrations}
@@ -576,6 +549,7 @@ function RegistrationPageSection() {
           enableDelete={false}
           isLoading={isLoading}
           customFilterComponent={eventFilterComponent}
+          title="Event Registrations"
           // Server-side pagination props
           totalCount={adminRegistrationsData?.pagination?.totalCount}
           currentPage={adminRegistrationsData?.pagination?.currentPage}
@@ -589,7 +563,7 @@ function RegistrationPageSection() {
         />
 
         <RegistrationSideSheet
-          registrationData={selectedRegistration}
+          registrationId={selectedRegistrationId}
           open={sheetOpen}
           onClose={() => setSheetOpen(false)}
           onRefresh={handleSheetRefresh}
@@ -742,7 +716,7 @@ function RegistrationPageSection() {
       </div>
 
       <RegistrationSideSheet
-        registrationData={selectedRegistration}
+        registrationId={selectedRegistrationId}
         open={sheetOpen}
         onClose={() => setSheetOpen(false)}
         onRefresh={handleSheetRefresh}
diff --git a/src/app/(PageSections)/Dashboard/RegistrationPageSection/RegistrationSideSheet.tsx b/src/app/(PageSections)/Dashboard/RegistrationPageSection/RegistrationSideSheet.tsx
index ee7e2b5..b829658 100644
--- a/src/app/(PageSections)/Dashboard/RegistrationPageSection/RegistrationSideSheet.tsx
+++ b/src/app/(PageSections)/Dashboard/RegistrationPageSection/RegistrationSideSheet.tsx
@@ -13,7 +13,12 @@ import { Skeleton } from "@/components/ui/skeleton";
 import { FormInput } from "@/components/ui/form-input";
 import { FormSelect } from "@/components/ui/form-select";
 import { FormDatePicker } from "@/components/ui/form-date-picker";
-import { parseDate } from "@internationalized/date";
+import { parseDate, today, getLocalTimeZone } from "@internationalized/date";
+import { useFormik } from "formik";
+import * as Yup from "yup";
+import PhoneNumberInput from "@/components/ui/phone-number-input";
+import CountriesSelect from "@/components/ui/countries-select";
+import { TANZANIA_REGION_OPTIONS } from "@/constants/tanzania-regions";
 import {
   RegistrationResponseI,
   AttendeeDetailI,
@@ -52,7 +57,7 @@ import {
 // ─── Types ────────────────────────────────────────────────────────────────────
 
 interface RegistrationSideSheetProps {
-  registrationData: RegistrationResponseI | null;
+  registrationId: string | null;
   open: boolean;
   onClose: () => void;
   onRefresh?: () => void;
@@ -103,6 +108,54 @@ function safeParseDate(dateStr?: string | null) {
   try { return parseDate(dateStr.slice(0, 10)); } catch { return undefined; }
 }
 
+// ─── Shared Phone Validator ───────────────────────────────────────────────────
+
+const isValidTanzanianPhone = (value: string | undefined): boolean => {
+  if (!value) return false;
+  const cleaned = value.replace(/[\s-]/g, "");
+  if (/^0[67]\d{8}$/.test(cleaned)) return true;
+  if (/^\+255[67]\d{8}$/.test(cleaned)) return true;
+  if (/^255[67]\d{8}$/.test(cleaned)) return true;
+  return false;
+};
+
+// ─── Edit Attendee Schema ─────────────────────────────────────────────────────
+
+const editAttendeeSchema = Yup.object({
+  firstName: Yup.string().min(2).required("First name is required"),
+  lastName: Yup.string().min(2).required("Last name is required"),
+  email: Yup.string()
+    .transform((v) => (v === "" ? undefined : v))
+    .email("Enter a valid email")
+    .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Enter a valid email")
+    .optional(),
+  phoneNumber: Yup.string()
+    .required("Phone number is required")
+    .test("valid-tz-phone", "Enter a valid phone number", isValidTanzanianPhone),
+  dateOfBirth: Yup.string().required("Date of birth is required"),
+  gender: Yup.string().required("Gender is required"),
+  country: Yup.string().required("Country is required"),
+  region: Yup.string().when("country", {
+    is: "Tanzania",
+    then: (s) => s.required("Region is required for Tanzania"),
+    otherwise: (s) => s.optional(),
+  }),
+  pace: Yup.string()
+    .optional()
+    .test("valid-pace", "Pace must be a whole number between 3 and 10", (v) => {
+      if (!v) return true;
+      const n = Number(v);
+      return Number.isInteger(n) && n >= 3 && n <= 10;
+    }),
+  emergencyContactPhone: Yup.string()
+    .required("Emergency phone is required")
+    .test("valid-tz-phone", "Enter a valid emergency phone number", isValidTanzanianPhone)
+    .test("different-from-phone", "Emergency phone must differ from main phone", function (v) {
+      if (!v || !this.parent.phoneNumber) return true;
+      return v !== this.parent.phoneNumber;
+    }),
+});
+
 // ─── Edit Attendee Form ───────────────────────────────────────────────────────
 
 function EditAttendeeForm({
@@ -119,21 +172,15 @@ function EditAttendeeForm({
   onCancel: () => void;
 }) {
   const { updateParticipant } = useEventRegistration();
-  const [form, setForm] = useState<UpdateParticipantI>(initial);
-  const [saving, setSaving] = useState(false);
-
-  const set = (field: keyof UpdateParticipantI, value: string) =>
-    setForm((p) => ({ ...p, [field]: value }));
 
-  const handleSave = async () => {
-    setSaving(true);
-    try {
-      await updateParticipant(registrationId, attendeeId, form);
+  const formik = useFormik<UpdateParticipantI>({
+    initialValues: initial,
+    validationSchema: editAttendeeSchema,
+    onSubmit: async (values) => {
+      await updateParticipant(registrationId, attendeeId, values);
       onSuccess();
-    } finally {
-      setSaving(false);
-    }
-  };
+    },
+  });
 
   return (
     <div className="space-y-4">
@@ -141,72 +188,101 @@ function EditAttendeeForm({
         <FormInput
           label="First Name"
           required
-          value={form.firstName ?? ""}
-          onChange={(e) => set("firstName", e.target.value)}
+          value={formik.values.firstName ?? ""}
+          onChange={formik.handleChange}
+          onBlur={formik.handleBlur}
+          name="firstName"
+          error={formik.touched.firstName && formik.errors.firstName ? formik.errors.firstName : undefined}
         />
         <FormInput
           label="Last Name"
           required
-          value={form.lastName ?? ""}
-          onChange={(e) => set("lastName", e.target.value)}
+          value={formik.values.lastName ?? ""}
+          onChange={formik.handleChange}
+          onBlur={formik.handleBlur}
+          name="lastName"
+          error={formik.touched.lastName && formik.errors.lastName ? formik.errors.lastName : undefined}
         />
         <FormInput
           label="Email"
           type="email"
-          value={form.email ?? ""}
-          onChange={(e) => set("email", e.target.value)}
+          value={formik.values.email ?? ""}
+          onChange={formik.handleChange}
+          onBlur={formik.handleBlur}
+          name="email"
+          error={formik.touched.email && formik.errors.email ? formik.errors.email : undefined}
         />
-        <FormInput
+        <PhoneNumberInput
           label="Phone Number"
           required
-          value={form.phoneNumber ?? ""}
-          onChange={(e) => set("phoneNumber", e.target.value)}
+          value={formik.values.phoneNumber ?? ""}
+          onChange={(value) => formik.setFieldValue("phoneNumber", value ?? "")}
+          onBlur={() => formik.setFieldTouched("phoneNumber", true)}
+          error={formik.touched.phoneNumber && formik.errors.phoneNumber ? formik.errors.phoneNumber : undefined}
+          defaultCountry="TZ"
         />
         <FormDatePicker
           label="Date of Birth"
           required
           granularity="day"
-          value={safeParseDate(form.dateOfBirth)}
-          onChange={(date) => set("dateOfBirth", date?.toString() ?? "")}
+          value={safeParseDate(formik.values.dateOfBirth)}
+          onChange={(date) => formik.setFieldValue("dateOfBirth", date?.toString() ?? "")}
+          maxValue={today(getLocalTimeZone()).subtract({ years: 2 })}
+          error={formik.touched.dateOfBirth && formik.errors.dateOfBirth ? formik.errors.dateOfBirth : undefined}
         />
         <FormSelect
           label="Gender"
           required
-          value={form.gender ?? ""}
-          onValueChange={(v) => set("gender", v)}
+          value={formik.values.gender ?? ""}
+          onValueChange={(v) => formik.setFieldValue("gender", v)}
           options={GENDER_OPTIONS}
           placeholder="Select gender"
+          error={formik.touched.gender && formik.errors.gender ? formik.errors.gender : undefined}
         />
-        <FormInput
+        <CountriesSelect
           label="Country"
-          value={form.country ?? ""}
-          onChange={(e) => set("country", e.target.value)}
-        />
-        <FormInput
-          label="Region"
-          value={form.region ?? ""}
-          onChange={(e) => set("region", e.target.value)}
+          required
+          value={formik.values.country ?? ""}
+          onChange={(v) => { formik.setFieldValue("country", v); formik.setFieldValue("region", ""); }}
+          error={formik.touched.country && formik.errors.country ? formik.errors.country : undefined}
         />
+        {formik.values.country === "Tanzania" && (
+          <FormSelect
+            label="Region"
+            required
+            value={formik.values.region ?? ""}
+            onValueChange={(v) => formik.setFieldValue("region", v)}
+            options={TANZANIA_REGION_OPTIONS}
+            placeholder="Select region"
+            error={formik.touched.region && formik.errors.region ? formik.errors.region : undefined}
+          />
+        )}
         <FormInput
           label="Pace (min/km)"
           type="number"
-          value={form.pace ?? ""}
-          onChange={(e) => set("pace", e.target.value)}
+          value={formik.values.pace ?? ""}
+          onChange={formik.handleChange}
+          onBlur={formik.handleBlur}
+          name="pace"
+          error={formik.touched.pace && formik.errors.pace ? formik.errors.pace : undefined}
         />
-        <FormInput
+        <PhoneNumberInput
           label="Emergency Phone"
           required
-          value={form.emergencyContactPhone ?? ""}
-          onChange={(e) => set("emergencyContactPhone", e.target.value)}
+          value={formik.values.emergencyContactPhone ?? ""}
+          onChange={(value) => formik.setFieldValue("emergencyContactPhone", value ?? "")}
+          onBlur={() => formik.setFieldTouched("emergencyContactPhone", true)}
+          error={formik.touched.emergencyContactPhone && formik.errors.emergencyContactPhone ? formik.errors.emergencyContactPhone : undefined}
+          defaultCountry="TZ"
         />
       </div>
 
       <div className="flex items-center gap-2 pt-2">
-        <Button size="sm" onClick={handleSave} disabled={saving} className="gap-1.5">
-          {saving ? <Loader2 className="w-3.5 h-3.5 animate-spin" /> : <Save className="w-3.5 h-3.5" />}
+        <Button size="sm" onClick={() => formik.handleSubmit()} disabled={formik.isSubmitting} className="gap-1.5">
+          {formik.isSubmitting ? <Loader2 className="w-3.5 h-3.5 animate-spin" /> : <Save className="w-3.5 h-3.5" />}
           Save Changes
         </Button>
-        <Button size="sm" variant="outline" onClick={onCancel} disabled={saving}>
+        <Button size="sm" variant="outline" onClick={onCancel} disabled={formik.isSubmitting}>
           Cancel
         </Button>
       </div>
@@ -214,6 +290,55 @@ function EditAttendeeForm({
   );
 }
 
+// ─── Add Member Schemas ───────────────────────────────────────────────────────
+
+const addMemberSchema = Yup.object({
+  firstName: Yup.string().min(2).required("First name is required"),
+  lastName: Yup.string().min(2).required("Last name is required"),
+  email: Yup.string()
+    .transform((v) => (v === "" ? undefined : v))
+    .email("Enter a valid email")
+    .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Enter a valid email")
+    .optional(),
+  phone: Yup.string()
+    .required("Phone number is required")
+    .test("valid-tz-phone", "Enter a valid phone number", isValidTanzanianPhone),
+  dateOfBirth: Yup.string().required("Date of birth is required"),
+  gender: Yup.string().required("Gender is required"),
+  country: Yup.string().required("Country is required"),
+  region: Yup.string().when("country", {
+    is: "Tanzania",
+    then: (s) => s.required("Region is required for Tanzania"),
+    otherwise: (s) => s.optional(),
+  }),
+  pace: Yup.string()
+    .optional()
+    .test("valid-pace", "Pace must be a whole number between 3 and 10", (v) => {
+      if (!v) return true;
+      const n = Number(v);
+      return Number.isInteger(n) && n >= 3 && n <= 10;
+    }),
+  emergencyPhone: Yup.string()
+    .required("Emergency phone is required")
+    .test("valid-tz-phone", "Enter a valid emergency phone number", isValidTanzanianPhone)
+    .test("different-from-phone", "Emergency phone must differ from main phone", function (v) {
+      if (!v || !this.parent.phone) return true;
+      return v !== this.parent.phone;
+    }),
+  selectedTicketId: Yup.string().required("Please select a ticket"),
+});
+
+const paymentSchema = Yup.object({
+  customerName: Yup.string().required("Customer name is required"),
+  customerPhone: Yup.string()
+    .required("Customer phone is required")
+    .test("valid-tz-phone", "Enter a valid phone number", isValidTanzanianPhone),
+  customerEmail: Yup.string()
+    .transform((v) => (v === "" ? undefined : v))
+    .email("Enter a valid email")
+    .optional(),
+});
+
 // ─── Add Member Form ──────────────────────────────────────────────────────────
 
 type AddMemberStep = "attendee" | "payment";
@@ -232,93 +357,75 @@ function AddMemberForm({
   const { amendmentAddAttendee, amendmentInitiatePayment } = useEventRegistration();
   const { findEventTickets, eventTickets } = useTicket();
   const [step, setStep] = useState<AddMemberStep>("attendee");
-  const [saving, setSaving] = useState(false);
-  const [selectedTicketId, setSelectedTicketId] = useState("");
-
-  const emptyForm = {
-    firstName: "",
-    lastName: "",
-    email: "",
-    phone: "",
-    dateOfBirth: "",
-    gender: "",
-    country: "",
-    region: "",
-    pace: "",
-    emergencyPhone: "",
-  };
-  const [form, setForm] = useState(emptyForm);
-
-  const emptyPayment = {
-    customerName: "",
-    customerPhone: "",
-    customerEmail: "",
-  };
-  const [paymentForm, setPaymentForm] = useState(emptyPayment);
 
   useEffect(() => {
     if (eventId) findEventTickets(eventId);
   }, [eventId]);
 
-  const set = (field: keyof typeof emptyForm, value: string) =>
-    setForm((p) => ({ ...p, [field]: value }));
-
-  const setP = (field: keyof typeof emptyPayment, value: string) =>
-    setPaymentForm((p) => ({ ...p, [field]: value }));
+  const tickets: TicketI[] = (eventTickets?.data || []).filter((t: TicketI) => t.active);
 
-  const handleAdd = async () => {
-    if (!form.firstName || !form.lastName || !form.phone || !form.dateOfBirth || !form.gender || !form.country || !form.emergencyPhone || !selectedTicketId) return;
-    setSaving(true);
-    try {
+  const attendeeFormik = useFormik({
+    initialValues: {
+      firstName: "",
+      lastName: "",
+      email: "",
+      phone: "",
+      dateOfBirth: "",
+      gender: "",
+      country: "Tanzania",
+      region: "",
+      pace: "",
+      emergencyPhone: "",
+      selectedTicketId: "",
+    },
+    validationSchema: addMemberSchema,
+    onSubmit: async (values) => {
       const payload: AddAttendeeI = {
         attendee: {
-          firstName: form.firstName,
-          lastName: form.lastName,
-          email: form.email || undefined,
-          phoneNumber: form.phone,
-          dateOfBirth: form.dateOfBirth,
-          gender: form.gender,
-          emergencyContactPhone: form.emergencyPhone,
-          country: form.country || undefined,
-          region: form.region || undefined,
-          pace: form.pace ? Number(form.pace) : undefined,
+          firstName: values.firstName,
+          lastName: values.lastName,
+          email: values.email || undefined,
+          phoneNumber: values.phone,
+          dateOfBirth: values.dateOfBirth,
+          gender: values.gender,
+          emergencyContactPhone: values.emergencyPhone,
+          country: values.country || undefined,
+          region: values.region || undefined,
+          pace: values.pace ? Number(values.pace) : undefined,
           isPrimaryRegistrant: false,
           attendeeRole: "MEMBER",
           eventRegistrationId: registrationId,
         },
-        selectedTickets: [{ ticketId: selectedTicketId, quantity: 1 }],
+        selectedTickets: [{ ticketId: values.selectedTicketId, quantity: 1 }],
       };
       await amendmentAddAttendee(registrationId, payload);
-      // Pre-fill payment form with member info
-      setPaymentForm({
-        customerName: `${form.firstName} ${form.lastName}`.trim(),
-        customerPhone: form.phone,
-        customerEmail: form.email,
+      paymentFormik.setValues({
+        customerName: `${values.firstName} ${values.lastName}`.trim(),
+        customerPhone: values.phone,
+        customerEmail: values.email,
       });
       setStep("payment");
-    } finally {
-      setSaving(false);
-    }
-  };
-
-  const handlePayment = async () => {
-    if (!paymentForm.customerName || !paymentForm.customerPhone) return;
-    setSaving(true);
-    try {
+    },
+  });
+
+  const paymentFormik = useFormik({
+    initialValues: {
+      customerName: "",
+      customerPhone: "",
+      customerEmail: "",
+    },
+    validationSchema: paymentSchema,
+    onSubmit: async (values) => {
       const paymentPayload: IntialPaymentI = {
-        customerName: paymentForm.customerName,
+        customerName: values.customerName,
         paymentMethod: "CONTROL_NUMBER",
-        customerPhone: paymentForm.customerPhone,
-        customerEmail: paymentForm.customerEmail,
+        customerPhone: values.customerPhone,
+        customerEmail: values.customerEmail,
       };
       await amendmentInitiatePayment(registrationId, paymentPayload);
       onSuccess();
-    } finally {
-      setSaving(false);
-    }
-  };
-
-  const tickets: TicketI[] = (eventTickets?.data || []).filter((t: TicketI) => t.active);
+    },
+  });
 
   // ── Step 1: Attendee details ──
   if (step === "attendee") {
@@ -335,86 +442,111 @@ function AddMemberForm({
           <FormInput
             label="First Name"
             required
-            value={form.firstName}
-            onChange={(e) => set("firstName", e.target.value)}
+            value={attendeeFormik.values.firstName}
+            onChange={attendeeFormik.handleChange}
+            onBlur={attendeeFormik.handleBlur}
+            name="firstName"
             placeholder="First name"
+            error={attendeeFormik.touched.firstName && attendeeFormik.errors.firstName ? attendeeFormik.errors.firstName : undefined}
           />
           <FormInput
             label="Last Name"
             required
-            value={form.lastName}
-            onChange={(e) => set("lastName", e.target.value)}
+            value={attendeeFormik.values.lastName}
+            onChange={attendeeFormik.handleChange}
+            onBlur={attendeeFormik.handleBlur}
+            name="lastName"
             placeholder="Last name"
+            error={attendeeFormik.touched.lastName && attendeeFormik.errors.lastName ? attendeeFormik.errors.lastName : undefined}
           />
-          <FormInput
+          <PhoneNumberInput
             label="Phone Number"
             required
-            value={form.phone}
-            onChange={(e) => set("phone", e.target.value)}
-            placeholder="+255..."
+            value={attendeeFormik.values.phone}
+            onChange={(value) => attendeeFormik.setFieldValue("phone", value ?? "")}
+            onBlur={() => attendeeFormik.setFieldTouched("phone", true)}
+            error={attendeeFormik.touched.phone && attendeeFormik.errors.phone ? attendeeFormik.errors.phone : undefined}
+            defaultCountry="TZ"
           />
           <FormInput
             label="Email"
             type="email"
-            value={form.email}
-            onChange={(e) => set("email", e.target.value)}
+            value={attendeeFormik.values.email}
+            onChange={attendeeFormik.handleChange}
+            onBlur={attendeeFormik.handleBlur}
+            name="email"
             placeholder="email@example.com"
+            error={attendeeFormik.touched.email && attendeeFormik.errors.email ? attendeeFormik.errors.email : undefined}
           />
           <FormDatePicker
             label="Date of Birth"
             required
             granularity="day"
-            value={safeParseDate(form.dateOfBirth)}
-            onChange={(date) => set("dateOfBirth", date?.toString() ?? "")}
+            value={safeParseDate(attendeeFormik.values.dateOfBirth)}
+            onChange={(date) => attendeeFormik.setFieldValue("dateOfBirth", date?.toString() ?? "")}
+            maxValue={today(getLocalTimeZone()).subtract({ years: 2 })}
+            error={attendeeFormik.touched.dateOfBirth && attendeeFormik.errors.dateOfBirth ? attendeeFormik.errors.dateOfBirth : undefined}
           />
           <FormSelect
             label="Gender"
             required
-            value={form.gender}
-            onValueChange={(v) => set("gender", v)}
+            value={attendeeFormik.values.gender}
+            onValueChange={(v) => attendeeFormik.setFieldValue("gender", v)}
             options={GENDER_OPTIONS}
             placeholder="Select gender"
+            error={attendeeFormik.touched.gender && attendeeFormik.errors.gender ? attendeeFormik.errors.gender : undefined}
           />
-          <FormInput
+          <CountriesSelect
             label="Country"
             required
-            value={form.country}
-            onChange={(e) => set("country", e.target.value)}
-            placeholder="Tanzania"
-          />
-          <FormInput
-            label="Region"
-            value={form.region}
-            onChange={(e) => set("region", e.target.value)}
-            placeholder="Region"
+            value={attendeeFormik.values.country}
+            onChange={(v) => { attendeeFormik.setFieldValue("country", v); attendeeFormik.setFieldValue("region", ""); }}
+            error={attendeeFormik.touched.country && attendeeFormik.errors.country ? attendeeFormik.errors.country : undefined}
           />
+          {attendeeFormik.values.country === "Tanzania" && (
+            <FormSelect
+              label="Region"
+              required
+              value={attendeeFormik.values.region}
+              onValueChange={(v) => attendeeFormik.setFieldValue("region", v)}
+              options={TANZANIA_REGION_OPTIONS}
+              placeholder="Select region"
+              error={attendeeFormik.touched.region && attendeeFormik.errors.region ? attendeeFormik.errors.region : undefined}
+            />
+          )}
           <FormInput
             label="Pace (min/km)"
             type="number"
-            value={form.pace}
-            onChange={(e) => set("pace", e.target.value)}
+            value={attendeeFormik.values.pace}
+            onChange={attendeeFormik.handleChange}
+            onBlur={attendeeFormik.handleBlur}
+            name="pace"
             placeholder="e.g. 5"
+            error={attendeeFormik.touched.pace && attendeeFormik.errors.pace ? attendeeFormik.errors.pace : undefined}
           />
-          <FormInput
+          <PhoneNumberInput
             label="Emergency Phone"
             required
-            value={form.emergencyPhone}
-            onChange={(e) => set("emergencyPhone", e.target.value)}
-            placeholder="+255..."
+            value={attendeeFormik.values.emergencyPhone}
+            onChange={(value) => attendeeFormik.setFieldValue("emergencyPhone", value ?? "")}
+            onBlur={() => attendeeFormik.setFieldTouched("emergencyPhone", true)}
+            error={attendeeFormik.touched.emergencyPhone && attendeeFormik.errors.emergencyPhone ? attendeeFormik.errors.emergencyPhone : undefined}
+            defaultCountry="TZ"
           />
 
           <div className="col-span-2">
             <FormSelect
               label="Select Ticket"
               required
-              value={selectedTicketId}
-              onValueChange={setSelectedTicketId}
+              value={attendeeFormik.values.selectedTicketId}
+              onValueChange={(v) => attendeeFormik.setFieldValue("selectedTicketId", v)}
               options={tickets.map((t: TicketI) => ({
                 label: `${t.name} — TZS ${t.price?.toLocaleString()}`,
                 value: String(t.id),
               }))}
-              placeholder={tickets.length === 0 ? "Loading tickets…" : "Choose a ticket"}
+              placeholder={tickets.length === 0 ? "Loading tickets\u2026" : "Choose a ticket"}
               disabled={tickets.length === 0}
+              error={attendeeFormik.touched.selectedTicketId && attendeeFormik.errors.selectedTicketId ? attendeeFormik.errors.selectedTicketId : undefined}
             />
           </div>
         </div>
@@ -422,14 +554,14 @@ function AddMemberForm({
         <div className="flex items-center gap-2 pt-1">
           <Button
             size="sm"
-            onClick={handleAdd}
-            disabled={saving || !form.firstName || !form.lastName || !form.phone || !form.dateOfBirth || !form.gender || !form.country || !form.emergencyPhone || !selectedTicketId}
+            onClick={() => attendeeFormik.handleSubmit()}
+            disabled={attendeeFormik.isSubmitting}
             className="gap-1.5"
           >
-            {saving ? <Loader2 className="w-3.5 h-3.5 animate-spin" /> : <UserPlus className="w-3.5 h-3.5" />}
+            {attendeeFormik.isSubmitting ? <Loader2 className="w-3.5 h-3.5 animate-spin" /> : <UserPlus className="w-3.5 h-3.5" />}
             Add Member
           </Button>
-          <Button size="sm" variant="outline" onClick={onCancel} disabled={saving}>
+          <Button size="sm" variant="outline" onClick={onCancel} disabled={attendeeFormik.isSubmitting}>
             Cancel
           </Button>
         </div>
@@ -442,7 +574,7 @@ function AddMemberForm({
     <div className="border border-dashed border-green-200 rounded-xl p-5 bg-green-50/30 space-y-4">
       <div className="flex items-center gap-2">
         <CheckCircle className="w-4 h-4 text-green-600 shrink-0" />
-        <h4 className="text-sm font-semibold text-gray-900">Member Added — Initiate Payment</h4>
+        <h4 className="text-sm font-semibold text-gray-900">Member Added \u2014 Initiate Payment</h4>
       </div>
       <p className="text-xs text-gray-500">
         Finalize the registration and generate a control number for payment.
@@ -453,38 +585,46 @@ function AddMemberForm({
           <FormInput
             label="Customer Name"
             required
-            value={paymentForm.customerName}
-            onChange={(e) => setP("customerName", e.target.value)}
+            value={paymentFormik.values.customerName}
+            onChange={paymentFormik.handleChange}
+            onBlur={paymentFormik.handleBlur}
+            name="customerName"
             placeholder="Full name"
+            error={paymentFormik.touched.customerName && paymentFormik.errors.customerName ? paymentFormik.errors.customerName : undefined}
           />
         </div>
-        <FormInput
+        <PhoneNumberInput
           label="Customer Phone"
           required
-          value={paymentForm.customerPhone}
-          onChange={(e) => setP("customerPhone", e.target.value)}
-          placeholder="+255..."
+          value={paymentFormik.values.customerPhone}
+          onChange={(value) => paymentFormik.setFieldValue("customerPhone", value ?? "")}
+          onBlur={() => paymentFormik.setFieldTouched("customerPhone", true)}
+          error={paymentFormik.touched.customerPhone && paymentFormik.errors.customerPhone ? paymentFormik.errors.customerPhone : undefined}
+          defaultCountry="TZ"
         />
         <FormInput
           label="Customer Email"
           type="email"
-          value={paymentForm.customerEmail}
-          onChange={(e) => setP("customerEmail", e.target.value)}
+          value={paymentFormik.values.customerEmail}
+          onChange={paymentFormik.handleChange}
+          onBlur={paymentFormik.handleBlur}
+          name="customerEmail"
           placeholder="email@example.com"
+          error={paymentFormik.touched.customerEmail && paymentFormik.errors.customerEmail ? paymentFormik.errors.customerEmail : undefined}
         />
       </div>
 
       <div className="flex items-center gap-2 pt-1">
         <Button
           size="sm"
-          onClick={handlePayment}
-          disabled={saving || !paymentForm.customerName || !paymentForm.customerPhone}
+          onClick={() => paymentFormik.handleSubmit()}
+          disabled={paymentFormik.isSubmitting}
           className="gap-1.5"
         >
-          {saving ? <Loader2 className="w-3.5 h-3.5 animate-spin" /> : <Save className="w-3.5 h-3.5" />}
+          {paymentFormik.isSubmitting ? <Loader2 className="w-3.5 h-3.5 animate-spin" /> : <Save className="w-3.5 h-3.5" />}
           Finalize & Initiate Payment
         </Button>
-        <Button size="sm" variant="outline" onClick={onSuccess} disabled={saving}>
+        <Button size="sm" variant="outline" onClick={onSuccess} disabled={paymentFormik.isSubmitting}>
           Skip Payment
         </Button>
       </div>
@@ -677,42 +817,45 @@ function UpgradeTicketForm({
 
 // ─── Individual Attendee Tab ──────────────────────────────────────────────────
 
-function IndividualAttendeeTab({
-  data,
+// ─── Individual Attendee Card ─────────────────────────────────────────────────
+
+function IndividualAttendeeCard({
+  attendeeDetail,
+  index,
+  registrationId,
+  eventId,
+  paymentStatus,
+  registrationStatus,
   onRefresh,
 }: {
-  data: RegistrationResponseI;
+  attendeeDetail: AttendeeDetailI;
+  index: number;
+  registrationId: string;
+  eventId: string;
+  paymentStatus: string;
+  registrationStatus: string;
   onRefresh?: () => void;
 }) {
   const { downloadDigitalTicket } = useEventRegistration();
   const [editing, setEditing] = useState(false);
-  const [expanded, setExpanded] = useState(true);
+  const [expanded, setExpanded] = useState(index === 0);
   const [isDownloading, setIsDownloading] = useState(false);
   const [upgradingTicketPurchaseId, setUpgradingTicketPurchaseId] = useState<string | null>(null);
 
-  const attendeeDetail = data.attendeeDetails?.[0];
-  const attendee = attendeeDetail?.attendee;
-  const isPaid = data.paymentStatus === "PAID";
+  const { attendee, ticketDetails, merchandiseDetails } = attendeeDetail;
+  const isPaid = paymentStatus === "PAID";
 
   const handleDownload = async () => {
     if (!attendee?.id) return;
     setIsDownloading(true);
     try {
-      await downloadDigitalTicket(String(data.registration?.id), String(attendee.id));
+      await downloadDigitalTicket(registrationId, String(attendee.id));
     } finally {
       setIsDownloading(false);
     }
   };
 
-  if (!attendee) {
-    return (
-      <div className="flex flex-col items-center justify-center py-16 text-center">
-        <AlertCircle className="w-10 h-10 text-gray-300 mb-3" />
-        <p className="text-sm font-medium text-gray-900">No attendee found</p>
-        <p className="text-xs text-gray-500 mt-1">This registration has no attendee details yet</p>
-      </div>
-    );
-  }
+  if (!attendee) return null;
 
   const initialForm: UpdateParticipantI = {
     firstName: attendee.firstName,
@@ -727,231 +870,380 @@ function IndividualAttendeeTab({
     emergencyContactPhone: attendee.emergencyContactPhone,
   };
 
+  const statusColor: Record<string, string> = {
+    CONFIRMED: "bg-green-100 text-green-700",
+    RESERVED: "bg-blue-100 text-blue-700",
+    EXPIRED: "bg-red-100 text-red-600",
+    CANCELLED: "bg-gray-100 text-gray-600",
+  };
+
   return (
-    <div className="space-y-5">
-      {/* Attendee Card */}
-      <div className="bg-white rounded-xl border border-gray-100 overflow-hidden">
-        {/* Card Header — click to expand/collapse */}
-        <div
-          role="button"
-          tabIndex={0}
-          onClick={() => { if (!editing) setExpanded((v) => !v); }}
-          onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") { if (!editing) setExpanded((v) => !v); } }}
-          className="w-full px-4 py-3 bg-gray-50 border-b border-gray-100 flex items-center justify-between cursor-pointer hover:bg-gray-100 transition-colors"
-        >
-          <div className="flex items-center gap-2.5">
-            <div className="w-8 h-8 rounded-full bg-primary-100 flex items-center justify-center shrink-0">
-              <User className="w-4 h-4 text-primary-700" />
-            </div>
-            <div>
-              <p className="text-sm font-semibold text-gray-900">
-                {[attendee.firstName, attendee.middleName, attendee.lastName].filter(Boolean).join(" ")}
-              </p>
-              <p className="text-xs text-gray-500">{attendee.attendeeRole}</p>
-            </div>
+    <div className="bg-white rounded-xl border border-gray-100 overflow-hidden">
+      {/* Card Header */}
+      <div
+        role="button"
+        tabIndex={0}
+        onClick={() => { if (!editing) setExpanded((v) => !v); }}
+        onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") { if (!editing) setExpanded((v) => !v); } }}
+        className="w-full px-4 py-3 bg-gray-50 border-b border-gray-100 flex items-center justify-between cursor-pointer hover:bg-gray-100 transition-colors"
+      >
+        <div className="flex items-center gap-2.5">
+          <div className="w-8 h-8 rounded-full bg-primary-100 flex items-center justify-center shrink-0">
+            <User className="w-4 h-4 text-primary-700" />
           </div>
-          <div className="flex items-center gap-2">
-            {isPaid && (
-              <Button
-                variant="outline"
-                size="sm"
-                className="h-8 gap-1.5 text-xs"
-                onClick={(e) => { e.stopPropagation(); handleDownload(); }}
-                disabled={isDownloading}
-              >
-                {isDownloading ? <Loader2 className="w-3.5 h-3.5 animate-spin" /> : <Download className="w-3.5 h-3.5" />}
-                Ticket
-              </Button>
-            )}
-            {!editing && (
-              <Button
-                variant="outline"
-                size="sm"
-                className="h-8 gap-1.5 text-xs"
-                onClick={(e) => { e.stopPropagation(); setExpanded(true); setEditing(true); }}
-              >
-                <Edit3 className="w-3.5 h-3.5" />
-                Edit
-              </Button>
-            )}
-            {expanded
-              ? <ChevronUp className="w-4 h-4 text-gray-400 shrink-0" />
-              : <ChevronDown className="w-4 h-4 text-gray-400 shrink-0" />
-            }
+          <div>
+            <p className="text-sm font-semibold text-gray-900">
+              {[attendee.firstName, attendee.middleName, attendee.lastName].filter(Boolean).join(" ")}
+            </p>
+            <p className="text-xs text-gray-500">{attendee.attendeeRole}</p>
           </div>
         </div>
+        <div className="flex items-center gap-2">
+          {isPaid && (
+            <Button
+              variant="outline"
+              size="sm"
+              className="h-8 gap-1.5 text-xs"
+              onClick={(e) => { e.stopPropagation(); handleDownload(); }}
+              disabled={isDownloading}
+            >
+              {isDownloading ? <Loader2 className="w-3.5 h-3.5 animate-spin" /> : <Download className="w-3.5 h-3.5" />}
+              Ticket
+            </Button>
+          )}
+          {!editing && (
+            <Button
+              variant="outline"
+              size="sm"
+              className="h-8 gap-1.5 text-xs"
+              onClick={(e) => { e.stopPropagation(); setExpanded(true); setEditing(true); }}
+            >
+              <Edit3 className="w-3.5 h-3.5" />
+              Edit
+            </Button>
+          )}
+          {expanded
+            ? <ChevronUp className="w-4 h-4 text-gray-400 shrink-0" />
+            : <ChevronDown className="w-4 h-4 text-gray-400 shrink-0" />
+          }
+        </div>
+      </div>
 
-        {/* Info or Edit Form — collapsed when expanded=false */}
-        {expanded && (
-          <div className="px-4 py-3">
-            {editing ? (
-              <EditAttendeeForm
-                registrationId={String(data.registration?.id)}
-                attendeeId={String(attendee.id)}
-                initial={initialForm}
-                onSuccess={() => { setEditing(false); onRefresh?.(); }}
-                onCancel={() => setEditing(false)}
-              />
-            ) : (
-              <div className="grid grid-cols-1 gap-0 divide-y divide-gray-50">
-                <div className="flex items-center gap-2 py-2">
-                  <Mail className="w-3.5 h-3.5 text-gray-400 shrink-0" />
-                  <span className="text-xs text-gray-500 w-28 shrink-0">Email</span>
-                  <span className="text-xs font-medium text-gray-900">{attendee.email || "—"}</span>
-                </div>
-                <div className="flex items-center gap-2 py-2">
-                  <Phone className="w-3.5 h-3.5 text-gray-400 shrink-0" />
-                  <span className="text-xs text-gray-500 w-28 shrink-0">Phone</span>
-                  <span className="text-xs font-medium text-gray-900">{attendee.phoneNumber || "—"}</span>
-                </div>
-                <div className="flex items-center gap-2 py-2">
-                  <CalendarDays className="w-3.5 h-3.5 text-gray-400 shrink-0" />
-                  <span className="text-xs text-gray-500 w-28 shrink-0">Date of Birth</span>
-                  <span className="text-xs font-medium text-gray-900">{formatDate(attendee.dateOfBirth)}</span>
-                </div>
+      {expanded && (
+        <div className="px-4 py-3">
+          {editing ? (
+            <EditAttendeeForm
+              registrationId={registrationId}
+              attendeeId={String(attendee.id)}
+              initial={initialForm}
+              onSuccess={() => { setEditing(false); onRefresh?.(); }}
+              onCancel={() => setEditing(false)}
+            />
+          ) : (
+            <div className="grid grid-cols-1 gap-0 divide-y divide-gray-50">
+              <div className="flex items-center gap-2 py-2">
+                <Mail className="w-3.5 h-3.5 text-gray-400 shrink-0" />
+                <span className="text-xs text-gray-500 w-28 shrink-0">Email</span>
+                <span className="text-xs font-medium text-gray-900">{attendee.email || "—"}</span>
+              </div>
+              <div className="flex items-center gap-2 py-2">
+                <Phone className="w-3.5 h-3.5 text-gray-400 shrink-0" />
+                <span className="text-xs text-gray-500 w-28 shrink-0">Phone</span>
+                <span className="text-xs font-medium text-gray-900">{attendee.phoneNumber || "—"}</span>
+              </div>
+              <div className="flex items-center gap-2 py-2">
+                <CalendarDays className="w-3.5 h-3.5 text-gray-400 shrink-0" />
+                <span className="text-xs text-gray-500 w-28 shrink-0">Date of Birth</span>
+                <span className="text-xs font-medium text-gray-900">{formatDate(attendee.dateOfBirth)}</span>
+              </div>
+              <div className="flex items-center gap-2 py-2">
+                <User className="w-3.5 h-3.5 text-gray-400 shrink-0" />
+                <span className="text-xs text-gray-500 w-28 shrink-0">Gender</span>
+                <span className="text-xs font-medium text-gray-900">{attendee.gender || "—"}</span>
+              </div>
+              {attendee.country && (
                 <div className="flex items-center gap-2 py-2">
-                  <User className="w-3.5 h-3.5 text-gray-400 shrink-0" />
-                  <span className="text-xs text-gray-500 w-28 shrink-0">Gender</span>
-                  <span className="text-xs font-medium text-gray-900">{attendee.gender || "—"}</span>
+                  <MapPin className="w-3.5 h-3.5 text-gray-400 shrink-0" />
+                  <span className="text-xs text-gray-500 w-28 shrink-0">Location</span>
+                  <span className="text-xs font-medium text-gray-900">
+                    {[attendee.region, attendee.country].filter(Boolean).join(", ")}
+                  </span>
                 </div>
-                {attendee.country && (
-                  <div className="flex items-center gap-2 py-2">
-                    <MapPin className="w-3.5 h-3.5 text-gray-400 shrink-0" />
-                    <span className="text-xs text-gray-500 w-28 shrink-0">Location</span>
-                    <span className="text-xs font-medium text-gray-900">
-                      {[attendee.region, attendee.country].filter(Boolean).join(", ")}
-                    </span>
-                  </div>
-                )}
-                {attendee.pace != null && (
-                  <div className="flex items-center gap-2 py-2">
-                    <span className="w-3.5 h-3.5 text-gray-400 shrink-0 text-center text-[10px] font-bold">⏱</span>
-                    <span className="text-xs text-gray-500 w-28 shrink-0">Pace</span>
-                    <span className="text-xs font-medium text-gray-900">{attendee.pace} min/km</span>
-                  </div>
-                )}
+              )}
+              {attendee.pace != null && (
                 <div className="flex items-center gap-2 py-2">
-                  <AlertCircle className="w-3.5 h-3.5 text-gray-400 shrink-0" />
-                  <span className="text-xs text-gray-500 w-28 shrink-0">Emergency</span>
-                  <span className="text-xs font-medium text-gray-900">{attendee.emergencyContactPhone || "—"}</span>
+                  <span className="w-3.5 h-3.5 text-gray-400 shrink-0 text-center text-[10px] font-bold">⏱</span>
+                  <span className="text-xs text-gray-500 w-28 shrink-0">Pace</span>
+                  <span className="text-xs font-medium text-gray-900">{attendee.pace} min/km</span>
                 </div>
+              )}
+              <div className="flex items-center gap-2 py-2">
+                <AlertCircle className="w-3.5 h-3.5 text-gray-400 shrink-0" />
+                <span className="text-xs text-gray-500 w-28 shrink-0">Emergency</span>
+                <span className="text-xs font-medium text-gray-900">{attendee.emergencyContactPhone || "—"}</span>
               </div>
-            )}
-          </div>
-        )}
-      </div>
+            </div>
+          )}
+        </div>
+      )}
 
-      {/* Tickets */}
-      {attendeeDetail.ticketDetails && attendeeDetail.ticketDetails.length > 0 && (
-        <div>
-          <h3 className="text-xs font-semibold text-gray-400 uppercase tracking-widest mb-3">Tickets</h3>
-          <div className="space-y-2">
-            {attendeeDetail.ticketDetails.map((td, i) => {
-              const ticketStatus = td.ticketPurchase?.status ?? "";
-              const ticketPurchaseId = String(td.ticketPurchase?.id ?? "");
-              const isUpgrading = upgradingTicketPurchaseId === ticketPurchaseId;
-              const statusColor: Record<string, string> = {
-                CONFIRMED: "bg-green-100 text-green-700",
-                RESERVED: "bg-blue-100 text-blue-700",
-                EXPIRED: "bg-red-100 text-red-600",
-                CANCELLED: "bg-gray-100 text-gray-600",
-              };
-              return (
-                <div key={i} className="space-y-2">
-                  <div className="bg-white border border-gray-100 rounded-xl p-3 flex items-center justify-between gap-3">
-                    <div className="flex items-center gap-2.5">
-                      <div className="w-8 h-8 rounded-lg bg-primary-50 flex items-center justify-center shrink-0">
-                        <FileText className="w-4 h-4 text-primary-600" />
-                      </div>
-                      <div>
-                        <p className="text-sm font-semibold text-gray-900">{td.ticket?.name ?? td.ticketName}</p>
-                        <p className="text-xs text-gray-400 font-mono">{td.ticketCode}</p>
-                      </div>
+      {/* Tickets (always visible below expand) */}
+      {expanded && ticketDetails && ticketDetails.length > 0 && (
+        <div className="px-4 pb-4 space-y-2">
+          <h3 className="text-xs font-semibold text-gray-400 uppercase tracking-widest pt-2">Tickets</h3>
+          {ticketDetails.map((td, i) => {
+            const ticketStatus = td.ticketPurchase?.status ?? "";
+            const ticketPurchaseId = String(td.ticketPurchase?.id ?? "");
+            const isUpgrading = upgradingTicketPurchaseId === ticketPurchaseId;
+            return (
+              <div key={i} className="space-y-2">
+                <div className="bg-gray-50 border border-gray-100 rounded-xl p-3 flex items-center justify-between gap-3">
+                  <div className="flex items-center gap-2.5">
+                    <div className="w-8 h-8 rounded-lg bg-primary-50 flex items-center justify-center shrink-0">
+                      <FileText className="w-4 h-4 text-primary-600" />
+                    </div>
+                    <div>
+                      <p className="text-sm font-semibold text-gray-900">{td.ticket?.name ?? td.ticketName}</p>
+                      <p className="text-xs text-gray-400 font-mono">{td.ticketCode}</p>
                     </div>
-                    <div className="flex items-center gap-2 shrink-0">
-                      <div className="text-right">
-                        <p className="text-sm font-bold text-gray-900">TZS {(td.pricePaid ?? 0).toLocaleString()}</p>
-                        {ticketStatus && (
-                          <span className={cn("inline-flex items-center rounded-full px-2 py-0.5 text-xs font-semibold mt-0.5", statusColor[ticketStatus] ?? "bg-gray-100 text-gray-600")}>
-                            {ticketStatus}
-                          </span>
-                        )}
-                      </div>
-                      {ticketPurchaseId && !isUpgrading && data.registration?.status === "REGISTERED" && data.paymentStatus === "PAID" && (
-                        <Button
-                          variant="outline"
-                          size="sm"
-                          className="h-8 gap-1.5 text-xs"
-                          onClick={() => setUpgradingTicketPurchaseId(ticketPurchaseId)}
-                        >
-                          <Edit3 className="w-3.5 h-3.5" />
-                          Upgrade
-                        </Button>
+                  </div>
+                  <div className="flex items-center gap-2 shrink-0">
+                    <div className="text-right">
+                      <p className="text-sm font-bold text-gray-900">TZS {(td.pricePaid ?? 0).toLocaleString()}</p>
+                      {ticketStatus && (
+                        <span className={cn("inline-flex items-center rounded-full px-2 py-0.5 text-xs font-semibold mt-0.5", statusColor[ticketStatus] ?? "bg-gray-100 text-gray-600")}>
+                          {ticketStatus}
+                        </span>
                       )}
                     </div>
+                    {ticketPurchaseId && !isUpgrading && registrationStatus === "REGISTERED" && isPaid && (
+                      <Button
+                        variant="outline"
+                        size="sm"
+                        className="h-8 gap-1.5 text-xs"
+                        onClick={() => setUpgradingTicketPurchaseId(ticketPurchaseId)}
+                      >
+                        <Edit3 className="w-3.5 h-3.5" />
+                        Upgrade
+                      </Button>
+                    )}
                   </div>
-                  {isUpgrading && attendee && (
-                    <UpgradeTicketForm
-                      registrationId={String(data.registration?.id)}
-                      eventId={String(data.registration?.eventId ?? "")}
-                      attendeeId={String(attendee.id)}
-                      currentTicketPurchaseId={ticketPurchaseId}
-                      currentTicketName={td.ticket?.name ?? td.ticketName ?? ""}
-                      onSuccess={() => { setUpgradingTicketPurchaseId(null); onRefresh?.(); }}
-                      onCancel={() => setUpgradingTicketPurchaseId(null)}
-                    />
-                  )}
                 </div>
-              );
-            })}
-          </div>
+                {isUpgrading && (
+                  <UpgradeTicketForm
+                    registrationId={registrationId}
+                    eventId={eventId}
+                    attendeeId={String(attendee.id)}
+                    currentTicketPurchaseId={ticketPurchaseId}
+                    currentTicketName={td.ticket?.name ?? td.ticketName ?? ""}
+                    onSuccess={() => { setUpgradingTicketPurchaseId(null); onRefresh?.(); }}
+                    onCancel={() => setUpgradingTicketPurchaseId(null)}
+                  />
+                )}
+              </div>
+            );
+          })}
         </div>
       )}
 
       {/* Merchandise */}
-      {attendeeDetail.merchandiseDetails && attendeeDetail.merchandiseDetails.length > 0 && (
-        <div>
-          <h3 className="text-xs font-semibold text-gray-400 uppercase tracking-widest mb-3 flex items-center gap-1.5">
+      {expanded && merchandiseDetails && merchandiseDetails.length > 0 && (
+        <div className="px-4 pb-4 space-y-2">
+          <h3 className="text-xs font-semibold text-gray-400 uppercase tracking-widest pt-2 flex items-center gap-1.5">
             <ShoppingBag className="w-3.5 h-3.5" />
             Merchandise
           </h3>
-          <div className="space-y-2">
-            {attendeeDetail.merchandiseDetails.map((m, i) => (
-              <div key={i} className="bg-white border border-gray-100 rounded-xl p-3 flex items-center justify-between gap-3">
-                <div>
-                  <p className="text-sm font-semibold text-gray-900">{m.productName}</p>
-                  {m.collectionCenterName && (
-                    <p className="text-xs text-gray-400">{m.collectionCenterName}</p>
-                  )}
-                </div>
-                <div className="text-right shrink-0">
-                  <p className="text-sm font-bold text-gray-900">TZS {(m.totalPrice ?? 0).toLocaleString()}</p>
-                  <p className="text-xs text-gray-400">× {m.quantity}</p>
-                </div>
+          {merchandiseDetails.map((m, i) => (
+            <div key={i} className="bg-gray-50 border border-gray-100 rounded-xl p-3 flex items-center justify-between gap-3">
+              <div>
+                <p className="text-sm font-semibold text-gray-900">{m.productName}</p>
+                {m.collectionCenterName && <p className="text-xs text-gray-400">{m.collectionCenterName}</p>}
               </div>
-            ))}
-          </div>
+              <div className="text-right shrink-0">
+                <p className="text-sm font-bold text-gray-900">TZS {(m.totalPrice ?? 0).toLocaleString()}</p>
+                <p className="text-xs text-gray-400">× {m.quantity}</p>
+              </div>
+            </div>
+          ))}
         </div>
       )}
     </div>
   );
 }
 
+// ─── Individual Attendee Tab ──────────────────────────────────────────────────
+
+function IndividualAttendeeTab({
+  data,
+  onRefresh,
+}: {
+  data: RegistrationResponseI;
+  onRefresh?: () => void;
+}) {
+  const attendeeDetails = data.attendeeDetails ?? [];
+
+  if (attendeeDetails.length === 0) {
+    return (
+      <div className="flex flex-col items-center justify-center py-16 text-center">
+        <AlertCircle className="w-10 h-10 text-gray-300 mb-3" />
+        <p className="text-sm font-medium text-gray-900">No attendees found</p>
+        <p className="text-xs text-gray-500 mt-1">This registration has no attendee details yet</p>
+      </div>
+    );
+  }
+
+  return (
+    <div className="space-y-4">
+      {attendeeDetails.map((ad, i) => (
+        <IndividualAttendeeCard
+          key={ad.attendee?.id ?? i}
+          attendeeDetail={ad}
+          index={i}
+          registrationId={String(data.registration?.id)}
+          eventId={String(data.registration?.eventId ?? "")}
+          paymentStatus={data.paymentStatus ?? ""}
+          registrationStatus={data.registration?.status ?? ""}
+          onRefresh={onRefresh}
+        />
+      ))}
+    </div>
+  );
+}
+
+// ─── Edit Group Member Schema ─────────────────────────────────────────────────
+
+const editGroupMemberSchema = Yup.object({
+  firstName: Yup.string().min(2).required("First name is required"),
+  lastName: Yup.string().min(2).required("Last name is required"),
+  email: Yup.string()
+    .transform((v) => (v === "" ? undefined : v))
+    .email("Enter a valid email")
+    .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Enter a valid email")
+    .optional(),
+  phone: Yup.string()
+    .required("Phone number is required")
+    .test("valid-tz-phone", "Enter a valid phone number", isValidTanzanianPhone),
+  dateOfBirth: Yup.string().required("Date of birth is required"),
+  gender: Yup.string().required("Gender is required"),
+  country: Yup.string().required("Country is required"),
+  region: Yup.string().when("country", {
+    is: "Tanzania",
+    then: (s) => s.required("Region is required for Tanzania"),
+    otherwise: (s) => s.optional(),
+  }),
+  pace: Yup.string()
+    .optional()
+    .test("valid-pace", "Pace must be a whole number between 3 and 10", (v) => {
+      if (!v) return true;
+      const n = Number(v);
+      return Number.isInteger(n) && n >= 3 && n <= 10;
+    }),
+  emergencyContactPhone: Yup.string()
+    .required("Emergency phone is required")
+    .test("valid-tz-phone", "Enter a valid emergency phone number", isValidTanzanianPhone)
+    .test("different-from-phone", "Emergency phone must differ from main phone", function (v) {
+      if (!v || !this.parent.phone) return true;
+      return v !== this.parent.phone;
+    }),
+});
+
+// ─── Edit Group Member Form ───────────────────────────────────────────────────
+
+function EditGroupMemberForm({
+  registrationId,
+  memberId,
+  initial,
+  onSuccess,
+  onCancel,
+}: {
+  registrationId: string;
+  memberId: string;
+  initial: {
+    firstName: string; lastName: string; email: string; phone: string;
+    dateOfBirth: string; gender: string; country: string; region: string;
+    pace: string; emergencyContactPhone: string;
+  };
+  onSuccess: () => void;
+  onCancel: () => void;
+}) {
+  const { updateParticipant } = useEventRegistration();
+
+  const formik = useFormik({
+    initialValues: initial,
+    validationSchema: editGroupMemberSchema,
+    onSubmit: async (values) => {
+      await updateParticipant(registrationId, memberId, {
+        firstName: values.firstName,
+        lastName: values.lastName,
+        email: values.email || undefined,
+        phoneNumber: values.phone,
+        dateOfBirth: values.dateOfBirth,
+        gender: values.gender,
+        country: values.country || undefined,
+        region: values.region || undefined,
+        pace: values.pace || undefined,
+        emergencyContactPhone: values.emergencyContactPhone,
+      });
+      onSuccess();
+    },
+  });
+
+  return (
+    <div className="space-y-4">
+      <div className="grid grid-cols-2 gap-3">
+        <FormInput label="First Name" required name="firstName" value={formik.values.firstName} onChange={formik.handleChange} onBlur={formik.handleBlur} error={formik.touched.firstName && formik.errors.firstName ? formik.errors.firstName : undefined} />
+        <FormInput label="Last Name" required name="lastName" value={formik.values.lastName} onChange={formik.handleChange} onBlur={formik.handleBlur} error={formik.touched.lastName && formik.errors.lastName ? formik.errors.lastName : undefined} />
+        <FormInput label="Email" type="email" name="email" value={formik.values.email} onChange={formik.handleChange} onBlur={formik.handleBlur} error={formik.touched.email && formik.errors.email ? formik.errors.email : undefined} />
+        <PhoneNumberInput label="Phone Number" required value={formik.values.phone} onChange={(v) => formik.setFieldValue("phone", v ?? "")} onBlur={() => formik.setFieldTouched("phone", true)} error={formik.touched.phone && formik.errors.phone ? formik.errors.phone : undefined} defaultCountry="TZ" />
+        <FormDatePicker label="Date of Birth" required granularity="day" value={safeParseDate(formik.values.dateOfBirth)} onChange={(d) => formik.setFieldValue("dateOfBirth", d?.toString() ?? "")} maxValue={today(getLocalTimeZone()).subtract({ years: 2 })} error={formik.touched.dateOfBirth && formik.errors.dateOfBirth ? formik.errors.dateOfBirth : undefined} />
+        <FormSelect label="Gender" required value={formik.values.gender} onValueChange={(v) => formik.setFieldValue("gender", v)} options={GENDER_OPTIONS} placeholder="Select gender" error={formik.touched.gender && formik.errors.gender ? formik.errors.gender : undefined} />
+        <CountriesSelect label="Country" required value={formik.values.country} onChange={(v) => { formik.setFieldValue("country", v); formik.setFieldValue("region", ""); }} error={formik.touched.country && formik.errors.country ? formik.errors.country : undefined} />
+        {formik.values.country === "Tanzania" && (
+          <FormSelect label="Region" required value={formik.values.region} onValueChange={(v) => formik.setFieldValue("region", v)} options={TANZANIA_REGION_OPTIONS} placeholder="Select region" error={formik.touched.region && formik.errors.region ? formik.errors.region : undefined} />
+        )}
+        <FormInput label="Pace (min/km)" type="number" name="pace" value={formik.values.pace} onChange={formik.handleChange} onBlur={formik.handleBlur} placeholder="e.g. 5" error={formik.touched.pace && formik.errors.pace ? formik.errors.pace : undefined} />
+        <PhoneNumberInput label="Emergency Phone" required value={formik.values.emergencyContactPhone} onChange={(v) => formik.setFieldValue("emergencyContactPhone", v ?? "")} onBlur={() => formik.setFieldTouched("emergencyContactPhone", true)} error={formik.touched.emergencyContactPhone && formik.errors.emergencyContactPhone ? formik.errors.emergencyContactPhone : undefined} defaultCountry="TZ" />
+      </div>
+      <div className="flex items-center gap-2 pt-2">
+        <Button size="sm" onClick={() => formik.handleSubmit()} disabled={formik.isSubmitting} className="gap-1.5">
+          {formik.isSubmitting ? <Loader2 className="w-3.5 h-3.5 animate-spin" /> : <Save className="w-3.5 h-3.5" />}
+          Save Changes
+        </Button>
+        <Button size="sm" variant="outline" onClick={onCancel} disabled={formik.isSubmitting}>Cancel</Button>
+      </div>
+    </div>
+  );
+}
+
 // ─── Member Card ──────────────────────────────────────────────────────────────
 
-function MemberCard({ md, index }: { md: AttendeeDetailI; index: number }) {
+function MemberCard({ md, index, registrationId, onRefresh }: { md: AttendeeDetailI; index: number; registrationId: string; onRefresh?: () => void }) {
   const [expanded, setExpanded] = useState(false);
+  const [editing, setEditing] = useState(false);
   const { attendee, ticketDetails, merchandiseDetails } = md;
   const primaryTicket = ticketDetails?.[0];
 
+  const initialEditValues = {
+    firstName: attendee.firstName ?? "",
+    lastName: attendee.lastName ?? "",
+    email: attendee.email ?? "",
+    phone: attendee.phoneNumber ?? "",
+    dateOfBirth: attendee.dateOfBirth ?? "",
+    gender: attendee.gender ?? "",
+    country: attendee.country ?? "",
+    region: attendee.region ?? "",
+    pace: attendee.pace != null ? String(attendee.pace) : "",
+    emergencyContactPhone: attendee.emergencyContactPhone ?? "",
+  };
+
   return (
     <div className="bg-white border border-gray-100 rounded-xl overflow-hidden">
       {/* Header — click to toggle */}
       <div
         role="button"
         tabIndex={0}
-        onClick={() => setExpanded((v) => !v)}
-        onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") setExpanded((v) => !v); }}
+        onClick={() => { if (!editing) setExpanded((v) => !v); }}
+        onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") { if (!editing) setExpanded((v) => !v); } }}
         className="px-4 py-3 flex items-center justify-between bg-gray-50 border-b border-gray-100 cursor-pointer hover:bg-gray-100 transition-colors"
       >
         <div className="flex items-center gap-2.5">
@@ -975,6 +1267,17 @@ function MemberCard({ md, index }: { md: AttendeeDetailI; index: number }) {
               <p className="text-xs text-gray-500">TZS {(primaryTicket.pricePaid ?? 0).toLocaleString()}</p>
             </div>
           )}
+          {!editing && (
+            <Button
+              variant="outline"
+              size="sm"
+              className="h-7 gap-1.5 text-xs"
+              onClick={(e) => { e.stopPropagation(); setExpanded(true); setEditing(true); }}
+            >
+              <Edit3 className="w-3.5 h-3.5" />
+              Edit
+            </Button>
+          )}
           {expanded
             ? <ChevronUp className="w-4 h-4 text-gray-400" />
             : <ChevronDown className="w-4 h-4 text-gray-400" />
@@ -985,53 +1288,63 @@ function MemberCard({ md, index }: { md: AttendeeDetailI; index: number }) {
       {/* Expanded Details */}
       {expanded && (
         <div className="px-4 py-3 space-y-3">
-          {/* Personal info rows */}
-          <div className="divide-y divide-gray-50">
-            {attendee.email && (
-              <div className="flex items-center gap-2 py-2">
-                <Mail className="w-3.5 h-3.5 text-gray-400 shrink-0" />
-                <span className="text-xs text-gray-500 w-28 shrink-0">Email</span>
-                <span className="text-xs font-medium text-gray-900">{attendee.email}</span>
-              </div>
-            )}
-            {attendee.phoneNumber && (
-              <div className="flex items-center gap-2 py-2">
-                <Phone className="w-3.5 h-3.5 text-gray-400 shrink-0" />
-                <span className="text-xs text-gray-500 w-28 shrink-0">Phone</span>
-                <span className="text-xs font-medium text-gray-900">{attendee.phoneNumber}</span>
-              </div>
-            )}
-            {attendee.dateOfBirth && (
-              <div className="flex items-center gap-2 py-2">
-                <CalendarDays className="w-3.5 h-3.5 text-gray-400 shrink-0" />
-                <span className="text-xs text-gray-500 w-28 shrink-0">Date of Birth</span>
-                <span className="text-xs font-medium text-gray-900">{formatDate(attendee.dateOfBirth)}</span>
-              </div>
-            )}
-            {attendee.gender && (
-              <div className="flex items-center gap-2 py-2">
-                <User className="w-3.5 h-3.5 text-gray-400 shrink-0" />
-                <span className="text-xs text-gray-500 w-28 shrink-0">Gender</span>
-                <span className="text-xs font-medium text-gray-900">{attendee.gender}</span>
-              </div>
-            )}
-            {attendee.country && (
-              <div className="flex items-center gap-2 py-2">
-                <MapPin className="w-3.5 h-3.5 text-gray-400 shrink-0" />
-                <span className="text-xs text-gray-500 w-28 shrink-0">Location</span>
-                <span className="text-xs font-medium text-gray-900">
-                  {[attendee.region, attendee.country].filter(Boolean).join(", ")}
-                </span>
-              </div>
-            )}
-            {attendee.emergencyContactPhone && (
-              <div className="flex items-center gap-2 py-2">
-                <AlertCircle className="w-3.5 h-3.5 text-gray-400 shrink-0" />
-                <span className="text-xs text-gray-500 w-28 shrink-0">Emergency</span>
-                <span className="text-xs font-medium text-gray-900">{attendee.emergencyContactPhone}</span>
-              </div>
-            )}
-          </div>
+          {/* Personal info rows or edit form */}
+          {editing ? (
+            <EditGroupMemberForm
+              registrationId={registrationId}
+              memberId={String(attendee.id)}
+              initial={initialEditValues}
+              onSuccess={() => { setEditing(false); onRefresh?.(); }}
+              onCancel={() => setEditing(false)}
+            />
+          ) : (
+            <div className="divide-y divide-gray-50">
+              {attendee.email && (
+                <div className="flex items-center gap-2 py-2">
+                  <Mail className="w-3.5 h-3.5 text-gray-400 shrink-0" />
+                  <span className="text-xs text-gray-500 w-28 shrink-0">Email</span>
+                  <span className="text-xs font-medium text-gray-900">{attendee.email}</span>
+                </div>
+              )}
+              {attendee.phoneNumber && (
+                <div className="flex items-center gap-2 py-2">
+                  <Phone className="w-3.5 h-3.5 text-gray-400 shrink-0" />
+                  <span className="text-xs text-gray-500 w-28 shrink-0">Phone</span>
+                  <span className="text-xs font-medium text-gray-900">{attendee.phoneNumber}</span>
+                </div>
+              )}
+              {attendee.dateOfBirth && (
+                <div className="flex items-center gap-2 py-2">
+                  <CalendarDays className="w-3.5 h-3.5 text-gray-400 shrink-0" />
+                  <span className="text-xs text-gray-500 w-28 shrink-0">Date of Birth</span>
+                  <span className="text-xs font-medium text-gray-900">{formatDate(attendee.dateOfBirth)}</span>
+                </div>
+              )}
+              {attendee.gender && (
+                <div className="flex items-center gap-2 py-2">
+                  <User className="w-3.5 h-3.5 text-gray-400 shrink-0" />
+                  <span className="text-xs text-gray-500 w-28 shrink-0">Gender</span>
+                  <span className="text-xs font-medium text-gray-900">{attendee.gender}</span>
+                </div>
+              )}
+              {attendee.country && (
+                <div className="flex items-center gap-2 py-2">
+                  <MapPin className="w-3.5 h-3.5 text-gray-400 shrink-0" />
+                  <span className="text-xs text-gray-500 w-28 shrink-0">Location</span>
+                  <span className="text-xs font-medium text-gray-900">
+                    {[attendee.region, attendee.country].filter(Boolean).join(", ")}
+                  </span>
+                </div>
+              )}
+              {attendee.emergencyContactPhone && (
+                <div className="flex items-center gap-2 py-2">
+                  <AlertCircle className="w-3.5 h-3.5 text-gray-400 shrink-0" />
+                  <span className="text-xs text-gray-500 w-28 shrink-0">Emergency</span>
+                  <span className="text-xs font-medium text-gray-900">{attendee.emergencyContactPhone}</span>
+                </div>
+              )}
+            </div>
+          )}
 
           {/* Tickets */}
           {ticketDetails && ticketDetails.length > 0 && (
@@ -1086,6 +1399,7 @@ function GroupMembersTab({
   const eventId = String(reg?.eventId ?? "");
   const registrationId = String(reg?.id ?? "");
   const atCapacity = (reg?.currentAttendeeCount ?? 0) >= (reg?.maxAttendees ?? 0);
+  const canAmend = reg?.status === "REGISTERED" || reg?.status === "AWAITING_AMENDMENT";
 
   return (
     <div className="space-y-4">
@@ -1099,7 +1413,7 @@ function GroupMembersTab({
             {reg?.currentAttendeeCount ?? 0} of {reg?.maxAttendees ?? 0} spots used
           </p>
         </div>
-        {!addingMember && !atCapacity && (
+        {!addingMember && !atCapacity && canAmend && (
           <Button
             size="sm"
             onClick={() => setAddingMember(true)}
@@ -1114,6 +1428,11 @@ function GroupMembersTab({
             Group Full
           </span>
         )}
+        {!canAmend && !atCapacity && (
+          <span className="text-xs text-gray-500 bg-gray-100 border border-gray-200 rounded-full px-2.5 py-1 font-medium">
+            Cannot amend ({reg?.status})
+          </span>
+        )}
       </div>
 
       {/* Add Member Form */}
@@ -1132,7 +1451,7 @@ function GroupMembersTab({
           <Users className="w-10 h-10 text-gray-300 mb-3" />
           <p className="text-sm font-medium text-gray-900">No members yet</p>
           <p className="text-xs text-gray-500 mt-1">Add the first member to this group registration</p>
-          {!addingMember && (
+          {!addingMember && canAmend && (
             <Button
               size="sm"
               className="mt-4 gap-1.5"
@@ -1146,7 +1465,7 @@ function GroupMembersTab({
       ) : (
         <div className="space-y-3">
           {members.map((md, index) => (
-            <MemberCard key={md.attendee.id ?? index} md={md} index={index} />
+            <MemberCard key={md.attendee.id ?? index} md={md} index={index} registrationId={registrationId} onRefresh={onRefresh} />
           ))}
         </div>
       )}
@@ -1157,66 +1476,64 @@ function GroupMembersTab({
 // ─── Main Side Sheet ──────────────────────────────────────────────────────────
 
 export function RegistrationSideSheet({
-  registrationData,
+  registrationId,
   open,
   onClose,
   onRefresh,
 }: RegistrationSideSheetProps) {
+  const { eventRegistrationOrderDetails, orderDetails } = useEventRegistration();
   const [activeTab, setActiveTab] = useState<SheetTab>("details");
+  const [isFetching, setIsFetching] = useState(false);
 
+  const registrationData: RegistrationResponseI | null = (orderDetails?.data as RegistrationResponseI) ?? null;
   const isGroup = registrationData?.registration?.registrationType === "GROUP";
   const reg = registrationData?.registration;
 
-  // Reset to details tab when a new registration is opened
+  // Fetch fresh data from API whenever the sheet opens or the registration changes
   useEffect(() => {
-    if (open) setActiveTab("details");
-  }, [registrationData?.registration?.id, open]);
+    if (open && registrationId) {
+      setActiveTab("details");
+      setIsFetching(true);
+      eventRegistrationOrderDetails(registrationId).finally(() => setIsFetching(false));
+    }
+  }, [registrationId, open]);
 
   return (
-    <Sheet open={open} onOpenChange={(o) => !o && onClose()}>
+    <>
+    {open && (
+      <div className="fixed inset-0 bg-black/60 backdrop-blur-md z-[99999998]" />
+    )}
+    <Sheet open={open} onOpenChange={(o) => !o && onClose()} modal={false}>
       <SheetContent
         side="right"
         className="sm:max-w-[620px] w-full p-0 flex flex-col gap-0"
+        onInteractOutside={(e) => e.preventDefault()}
+        onFocusOutside={(e) => e.preventDefault()}
       >
         {/* ── Header ── */}
         <SheetHeader className="px-6 pt-5 pb-4 border-b border-gray-100 shrink-0">
-          {registrationData ? (
-            <div className="pr-6 space-y-2">
-              <SheetTitle className="text-base font-bold text-gray-900 font-mono">
-                {reg?.registrationReference}
-              </SheetTitle>
+          <div className="pr-6 space-y-2">
+            <SheetTitle className="text-base font-bold text-gray-900 font-mono">
+              {isFetching || !registrationData ? (
+                <Skeleton className="h-5 w-48" />
+              ) : (
+                reg?.registrationReference
+              )}
+            </SheetTitle>
+            {isFetching || !registrationData ? (
+              <>
+                <Skeleton className="h-4 w-64" />
+                <div className="flex gap-2">
+                  <Skeleton className="h-5 w-20 rounded-full" />
+                  <Skeleton className="h-5 w-20 rounded-full" />
+                </div>
+              </>
+            ) : (
               <p className="text-sm text-gray-500 leading-snug line-clamp-1">
                 {reg?.displayName}
               </p>
-              {/* <div className="flex items-center gap-2 flex-wrap">
-                <span className={cn(
-                  "inline-flex items-center gap-1 rounded-full px-2.5 py-0.5 text-xs font-semibold",
-                  reg?.registrationType === "GROUP"
-                    ? "bg-primary-50 text-primary-700"
-                    : "bg-secondary-50 text-secondary-700"
-                )}>
-                  {reg?.registrationType === "GROUP" ? <Users className="w-3 h-3" /> : <User className="w-3 h-3" />}
-                  {reg?.registrationType}
-                </span>
-                <span className={cn("inline-flex items-center gap-1 rounded-full px-2.5 py-0.5 text-xs font-semibold", regStatus.color)}>
-                  {regStatus.icon}
-                  {regStatus.label}
-                </span>
-                <span className={cn("inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-semibold", pmtStatus.color)}>
-                  {pmtStatus.label}
-                </span>
-              </div> */}
-            </div>
-          ) : (
-            <div className="pr-6 space-y-2">
-              <Skeleton className="h-5 w-48" />
-              <Skeleton className="h-4 w-64" />
-              <div className="flex gap-2">
-                <Skeleton className="h-5 w-20 rounded-full" />
-                <Skeleton className="h-5 w-20 rounded-full" />
-              </div>
-            </div>
-          )}
+            )}
+          </div>
         </SheetHeader>
 
         {/* ── Tabs ── */}
@@ -1237,29 +1554,30 @@ export function RegistrationSideSheet({
           </div>
 
           <div className="flex-1 overflow-y-auto">
-            {registrationData ? (
+            {isFetching || !registrationData ? (
+              <div className="p-6 space-y-4">
+                {[1, 2, 3, 4, 5].map((i) => (
+                  <Skeleton key={i} className="h-10 w-full rounded-lg" />
+                ))}
+              </div>
+            ) : (
               <>
                 <TabsContent value="details" className="p-6 m-0">
                   <RegistrationDetailsTab data={registrationData} />
                 </TabsContent>
                 <TabsContent value="people" className="p-6 m-0">
                   {isGroup ? (
-                    <GroupMembersTab data={registrationData} onRefresh={onRefresh} />
+                    <GroupMembersTab data={registrationData} onRefresh={() => { if (registrationId) eventRegistrationOrderDetails(registrationId); onRefresh?.(); }} />
                   ) : (
-                    <IndividualAttendeeTab data={registrationData} onRefresh={onRefresh} />
+                    <IndividualAttendeeTab data={registrationData} onRefresh={() => { if (registrationId) eventRegistrationOrderDetails(registrationId); onRefresh?.(); }} />
                   )}
                 </TabsContent>
               </>
-            ) : (
-              <div className="p-6 space-y-4">
-                {[1, 2, 3, 4, 5].map((i) => (
-                  <Skeleton key={i} className="h-10 w-full rounded-lg" />
-                ))}
-              </div>
             )}
           </div>
         </Tabs>
       </SheetContent>
     </Sheet>
+    </>
   );
 }
diff --git a/src/app/(PageSections)/Dashboard/SponsorPageSections/components/SponsorForm.tsx b/src/app/(PageSections)/Dashboard/SponsorPageSections/components/SponsorForm.tsx
index 282f9c9..8bca975 100644
--- a/src/app/(PageSections)/Dashboard/SponsorPageSections/components/SponsorForm.tsx
+++ b/src/app/(PageSections)/Dashboard/SponsorPageSections/components/SponsorForm.tsx
@@ -162,7 +162,8 @@ const createSponsorValidationSchema = (sponsorTiersData?: SponsorTierI[]) =>
 
     contactEmail: Yup.string()
       .trim()
-      .email("Must be a valid email address")
+      .email("Please enter a valid email address address")
+      .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address")
       .required("Contact email is required"),
 
     contactPhone: Yup.string().trim().required("Contact phone is required"),
@@ -189,13 +190,12 @@ const Stepper: React.FC<StepperProps> = ({ steps, currentStep }) => {
             <div className="flex flex-col items-center flex-1">
               <div className="flex items-center justify-center w-full mb-2">
                 <div
-                  className={`w-10 h-10 sm:w-12 sm:h-12 rounded-full flex items-center justify-center font-semibold transition-all duration-300 ${
-                    index < currentStep
+                  className={`w-10 h-10 sm:w-12 sm:h-12 rounded-full flex items-center justify-center font-semibold transition-all duration-300 ${index < currentStep
                       ? "bg-primary-950 text-white"
                       : index === currentStep
-                      ? "bg-primary-950 text-white ring-3 ring-primary-500/20"
-                      : "bg-gray-200 text-gray-500"
-                  }`}
+                        ? "bg-primary-950 text-white ring-3 ring-primary-500/20"
+                        : "bg-gray-200 text-gray-500"
+                    }`}
                 >
                   {index < currentStep ? (
                     <Check className="w-5 h-5" />
@@ -205,9 +205,8 @@ const Stepper: React.FC<StepperProps> = ({ steps, currentStep }) => {
                 </div>
               </div>
               <p
-                className={`text-xs sm:text-sm font-medium text-center transition-all duration-300 px-1 ${
-                  index <= currentStep ? "text-primary-950" : "text-gray-400"
-                }`}
+                className={`text-xs sm:text-sm font-medium text-center transition-all duration-300 px-1 ${index <= currentStep ? "text-primary-950" : "text-gray-400"
+                  }`}
               >
                 {step}
               </p>
@@ -215,9 +214,8 @@ const Stepper: React.FC<StepperProps> = ({ steps, currentStep }) => {
             {index < steps.length - 1 && (
               <div className="flex-1 h-[2px] -mt-5">
                 <div
-                  className={`h-full rounded transition-all duration-300 ${
-                    index < currentStep ? "bg-primary-950" : "bg-gray-200"
-                  }`}
+                  className={`h-full rounded transition-all duration-300 ${index < currentStep ? "bg-primary-950" : "bg-gray-200"
+                    }`}
                 />
               </div>
             )}
@@ -1042,18 +1040,18 @@ export function SponsorForm({
                     {uploadingFields.logo
                       ? "Uploading Logo..."
                       : isSubmitting
-                      ? mode === "create"
-                        ? "Creating..."
-                        : "Updating..."
-                      : mode === "create"
-                      ? "Create Sponsor"
-                      : "Update Sponsor"}
+                        ? mode === "create"
+                          ? "Creating..."
+                          : "Updating..."
+                        : mode === "create"
+                          ? "Create Sponsor"
+                          : "Update Sponsor"}
                   </Button>
                 )}
               </div>
             </div>
 
-            
+
           </Form>
         )}
       </Formik>
diff --git a/src/app/(PageSections)/Dashboard/TicketsPageSection/TicketsPagesection.tsx b/src/app/(PageSections)/Dashboard/TicketsPageSection/TicketsPagesection.tsx
index 9815a67..c282143 100644
--- a/src/app/(PageSections)/Dashboard/TicketsPageSection/TicketsPagesection.tsx
+++ b/src/app/(PageSections)/Dashboard/TicketsPageSection/TicketsPagesection.tsx
@@ -138,6 +138,7 @@ interface TicketFilters extends Record<string, unknown> {
   "ticketCode.contains"?: string;
   "purchaseReference.contains"?: string;
   "status.equals"?: string;
+  sort?: string;
 }
 
 function TicketsPagesection() {
@@ -157,7 +158,8 @@ function TicketsPagesection() {
     if (queryFilters.size) params.size = queryFilters.size;
     if (queryFilters["ticketCode.contains"]) params["ticketCode.contains"] = queryFilters["ticketCode.contains"];
     if (queryFilters["purchaseReference.contains"]) params["purchaseReference.contains"] = queryFilters["purchaseReference.contains"];
-    if (queryFilters["status.equals"]) params["status.equals"] = queryFilters["status.equals"];
+    if (queryFilters["status.equals"]) params.status = queryFilters["status.equals"];
+    if (queryFilters.sort) params.sort = queryFilters.sort;
     findAllEventTicketPurchases(queryFilters.eventId, params);
   };
 
@@ -219,14 +221,32 @@ function TicketsPagesection() {
 
   // Map column name keys (e.g. "status") to API param keys (e.g. "status.equals")
   const handleFilterChange = (params: Record<string, string>) => {
-    const mapped: Record<string, string> = {};
+    const mapped: Record<string, string | undefined> = {};
     for (const [key, value] of Object.entries(params)) {
-      if (key === "status") mapped["status.equals"] = value;
+      if (key === "status") mapped["status.equals"] = value || undefined;
       else mapped[key] = value;
     }
     updateFilters({ ...mapped, page: 0 });
   };
 
+  // Map table column IDs to API entity field names
+  const COLUMN_TO_API_SORT: Record<string, string> = {
+    ticketCode: "ticketCode",
+    attendee: "attendee.firstName",
+    ticketName: "ticket.name",
+    quantity: "quantity",
+    pricePaid: "pricePaid",
+    purchaseDate: "purchaseDate",
+    status: "status",
+  };
+
+  const handleSortingChange = (sortStr: string) => {
+    // sortStr from DataTable is "columnId,asc|desc"
+    const [columnId, direction] = sortStr.split(",");
+    const apiField = COLUMN_TO_API_SORT[columnId] ?? columnId;
+    updateFilters({ sort: `${apiField},${direction}`, page: 0 });
+  };
+
   const getCustomActions = (item: TicketPurchaseItemI): CustomAction[] => [
     {
       id: "view",
@@ -237,6 +257,11 @@ function TicketsPagesection() {
     },
   ];
 
+  // Derive column filters from URL-synced filters so the DataTable UI reflects the current state
+  const externalColumnFilters = [
+    ...(filters["status.equals"] ? [{ id: "status", value: filters["status.equals"] }] : []),
+  ];
+
   const eventOptions: SelectOption[] =
     (events.data || [])
       .filter((e) => e.status === "PUBLISHED" || e.status === "COMPLETED")
@@ -294,6 +319,8 @@ function TicketsPagesection() {
         onPageSizeChange={handlePageSizeChange}
         onSearchChange={handleSearchChange}
         onFilterChange={handleFilterChange}
+        onSortingChange={handleSortingChange}
+        externalColumnFilters={externalColumnFilters}
       />
 
       <TicketSideSheet
diff --git a/src/app/(PageSections)/Dashboard/UserManagementPageSection/CreateUserForm.tsx b/src/app/(PageSections)/Dashboard/UserManagementPageSection/CreateUserForm.tsx
index f5b7b6f..59735cf 100644
--- a/src/app/(PageSections)/Dashboard/UserManagementPageSection/CreateUserForm.tsx
+++ b/src/app/(PageSections)/Dashboard/UserManagementPageSection/CreateUserForm.tsx
@@ -37,53 +37,54 @@ const getUserSchema = (isEdit: boolean) => {
       .min(2, "Last name must be at least 2 characters")
       .required("Last name is required"),
     email: Yup.string()
-      .email("Invalid email address")
+      .email("Please enter a valid email address")
+      .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address")
       .required("Email is required"),
     password: isEdit
       ? Yup.string()
-          .optional()
-          .test("password-strength", function (value) {
-            if (!value) return true; // Optional for edit mode
+        .optional()
+        .test("password-strength", function (value) {
+          if (!value) return true; // Optional for edit mode
 
-            const errors = [];
-            if (value.length < 8)
-              errors.push("Password must be at least 8 characters");
-            if (!/[A-Z]/.test(value))
-              errors.push(
-                "Password must contain at least one uppercase letter"
-              );
-            if (!/[a-z]/.test(value))
-              errors.push(
-                "Password must contain at least one lowercase letter"
-              );
-            if (!/\d/.test(value))
-              errors.push("Password must contain at least one number");
-            if (!/[!@#$%^&*(),.?":{}|<>]/.test(value))
-              errors.push(
-                "Password must contain at least one special character"
-              );
+          const errors = [];
+          if (value.length < 8)
+            errors.push("Password must be at least 8 characters");
+          if (!/[A-Z]/.test(value))
+            errors.push(
+              "Password must contain at least one uppercase letter"
+            );
+          if (!/[a-z]/.test(value))
+            errors.push(
+              "Password must contain at least one lowercase letter"
+            );
+          if (!/\d/.test(value))
+            errors.push("Password must contain at least one number");
+          if (!/[!@#$%^&*(),.?":{}|<>]/.test(value))
+            errors.push(
+              "Password must contain at least one special character"
+            );
 
-            if (errors.length > 0) {
-              return this.createError({ message: errors[0] });
-            }
-            return true;
-          })
+          if (errors.length > 0) {
+            return this.createError({ message: errors[0] });
+          }
+          return true;
+        })
       : Yup.string()
-          .min(8, "Password must be at least 8 characters")
-          .matches(
-            /[A-Z]/,
-            "Password must contain at least one uppercase letter"
-          )
-          .matches(
-            /[a-z]/,
-            "Password must contain at least one lowercase letter"
-          )
-          .matches(/\d/, "Password must contain at least one number")
-          .matches(
-            /[!@#$%^&*(),.?":{}|<>]/,
-            "Password must contain at least one special character"
-          )
-          .required("Password is required"),
+        .min(8, "Password must be at least 8 characters")
+        .matches(
+          /[A-Z]/,
+          "Password must contain at least one uppercase letter"
+        )
+        .matches(
+          /[a-z]/,
+          "Password must contain at least one lowercase letter"
+        )
+        .matches(/\d/, "Password must contain at least one number")
+        .matches(
+          /[!@#$%^&*(),.?":{}|<>]/,
+          "Password must contain at least one special character"
+        )
+        .required("Password is required"),
     confirmPassword: Yup.string().test("passwords-match", function (value) {
       const { password } = this.parent;
 
@@ -199,7 +200,7 @@ function UserForm({ closeModal, mode = "create", initialData }: UserFormProps) {
         };
 
         if (createUser) {
-         
+
           await createUser(createData);
           closeModal(); // Only close on success
         }
@@ -232,11 +233,11 @@ function UserForm({ closeModal, mode = "create", initialData }: UserFormProps) {
           // For edit mode, check if there are meaningful changes
           const hasChanges = isEdit
             ? values.firstName !== (initialData?.firstName || "") ||
-              values.lastName !== (initialData?.lastName || "") ||
-              values.email !== (initialData?.email || "") ||
-              JSON.stringify(values.authorities.sort()) !==
-                JSON.stringify((initialData?.authorities || []).sort()) ||
-              (values.password && values.password.length > 0) // Fixed: Added null check
+            values.lastName !== (initialData?.lastName || "") ||
+            values.email !== (initialData?.email || "") ||
+            JSON.stringify(values.authorities.sort()) !==
+            JSON.stringify((initialData?.authorities || []).sort()) ||
+            (values.password && values.password.length > 0) // Fixed: Added null check
             : true;
 
           // Button should be enabled when:
@@ -401,8 +402,8 @@ function UserForm({ closeModal, mode = "create", initialData }: UserFormProps) {
                       ? "Updating..."
                       : "Creating..."
                     : isEdit
-                    ? "Update User"
-                    : "Create User"}
+                      ? "Update User"
+                      : "Create User"}
                 </Button>
               </div>
             </Form>
diff --git a/src/app/(PageSections)/Landing/SponsorPagesection.tsx b/src/app/(PageSections)/Landing/SponsorPagesection.tsx
index 413efcc..ea8bb4d 100644
--- a/src/app/(PageSections)/Landing/SponsorPagesection.tsx
+++ b/src/app/(PageSections)/Landing/SponsorPagesection.tsx
@@ -50,124 +50,125 @@ interface SponsorFormData {
 // Validation schema factory (to access sponsor tiers data)
 const createSponsorValidationSchema = (sponsorTiersData?: SponsorTierI[]) =>
   Yup.object().shape({
-  name: Yup.string()
-    .trim()
-    .min(2, "Sponsor name must be at least 2 characters")
-    .max(100, "Sponsor name cannot exceed 100 characters")
-    .required("Sponsor name is required"),
-
-  logo: Yup.string().nullable().required("Logo is required"),
-
-  sponsorshipTierId: Yup.string().required("Sponsor tier is required"),
-
-  amount: Yup.string()
-    .trim()
-    .matches(/^\d+(\.\d{1,2})?$/, "Amount must be a valid number")
-    .required("Sponsorship amount is required")
-    .test(
-      "is-in-tier-range",
-      function (value) {
-        const { sponsorshipTierId } = this.parent;
-        if (!value || !sponsorshipTierId || !sponsorTiersData) return true;
-
-        const selectedTier = sponsorTiersData.find(
-          (tier) => String(tier.id) === sponsorshipTierId
-        );
-
-        if (!selectedTier) return true;
-
-        const amount = parseFloat(value);
-        const { minimumAmount, maximumAmount } = selectedTier;
-
-        if (amount < minimumAmount || amount > maximumAmount) {
-          return this.createError({
-            message: `Amount must be between ${minimumAmount.toLocaleString()} and ${maximumAmount.toLocaleString()}`,
-          });
+    name: Yup.string()
+      .trim()
+      .min(2, "Sponsor name must be at least 2 characters")
+      .max(100, "Sponsor name cannot exceed 100 characters")
+      .required("Sponsor name is required"),
+
+    logo: Yup.string().nullable().required("Logo is required"),
+
+    sponsorshipTierId: Yup.string().required("Sponsor tier is required"),
+
+    amount: Yup.string()
+      .trim()
+      .matches(/^\d+(\.\d{1,2})?$/, "Amount must be a valid number")
+      .required("Sponsorship amount is required")
+      .test(
+        "is-in-tier-range",
+        function (value) {
+          const { sponsorshipTierId } = this.parent;
+          if (!value || !sponsorshipTierId || !sponsorTiersData) return true;
+
+          const selectedTier = sponsorTiersData.find(
+            (tier) => String(tier.id) === sponsorshipTierId
+          );
+
+          if (!selectedTier) return true;
+
+          const amount = parseFloat(value);
+          const { minimumAmount, maximumAmount } = selectedTier;
+
+          if (amount < minimumAmount || amount > maximumAmount) {
+            return this.createError({
+              message: `Amount must be between ${minimumAmount.toLocaleString()} and ${maximumAmount.toLocaleString()}`,
+            });
+          }
+
+          return true;
         }
-
-        return true;
-      }
-    ),
-
-  description: Yup.string()
-    .trim()
-    .min(10, "Description must be at least 10 characters")
-    .max(500, "Description cannot exceed 500 characters")
-    .required("Description is required"),
-
-  // Company Information
-  websiteUrl: Yup.string()
-    .trim()
-    .url("Must be a valid URL")
-    .required("Website URL is required"),
-
-  industry: Yup.string()
-    .trim()
-    .max(100, "Industry cannot exceed 100 characters")
-    .required("Industry is required"),
-
-  address: Yup.string()
-    .trim()
-    .max(200, "Address cannot exceed 200 characters")
-    .required("Address is required"),
-
-  city: Yup.string()
-    .trim()
-    .max(100, "City cannot exceed 100 characters")
-    .required("City is required"),
-
-  state: Yup.string()
-    .trim()
-    .max(100, "State cannot exceed 100 characters")
-    .required("State is required"),
-
-  postalCode: Yup.string()
-    .trim()
-    .max(20, "Postal code cannot exceed 20 characters")
-    .required("Postal code is required"),
-
-  country: Yup.string()
-    .trim()
-    .max(100, "Country cannot exceed 100 characters")
-    .required("Country is required"),
-
-  // Contact Information
-  contactFirstName: Yup.string()
-    .trim()
-    .max(100, "First name cannot exceed 100 characters")
-    .required("Contact first name is required"),
-
-  contactLastName: Yup.string()
-    .trim()
-    .max(100, "Last name cannot exceed 100 characters")
-    .required("Contact last name is required"),
-
-  contactEmail: Yup.string()
-    .trim()
-    .email("Must be a valid email address")
-    .required("Contact email is required"),
-
-  contactPhone: Yup.string().trim().required("Contact phone is required"),
-
-  // Additional Information
-  notes: Yup.string()
-    .trim()
-    .max(1000, "Notes cannot exceed 1000 characters")
-    .notRequired(),
-
-  marketingMessage: Yup.string()
-    .trim()
-    .max(500, "Marketing message cannot exceed 500 characters")
-    .notRequired(),
-
-  specialBenefits: Yup.string()
-    .trim()
-    .max(1000, "Special benefits cannot exceed 1000 characters")
-    .notRequired(),
-
-  acceptedTerms: Yup.boolean()
-    .oneOf([true], "You must accept the terms and conditions")
-    .required("You must accept the terms and conditions"),
+      ),
+
+    description: Yup.string()
+      .trim()
+      .min(10, "Description must be at least 10 characters")
+      .max(500, "Description cannot exceed 500 characters")
+      .required("Description is required"),
+
+    // Company Information
+    websiteUrl: Yup.string()
+      .trim()
+      .url("Must be a valid URL")
+      .required("Website URL is required"),
+
+    industry: Yup.string()
+      .trim()
+      .max(100, "Industry cannot exceed 100 characters")
+      .required("Industry is required"),
+
+    address: Yup.string()
+      .trim()
+      .max(200, "Address cannot exceed 200 characters")
+      .required("Address is required"),
+
+    city: Yup.string()
+      .trim()
+      .max(100, "City cannot exceed 100 characters")
+      .required("City is required"),
+
+    state: Yup.string()
+      .trim()
+      .max(100, "State cannot exceed 100 characters")
+      .required("State is required"),
+
+    postalCode: Yup.string()
+      .trim()
+      .max(20, "Postal code cannot exceed 20 characters")
+      .required("Postal code is required"),
+
+    country: Yup.string()
+      .trim()
+      .max(100, "Country cannot exceed 100 characters")
+      .required("Country is required"),
+
+    // Contact Information
+    contactFirstName: Yup.string()
+      .trim()
+      .max(100, "First name cannot exceed 100 characters")
+      .required("Contact first name is required"),
+
+    contactLastName: Yup.string()
+      .trim()
+      .max(100, "Last name cannot exceed 100 characters")
+      .required("Contact last name is required"),
+
+    contactEmail: Yup.string()
+      .trim()
+      .email("Please enter a valid email address")
+      .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address")
+      .required("Contact email is required"),
+
+    contactPhone: Yup.string().trim().required("Contact phone is required"),
+
+    // Additional Information
+    notes: Yup.string()
+      .trim()
+      .max(1000, "Notes cannot exceed 1000 characters")
+      .notRequired(),
+
+    marketingMessage: Yup.string()
+      .trim()
+      .max(500, "Marketing message cannot exceed 500 characters")
+      .notRequired(),
+
+    specialBenefits: Yup.string()
+      .trim()
+      .max(1000, "Special benefits cannot exceed 1000 characters")
+      .notRequired(),
+
+    acceptedTerms: Yup.boolean()
+      .oneOf([true], "You must accept the terms and conditions")
+      .required("You must accept the terms and conditions"),
   });
 
 // Stepper Component
@@ -185,13 +186,12 @@ const Stepper: React.FC<StepperProps> = ({ steps, currentStep }) => {
             <div className="flex flex-col items-center flex-1">
               <div className="flex items-center justify-center w-full mb-2">
                 <div
-                  className={`w-10 h-10 sm:w-12 sm:h-12 rounded-full flex items-center justify-center font-semibold transition-all duration-300 ${
-                    index < currentStep
+                  className={`w-10 h-10 sm:w-12 sm:h-12 rounded-full flex items-center justify-center font-semibold transition-all duration-300 ${index < currentStep
                       ? "bg-primary-950 text-white"
                       : index === currentStep
-                      ? "bg-primary-950 text-white ring-3 ring-primary-500/20"
-                      : "bg-gray-200 text-gray-500"
-                  }`}
+                        ? "bg-primary-950 text-white ring-3 ring-primary-500/20"
+                        : "bg-gray-200 text-gray-500"
+                    }`}
                 >
                   {index < currentStep ? (
                     <Check className="w-5 h-5" />
@@ -201,9 +201,8 @@ const Stepper: React.FC<StepperProps> = ({ steps, currentStep }) => {
                 </div>
               </div>
               <p
-                className={`text-xs sm:text-sm font-medium text-center transition-all duration-300 px-1 ${
-                  index <= currentStep ? "text-primary-950" : "text-gray-400"
-                }`}
+                className={`text-xs sm:text-sm font-medium text-center transition-all duration-300 px-1 ${index <= currentStep ? "text-primary-950" : "text-gray-400"
+                  }`}
               >
                 {step}
               </p>
@@ -211,9 +210,8 @@ const Stepper: React.FC<StepperProps> = ({ steps, currentStep }) => {
             {index < steps.length - 1 && (
               <div className="flex-1 h-[2px] -mt-5">
                 <div
-                  className={`h-full rounded transition-all duration-300 ${
-                    index < currentStep ? "bg-primary-950" : "bg-gray-200"
-                  }`}
+                  className={`h-full rounded transition-all duration-300 ${index < currentStep ? "bg-primary-950" : "bg-gray-200"
+                    }`}
                 />
               </div>
             )}
@@ -647,486 +645,486 @@ function SponsorPagesection() {
             </p>
           </div>
         ) : (
-        <>
-        <div className="mb-6">
-          <h2 className="text-2xl font-bold text-primary-950">
-            Become a Sponsor
-          </h2>
-          <p className="text-sm text-gray-600 mt-1">
-            Fill in the details below to register as a sponsor for this event
-          </p>
-        </div>
-
-        <Stepper steps={steps} currentStep={currentStep} />
-
-        <Formik
-          initialValues={initialValues}
-          validationSchema={createSponsorValidationSchema(sponsorTiers.data)}
-          onSubmit={handleSubmit}
-          enableReinitialize
-        >
-          {({
-            values,
-            errors,
-            touched,
-            setFieldValue,
-            setFieldTouched,
-            isValid,
-            dirty,
-          }) => (
-            <Form className="space-y-6 rounded-lg border p-4 sm:p-6 bg-white shadow-sm">
-              {/* Step 0: Basic Information */}
-              {currentStep === 0 && (
-                <div className="space-y-6 animate-in fade-in duration-300">
-                  <h3 className="text-lg font-semibold text-primary-950 border-b pb-2">
-                    Basic Sponsor Information
-                  </h3>
-
-                  {/* Sponsor Name */}
-                  <FormInput
-                    label="Sponsor Name"
-                    placeholder="Enter sponsor organization name"
-                    required
-                    value={values.name}
-                    onChange={(e) => setFieldValue("name", e.target.value)}
-                    onBlur={() => setFieldTouched("name", true)}
-                    error={touched.name && errors.name ? errors.name : undefined}
-                    helpText="Enter the full name of the sponsor organization"
-                  />
-
-                  {/* Logo Upload */}
-                  <div className="space-y-2">
-                    <label className="block text-sm font-medium text-foreground">
-                      Logo <span className="text-destructive ml-1">*</span>
-                    </label>
-                    <ImageUploader
-                      value={logoFile}
-                      onChange={(file) =>
-                        handleFileUpload("logo", file, setFieldValue)
-                      }
-                      onRemove={() => {
-                        setFieldValue("logo", null);
-                        setLogoFile(null);
-                      }}
-                      label="Sponsor Logo"
-                      showTitle={false}
-                      acceptedFormats={["JPG", "PNG", "GIF", "WebP"]}
-                      maxSize="max 5MB"
-                      className="w-full"
-                      disabled={uploadingFields.logo}
-                      isUploading={uploadingFields.logo}
-                    />
-                    {touched.logo && errors.logo && (
-                      <p className="text-sm text-destructive">{errors.logo}</p>
-                    )}
-                    {values.logo && !logoFile && (
-                      <div className="flex items-center gap-2 text-sm text-green-600">
-                        <Check className="w-4 h-4" />
-                        <span>Logo uploaded successfully</span>
+          <>
+            <div className="mb-6">
+              <h2 className="text-2xl font-bold text-primary-950">
+                Become a Sponsor
+              </h2>
+              <p className="text-sm text-gray-600 mt-1">
+                Fill in the details below to register as a sponsor for this event
+              </p>
+            </div>
+
+            <Stepper steps={steps} currentStep={currentStep} />
+
+            <Formik
+              initialValues={initialValues}
+              validationSchema={createSponsorValidationSchema(sponsorTiers.data)}
+              onSubmit={handleSubmit}
+              enableReinitialize
+            >
+              {({
+                values,
+                errors,
+                touched,
+                setFieldValue,
+                setFieldTouched,
+                isValid,
+                dirty,
+              }) => (
+                <Form className="space-y-6 rounded-lg border p-4 sm:p-6 bg-white shadow-sm">
+                  {/* Step 0: Basic Information */}
+                  {currentStep === 0 && (
+                    <div className="space-y-6 animate-in fade-in duration-300">
+                      <h3 className="text-lg font-semibold text-primary-950 border-b pb-2">
+                        Basic Sponsor Information
+                      </h3>
+
+                      {/* Sponsor Name */}
+                      <FormInput
+                        label="Sponsor Name"
+                        placeholder="Enter sponsor organization name"
+                        required
+                        value={values.name}
+                        onChange={(e) => setFieldValue("name", e.target.value)}
+                        onBlur={() => setFieldTouched("name", true)}
+                        error={touched.name && errors.name ? errors.name : undefined}
+                        helpText="Enter the full name of the sponsor organization"
+                      />
+
+                      {/* Logo Upload */}
+                      <div className="space-y-2">
+                        <label className="block text-sm font-medium text-foreground">
+                          Logo <span className="text-destructive ml-1">*</span>
+                        </label>
+                        <ImageUploader
+                          value={logoFile}
+                          onChange={(file) =>
+                            handleFileUpload("logo", file, setFieldValue)
+                          }
+                          onRemove={() => {
+                            setFieldValue("logo", null);
+                            setLogoFile(null);
+                          }}
+                          label="Sponsor Logo"
+                          showTitle={false}
+                          acceptedFormats={["JPG", "PNG", "GIF", "WebP"]}
+                          maxSize="max 5MB"
+                          className="w-full"
+                          disabled={uploadingFields.logo}
+                          isUploading={uploadingFields.logo}
+                        />
+                        {touched.logo && errors.logo && (
+                          <p className="text-sm text-destructive">{errors.logo}</p>
+                        )}
+                        {values.logo && !logoFile && (
+                          <div className="flex items-center gap-2 text-sm text-green-600">
+                            <Check className="w-4 h-4" />
+                            <span>Logo uploaded successfully</span>
+                          </div>
+                        )}
+                        <p className="text-xs text-gray-500">
+                          Upload the sponsor&apos;s logo (max 5MB)
+                        </p>
                       </div>
-                    )}
-                    <p className="text-xs text-gray-500">
-                      Upload the sponsor&apos;s logo (max 5MB)
-                    </p>
-                  </div>
 
-                  {/* Sponsor Tier and Amount Row */}
-                  <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
-                    {/* Sponsor Tier */}
-                    <UniversalSelect
-                      label="Sponsor Tier"
-                      required
-                      placeholder="Select a sponsor tier"
-                      value={values.sponsorshipTierId}
-                      onValueChange={(value) => {
-                        setFieldValue("sponsorshipTierId", value as string);
-                        // Auto-fill amount with middle value of tier range
-                        const middleAmount = calculateMiddleAmount(value as string);
-                        if (middleAmount > 0) {
-                          setFieldValue("amount", middleAmount.toString());
-                        }
-                      }}
-                      options={tierOptions}
-                      searchable
-                      error={
-                        touched.sponsorshipTierId && errors.sponsorshipTierId
-                          ? errors.sponsorshipTierId
-                          : undefined
-                      }
-                      helperText="Choose the sponsorship tier"
-                      disabled={tierOptions.length === 0}
-                    />
-
-                    {/* Amount */}
-                    <FormInput
-                      label="Sponsorship Amount"
-                      placeholder="50000.00"
-                      required
-                      type="text"
-                      value={values.amount}
-                      onChange={(e) => setFieldValue("amount", e.target.value)}
-                      onBlur={() => setFieldTouched("amount", true)}
-                      error={
-                        touched.amount && errors.amount ? errors.amount : undefined
-                      }
-                      helpText={
-                        values.sponsorshipTierId
-                          ? getTierRangeText(values.sponsorshipTierId)
-                          : "Select a tier first to see the amount range"
-                      }
-                    />
-                  </div>
+                      {/* Sponsor Tier and Amount Row */}
+                      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
+                        {/* Sponsor Tier */}
+                        <UniversalSelect
+                          label="Sponsor Tier"
+                          required
+                          placeholder="Select a sponsor tier"
+                          value={values.sponsorshipTierId}
+                          onValueChange={(value) => {
+                            setFieldValue("sponsorshipTierId", value as string);
+                            // Auto-fill amount with middle value of tier range
+                            const middleAmount = calculateMiddleAmount(value as string);
+                            if (middleAmount > 0) {
+                              setFieldValue("amount", middleAmount.toString());
+                            }
+                          }}
+                          options={tierOptions}
+                          searchable
+                          error={
+                            touched.sponsorshipTierId && errors.sponsorshipTierId
+                              ? errors.sponsorshipTierId
+                              : undefined
+                          }
+                          helperText="Choose the sponsorship tier"
+                          disabled={tierOptions.length === 0}
+                        />
+
+                        {/* Amount */}
+                        <FormInput
+                          label="Sponsorship Amount"
+                          placeholder="50000.00"
+                          required
+                          type="text"
+                          value={values.amount}
+                          onChange={(e) => setFieldValue("amount", e.target.value)}
+                          onBlur={() => setFieldTouched("amount", true)}
+                          error={
+                            touched.amount && errors.amount ? errors.amount : undefined
+                          }
+                          helpText={
+                            values.sponsorshipTierId
+                              ? getTierRangeText(values.sponsorshipTierId)
+                              : "Select a tier first to see the amount range"
+                          }
+                        />
+                      </div>
 
-                  {/* Description */}
-                  <FormTextarea
-                    label="Description"
-                    placeholder="Describe the sponsor and sponsorship details"
-                    required
-                    value={values.description}
-                    onChange={(e) => setFieldValue("description", e.target.value)}
-                    onBlur={() => setFieldTouched("description", true)}
-                    error={
-                      touched.description && errors.description
-                        ? errors.description
-                        : undefined
-                    }
-                    rows={4}
-                  />
-                </div>
-              )}
+                      {/* Description */}
+                      <FormTextarea
+                        label="Description"
+                        placeholder="Describe the sponsor and sponsorship details"
+                        required
+                        value={values.description}
+                        onChange={(e) => setFieldValue("description", e.target.value)}
+                        onBlur={() => setFieldTouched("description", true)}
+                        error={
+                          touched.description && errors.description
+                            ? errors.description
+                            : undefined
+                        }
+                        rows={4}
+                      />
+                    </div>
+                  )}
 
-              {/* Step 1: Company Information */}
-              {currentStep === 1 && (
-                <div className="space-y-6 animate-in fade-in duration-300">
-                  <h3 className="text-lg font-semibold text-primary-950 border-b pb-2">
-                    Company Information
-                  </h3>
-
-                  {/* Website URL */}
-                  <FormInput
-                    label="Website URL"
-                    placeholder="https://example.com"
-                    type="url"
-                    required
-                    value={values.websiteUrl}
-                    onChange={(e) => setFieldValue("websiteUrl", e.target.value)}
-                    onBlur={() => setFieldTouched("websiteUrl", true)}
-                    error={
-                      touched.websiteUrl && errors.websiteUrl
-                        ? errors.websiteUrl
-                        : undefined
-                    }
-                    helpText="Company website URL"
-                  />
-
-                  {/* Industry */}
-                  <FormInput
-                    label="Industry"
-                    placeholder="Technology, Healthcare, Finance, etc."
-                    required
-                    value={values.industry}
-                    onChange={(e) => setFieldValue("industry", e.target.value)}
-                    onBlur={() => setFieldTouched("industry", true)}
-                    error={
-                      touched.industry && errors.industry
-                        ? errors.industry
-                        : undefined
-                    }
-                    helpText="Industry or sector"
-                  />
-
-                  {/* Address */}
-                  <FormInput
-                    label="Address"
-                    placeholder="Street address"
-                    required
-                    value={values.address}
-                    onChange={(e) => setFieldValue("address", e.target.value)}
-                    onBlur={() => setFieldTouched("address", true)}
-                    error={
-                      touched.address && errors.address
-                        ? errors.address
-                        : undefined
-                    }
-                  />
-
-                  {/* City, State, Postal Code */}
-                  <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
-                    <FormInput
-                      label="City"
-                      placeholder="City"
-                      required
-                      value={values.city}
-                      onChange={(e) => setFieldValue("city", e.target.value)}
-                      onBlur={() => setFieldTouched("city", true)}
-                      error={
-                        touched.city && errors.city ? errors.city : undefined
-                      }
-                    />
-
-                    <FormInput
-                      label="State"
-                      placeholder="State/Province"
-                      required
-                      value={values.state}
-                      onChange={(e) => setFieldValue("state", e.target.value)}
-                      onBlur={() => setFieldTouched("state", true)}
-                      error={
-                        touched.state && errors.state ? errors.state : undefined
-                      }
-                    />
-
-                    <FormInput
-                      label="Postal Code"
-                      placeholder="Postal/Zip code"
-                      required
-                      value={values.postalCode}
-                      onChange={(e) =>
-                        setFieldValue("postalCode", e.target.value)
-                      }
-                      onBlur={() => setFieldTouched("postalCode", true)}
-                      error={
-                        touched.postalCode && errors.postalCode
-                          ? errors.postalCode
-                          : undefined
-                      }
-                    />
-                  </div>
+                  {/* Step 1: Company Information */}
+                  {currentStep === 1 && (
+                    <div className="space-y-6 animate-in fade-in duration-300">
+                      <h3 className="text-lg font-semibold text-primary-950 border-b pb-2">
+                        Company Information
+                      </h3>
+
+                      {/* Website URL */}
+                      <FormInput
+                        label="Website URL"
+                        placeholder="https://example.com"
+                        type="url"
+                        required
+                        value={values.websiteUrl}
+                        onChange={(e) => setFieldValue("websiteUrl", e.target.value)}
+                        onBlur={() => setFieldTouched("websiteUrl", true)}
+                        error={
+                          touched.websiteUrl && errors.websiteUrl
+                            ? errors.websiteUrl
+                            : undefined
+                        }
+                        helpText="Company website URL"
+                      />
+
+                      {/* Industry */}
+                      <FormInput
+                        label="Industry"
+                        placeholder="Technology, Healthcare, Finance, etc."
+                        required
+                        value={values.industry}
+                        onChange={(e) => setFieldValue("industry", e.target.value)}
+                        onBlur={() => setFieldTouched("industry", true)}
+                        error={
+                          touched.industry && errors.industry
+                            ? errors.industry
+                            : undefined
+                        }
+                        helpText="Industry or sector"
+                      />
+
+                      {/* Address */}
+                      <FormInput
+                        label="Address"
+                        placeholder="Street address"
+                        required
+                        value={values.address}
+                        onChange={(e) => setFieldValue("address", e.target.value)}
+                        onBlur={() => setFieldTouched("address", true)}
+                        error={
+                          touched.address && errors.address
+                            ? errors.address
+                            : undefined
+                        }
+                      />
+
+                      {/* City, State, Postal Code */}
+                      <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
+                        <FormInput
+                          label="City"
+                          placeholder="City"
+                          required
+                          value={values.city}
+                          onChange={(e) => setFieldValue("city", e.target.value)}
+                          onBlur={() => setFieldTouched("city", true)}
+                          error={
+                            touched.city && errors.city ? errors.city : undefined
+                          }
+                        />
+
+                        <FormInput
+                          label="State"
+                          placeholder="State/Province"
+                          required
+                          value={values.state}
+                          onChange={(e) => setFieldValue("state", e.target.value)}
+                          onBlur={() => setFieldTouched("state", true)}
+                          error={
+                            touched.state && errors.state ? errors.state : undefined
+                          }
+                        />
+
+                        <FormInput
+                          label="Postal Code"
+                          placeholder="Postal/Zip code"
+                          required
+                          value={values.postalCode}
+                          onChange={(e) =>
+                            setFieldValue("postalCode", e.target.value)
+                          }
+                          onBlur={() => setFieldTouched("postalCode", true)}
+                          error={
+                            touched.postalCode && errors.postalCode
+                              ? errors.postalCode
+                              : undefined
+                          }
+                        />
+                      </div>
 
-                  {/* Country */}
-                  <FormInput
-                    label="Country"
-                    placeholder="Country"
-                    required
-                    value={values.country}
-                    onChange={(e) => setFieldValue("country", e.target.value)}
-                    onBlur={() => setFieldTouched("country", true)}
-                    error={
-                      touched.country && errors.country
-                        ? errors.country
-                        : undefined
-                    }
-                  />
-                </div>
-              )}
+                      {/* Country */}
+                      <FormInput
+                        label="Country"
+                        placeholder="Country"
+                        required
+                        value={values.country}
+                        onChange={(e) => setFieldValue("country", e.target.value)}
+                        onBlur={() => setFieldTouched("country", true)}
+                        error={
+                          touched.country && errors.country
+                            ? errors.country
+                            : undefined
+                        }
+                      />
+                    </div>
+                  )}
 
-              {/* Step 2: Contact Information */}
-              {currentStep === 2 && (
-                <div className="space-y-6 animate-in fade-in duration-300">
-                  <h3 className="text-lg font-semibold text-primary-950 border-b pb-2">
-                    Contact Information
-                  </h3>
-
-                  {/* Contact Person Name */}
-                  <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
-                    <FormInput
-                      label="First Name"
-                      placeholder="First name"
-                      required
-                      value={values.contactFirstName}
-                      onChange={(e) =>
-                        setFieldValue("contactFirstName", e.target.value)
-                      }
-                      onBlur={() => setFieldTouched("contactFirstName", true)}
-                      error={
-                        touched.contactFirstName && errors.contactFirstName
-                          ? errors.contactFirstName
-                          : undefined
-                      }
-                    />
-
-                    <FormInput
-                      label="Last Name"
-                      placeholder="Last name"
-                      required
-                      value={values.contactLastName}
-                      onChange={(e) =>
-                        setFieldValue("contactLastName", e.target.value)
-                      }
-                      onBlur={() => setFieldTouched("contactLastName", true)}
-                      error={
-                        touched.contactLastName && errors.contactLastName
-                          ? errors.contactLastName
-                          : undefined
-                      }
-                    />
-
-                    <FormInput
-                      label="Contact Email"
-                      placeholder="email@example.com"
-                      type="email"
-                      required
-                      value={values.contactEmail}
-                      onChange={(e) =>
-                        setFieldValue("contactEmail", e.target.value)
-                      }
-                      onBlur={() => setFieldTouched("contactEmail", true)}
-                      error={
-                        touched.contactEmail && errors.contactEmail
-                          ? errors.contactEmail
-                          : undefined
-                      }
-                    />
-
-                    <PhoneNumberInput
-                      label="Contact Phone"
-                      placeholder="Enter phone number"
-                      required
-                      value={values.contactPhone}
-                      onChange={(value) => setFieldValue("contactPhone", value)}
-                      error={
-                        touched.contactPhone && errors.contactPhone
-                          ? errors.contactPhone
-                          : undefined
-                      }
-                      defaultCountry="TZ"
-                    />
-                  </div>
-                </div>
-              )}
+                  {/* Step 2: Contact Information */}
+                  {currentStep === 2 && (
+                    <div className="space-y-6 animate-in fade-in duration-300">
+                      <h3 className="text-lg font-semibold text-primary-950 border-b pb-2">
+                        Contact Information
+                      </h3>
+
+                      {/* Contact Person Name */}
+                      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
+                        <FormInput
+                          label="First Name"
+                          placeholder="First name"
+                          required
+                          value={values.contactFirstName}
+                          onChange={(e) =>
+                            setFieldValue("contactFirstName", e.target.value)
+                          }
+                          onBlur={() => setFieldTouched("contactFirstName", true)}
+                          error={
+                            touched.contactFirstName && errors.contactFirstName
+                              ? errors.contactFirstName
+                              : undefined
+                          }
+                        />
+
+                        <FormInput
+                          label="Last Name"
+                          placeholder="Last name"
+                          required
+                          value={values.contactLastName}
+                          onChange={(e) =>
+                            setFieldValue("contactLastName", e.target.value)
+                          }
+                          onBlur={() => setFieldTouched("contactLastName", true)}
+                          error={
+                            touched.contactLastName && errors.contactLastName
+                              ? errors.contactLastName
+                              : undefined
+                          }
+                        />
+
+                        <FormInput
+                          label="Contact Email"
+                          placeholder="email@example.com"
+                          type="email"
+                          required
+                          value={values.contactEmail}
+                          onChange={(e) =>
+                            setFieldValue("contactEmail", e.target.value)
+                          }
+                          onBlur={() => setFieldTouched("contactEmail", true)}
+                          error={
+                            touched.contactEmail && errors.contactEmail
+                              ? errors.contactEmail
+                              : undefined
+                          }
+                        />
+
+                        <PhoneNumberInput
+                          label="Contact Phone"
+                          placeholder="Enter phone number"
+                          required
+                          value={values.contactPhone}
+                          onChange={(value) => setFieldValue("contactPhone", value)}
+                          error={
+                            touched.contactPhone && errors.contactPhone
+                              ? errors.contactPhone
+                              : undefined
+                          }
+                          defaultCountry="TZ"
+                        />
+                      </div>
+                    </div>
+                  )}
 
-              {/* Step 3: Additional Information */}
-              {currentStep === 3 && (
-                <div className="space-y-6 animate-in fade-in duration-300">
-                  <h3 className="text-lg font-semibold text-primary-950 border-b pb-2">
-                    Additional Information
-                  </h3>
-
-                  {/* Marketing Message */}
-                  <FormTextarea
-                    label="Marketing Message"
-                    placeholder="Message to be displayed with your sponsorship"
-                    value={values.marketingMessage}
-                    onChange={(e) =>
-                      setFieldValue("marketingMessage", e.target.value)
-                    }
-                    onBlur={() => setFieldTouched("marketingMessage", true)}
-                    error={
-                      touched.marketingMessage && errors.marketingMessage
-                        ? errors.marketingMessage
-                        : undefined
-                    }
-                    rows={3}
-                  />
-
-                  {/* Special Benefits */}
-                  <FormTextarea
-                    label="Special Benefits"
-                    placeholder="Any special benefits or requirements"
-                    value={values.specialBenefits}
-                    onChange={(e) =>
-                      setFieldValue("specialBenefits", e.target.value)
-                    }
-                    onBlur={() => setFieldTouched("specialBenefits", true)}
-                    error={
-                      touched.specialBenefits && errors.specialBenefits
-                        ? errors.specialBenefits
-                        : undefined
-                    }
-                    rows={3}
-                  />
-
-                  {/* Notes */}
-                  <FormTextarea
-                    label="Notes"
-                    placeholder="Internal notes about this sponsor"
-                    value={values.notes}
-                    onChange={(e) => setFieldValue("notes", e.target.value)}
-                    onBlur={() => setFieldTouched("notes", true)}
-                    error={
-                      touched.notes && errors.notes ? errors.notes : undefined
-                    }
-                    rows={3}
-                  />
-
-                  {/* Accept Terms */}
-                  <div className="flex items-start space-x-3 p-4 bg-gray-50 rounded-lg border border-gray-200">
-                    <input
-                      type="checkbox"
-                      id="acceptedTerms"
-                      checked={values.acceptedTerms}
-                      onChange={(e) =>
-                        setFieldValue("acceptedTerms", e.target.checked)
-                      }
-                      onBlur={() => setFieldTouched("acceptedTerms", true)}
-                      className="mt-1 h-4 w-4 text-primary-950 focus:ring-primary-950 border-gray-300 rounded"
-                    />
-                    <label
-                      htmlFor="acceptedTerms"
-                      className="text-sm text-gray-700 cursor-pointer"
-                    >
-                      I accept the terms and conditions and confirm that all
-                      information provided is accurate. I understand that this
-                      sponsorship submission will be reviewed by the event
-                      organizers.{" "}
-                      <span className="text-destructive">*</span>
-                    </label>
-                  </div>
-                  {touched.acceptedTerms && errors.acceptedTerms && (
-                    <p className="text-sm text-destructive -mt-4">
-                      {errors.acceptedTerms}
-                    </p>
+                  {/* Step 3: Additional Information */}
+                  {currentStep === 3 && (
+                    <div className="space-y-6 animate-in fade-in duration-300">
+                      <h3 className="text-lg font-semibold text-primary-950 border-b pb-2">
+                        Additional Information
+                      </h3>
+
+                      {/* Marketing Message */}
+                      <FormTextarea
+                        label="Marketing Message"
+                        placeholder="Message to be displayed with your sponsorship"
+                        value={values.marketingMessage}
+                        onChange={(e) =>
+                          setFieldValue("marketingMessage", e.target.value)
+                        }
+                        onBlur={() => setFieldTouched("marketingMessage", true)}
+                        error={
+                          touched.marketingMessage && errors.marketingMessage
+                            ? errors.marketingMessage
+                            : undefined
+                        }
+                        rows={3}
+                      />
+
+                      {/* Special Benefits */}
+                      <FormTextarea
+                        label="Special Benefits"
+                        placeholder="Any special benefits or requirements"
+                        value={values.specialBenefits}
+                        onChange={(e) =>
+                          setFieldValue("specialBenefits", e.target.value)
+                        }
+                        onBlur={() => setFieldTouched("specialBenefits", true)}
+                        error={
+                          touched.specialBenefits && errors.specialBenefits
+                            ? errors.specialBenefits
+                            : undefined
+                        }
+                        rows={3}
+                      />
+
+                      {/* Notes */}
+                      <FormTextarea
+                        label="Notes"
+                        placeholder="Internal notes about this sponsor"
+                        value={values.notes}
+                        onChange={(e) => setFieldValue("notes", e.target.value)}
+                        onBlur={() => setFieldTouched("notes", true)}
+                        error={
+                          touched.notes && errors.notes ? errors.notes : undefined
+                        }
+                        rows={3}
+                      />
+
+                      {/* Accept Terms */}
+                      <div className="flex items-start space-x-3 p-4 bg-gray-50 rounded-lg border border-gray-200">
+                        <input
+                          type="checkbox"
+                          id="acceptedTerms"
+                          checked={values.acceptedTerms}
+                          onChange={(e) =>
+                            setFieldValue("acceptedTerms", e.target.checked)
+                          }
+                          onBlur={() => setFieldTouched("acceptedTerms", true)}
+                          className="mt-1 h-4 w-4 text-primary-950 focus:ring-primary-950 border-gray-300 rounded"
+                        />
+                        <label
+                          htmlFor="acceptedTerms"
+                          className="text-sm text-gray-700 cursor-pointer"
+                        >
+                          I accept the terms and conditions and confirm that all
+                          information provided is accurate. I understand that this
+                          sponsorship submission will be reviewed by the event
+                          organizers.{" "}
+                          <span className="text-destructive">*</span>
+                        </label>
+                      </div>
+                      {touched.acceptedTerms && errors.acceptedTerms && (
+                        <p className="text-sm text-destructive -mt-4">
+                          {errors.acceptedTerms}
+                        </p>
+                      )}
+                    </div>
                   )}
-                </div>
-              )}
 
-              {/* Navigation Buttons */}
-              <div className="flex gap-4 pt-6 border-t justify-between">
-                <Button
-                  type="button"
-                  variant="outline"
-                  onClick={prevStep}
-                  disabled={currentStep === 0}
-                  className="flex items-center gap-2"
-                >
-                  <ChevronLeft className="w-4 h-4" />
-                  Previous
-                </Button>
-
-                <div className="flex gap-3">
-                  <Button
-                    type="button"
-                    variant="outline"
-                    onClick={() => router.back()}
-                    disabled={isSubmitting}
-                  >
-                    Cancel
-                  </Button>
-
-                  {currentStep < steps.length - 1 ? (
+                  {/* Navigation Buttons */}
+                  <div className="flex gap-4 pt-6 border-t justify-between">
                     <Button
                       type="button"
-                      onClick={() => nextStep(values, setFieldTouched)}
-                      disabled={
-                        !validateCurrentStep(errors, touched, values) ||
-                        uploadingFields.logo
-                      }
-                      className="flex items-center gap-2 bg-primary-950 hover:bg-primary-900"
+                      variant="outline"
+                      onClick={prevStep}
+                      disabled={currentStep === 0}
+                      className="flex items-center gap-2"
                     >
-                      Next
-                      <ChevronRight className="w-4 h-4" />
+                      <ChevronLeft className="w-4 h-4" />
+                      Previous
                     </Button>
-                  ) : (
-                    <Button
-                      type="submit"
-                      disabled={
-                        !isValid || !dirty || isSubmitting || uploadingFields.logo
-                      }
-                      className="min-w-[140px] bg-primary-950 hover:bg-primary-900"
-                    >
-                      {uploadingFields.logo
-                        ? "Uploading..."
-                        : isSubmitting
-                        ? "Submitting..."
-                        : "Submit Registration"}
-                    </Button>
-                  )}
-                </div>
-              </div>
-            </Form>
-          )}
-        </Formik>
-        </>
+
+                    <div className="flex gap-3">
+                      <Button
+                        type="button"
+                        variant="outline"
+                        onClick={() => router.back()}
+                        disabled={isSubmitting}
+                      >
+                        Cancel
+                      </Button>
+
+                      {currentStep < steps.length - 1 ? (
+                        <Button
+                          type="button"
+                          onClick={() => nextStep(values, setFieldTouched)}
+                          disabled={
+                            !validateCurrentStep(errors, touched, values) ||
+                            uploadingFields.logo
+                          }
+                          className="flex items-center gap-2 bg-primary-950 hover:bg-primary-900"
+                        >
+                          Next
+                          <ChevronRight className="w-4 h-4" />
+                        </Button>
+                      ) : (
+                        <Button
+                          type="submit"
+                          disabled={
+                            !isValid || !dirty || isSubmitting || uploadingFields.logo
+                          }
+                          className="min-w-[140px] bg-primary-950 hover:bg-primary-900"
+                        >
+                          {uploadingFields.logo
+                            ? "Uploading..."
+                            : isSubmitting
+                              ? "Submitting..."
+                              : "Submit Registration"}
+                        </Button>
+                      )}
+                    </div>
+                  </div>
+                </Form>
+              )}
+            </Formik>
+          </>
         )}
       </div>
     </div>
diff --git a/src/app/(PageSections)/Landing/sections/Donation/DonationForm.tsx b/src/app/(PageSections)/Landing/sections/Donation/DonationForm.tsx
index 780dc62..497c11c 100644
--- a/src/app/(PageSections)/Landing/sections/Donation/DonationForm.tsx
+++ b/src/app/(PageSections)/Landing/sections/Donation/DonationForm.tsx
@@ -91,7 +91,7 @@ const validationSchema = Yup.object({
   phone: Yup.string()
     .required("Phone number is required")
     .test("is-valid-tanzanian-phone", "Enter a valid Tanzanian phone number (e.g., 0756789456, +255756789456, or 255756789456)", isValidTanzanianPhone),
-  email: Yup.string().email("Invalid email address").nullable(),
+  email: Yup.string().email("Please enter a valid email address").matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address").nullable(),
   donationCause: Yup.string().required("Please select a donation course"),
   donationAmount: Yup.number()
     .required("Donation amount is required")
@@ -274,7 +274,7 @@ function DonationForm() {
                   required={true}
                   value={formik.values.phone}
                   onChange={(value) => formik.setFieldValue("phone", value)}
-               
+
                 />
                 {formik.touched.phone && formik.errors.phone && (
                   <p className="text-red-500 text-sm mt-1">
diff --git a/src/app/(PageSections)/PaymentCheckoutPageSection/Sections/EditParticipantModal.tsx b/src/app/(PageSections)/PaymentCheckoutPageSection/Sections/EditParticipantModal.tsx
index 3727a58..7e19866 100644
--- a/src/app/(PageSections)/PaymentCheckoutPageSection/Sections/EditParticipantModal.tsx
+++ b/src/app/(PageSections)/PaymentCheckoutPageSection/Sections/EditParticipantModal.tsx
@@ -47,7 +47,8 @@ const validationSchema = Yup.object({
     .required("Last name is required"),
   email: Yup.string()
     .transform((val) => (val === "" ? undefined : val))
-    .email("Invalid email address")
+    .email("Please enter a valid email address")
+    .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address")
     .optional(),
   phoneNumber: Yup.string()
     .min(10, "Phone number must be at least 10 digits")
diff --git a/src/app/(Pages)/account/page.tsx b/src/app/(Pages)/account/page.tsx
index 78a2f1c..938cb03 100644
--- a/src/app/(Pages)/account/page.tsx
+++ b/src/app/(Pages)/account/page.tsx
@@ -99,9 +99,9 @@ export default function AccountPage() {
         firstName: user.firstName || "",
         lastName: user.lastName || "",
         middleName: user.middleName || "",
-        phoneNumber: user.phone || "",
+        phoneNumber: user.phoneNumber || "",
         country: "",
-        language: "en",
+        language: user.langKey,
         dateOfBirth: null,
       });
     }
@@ -122,9 +122,9 @@ export default function AccountPage() {
         firstName: user.firstName || "",
         lastName: user.lastName || "",
         middleName: user.middleName || "",
-        phoneNumber: user.phone || "",
+        phoneNumber: user.phoneNumber || "",
         country: "",
-        language: "en",
+        language:user.langKey,
         dateOfBirth: null,
       });
     }
@@ -144,15 +144,13 @@ export default function AccountPage() {
         firstName: formData.firstName,
         lastName: formData.lastName,
         middleName: formData.middleName,
+        langKey:formData.language,
         phoneNumber: formData.phoneNumber || user?.phoneNumber,
       });
 
-      toast({
-        title: "Success",
-        description: "Profile updated successfully",
-      });
+   
     } catch (error) {
-      console.error("Failed to update profile:", error);
+    
       toast({
         title: "Error",
         description: "Failed to update profile",
diff --git a/src/components/shared/registration/GroupRegistration.tsx b/src/components/shared/registration/GroupRegistration.tsx
index 3441212..60c0d96 100644
--- a/src/components/shared/registration/GroupRegistration.tsx
+++ b/src/components/shared/registration/GroupRegistration.tsx
@@ -52,7 +52,9 @@ const validationSchema = Yup.object().shape({
     .matches(/^\+?[1-9]\d{1,14}$/, "Please enter a valid phone number"),
   representativeEmail: Yup.string()
     .required("Email is required")
-    .email("Please enter a valid email address"),
+    .email("Please enter a valid email address")
+    .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address")
+    ,
   termsAccepted: Yup.boolean()
     .oneOf([true], "You must accept the terms and conditions")
     .required("You must accept the terms and conditions"),
diff --git a/src/components/shared/registration/ParticipantDetails.tsx b/src/components/shared/registration/ParticipantDetails.tsx
index 153fdb6..5a4fc3c 100644
--- a/src/components/shared/registration/ParticipantDetails.tsx
+++ b/src/components/shared/registration/ParticipantDetails.tsx
@@ -206,6 +206,7 @@ const participantSchema = Yup.object().shape({
     .trim()
     .transform((val) => (val === "" ? undefined : val))
     .email("Please enter a valid email")
+    .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address")
     .optional(),
   phoneNumber: Yup.string()
     .trim()
diff --git a/src/components/shared/registration/RegistrationForm.tsx b/src/components/shared/registration/RegistrationForm.tsx
index 3e46dbb..39ade60 100644
--- a/src/components/shared/registration/RegistrationForm.tsx
+++ b/src/components/shared/registration/RegistrationForm.tsx
@@ -119,7 +119,8 @@ const baseSchemaFields = {
 
   email: Yup.string()
     .transform((val) => (val === "" ? undefined : val))
-    .email("Invalid email address")
+    .email("Please enter a valid email address")
+    .matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address")
     .optional(),
 
   phone: Yup.string()
@@ -250,8 +251,8 @@ const RegistrationForm = () => {
     groupId && supportsGroupRegistration
       ? "group"
       : categoryParam === "group" && supportsGroupRegistration
-      ? "group"
-      : "individual"
+        ? "group"
+        : "individual"
   );
   const [currentStep, setCurrentStep] = useState(1);
   const [registrations, setRegistrations] = useState<RegistrationData[]>([]);
@@ -317,7 +318,7 @@ const RegistrationForm = () => {
     if (eventId) {
       loadFromLocalStorage();
     }
-  // eslint-disable-next-line react-hooks/exhaustive-deps
+    // eslint-disable-next-line react-hooks/exhaustive-deps
   }, [eventId]);
 
   // Save to localStorage whenever registrations change
@@ -499,7 +500,7 @@ const RegistrationForm = () => {
         }
       }
     }
-  // eslint-disable-next-line react-hooks/exhaustive-deps
+    // eslint-disable-next-line react-hooks/exhaustive-deps
   }, [eventTickets, ticketId]);
 
   // Create tickets array from API data - using ticket ID directly
@@ -779,33 +780,115 @@ const RegistrationForm = () => {
 
   // Transform registration data to API format
   const transformToAPIFormat = (
-  registrations: RegistrationData[]
-): IndividualRegistationI => {
-  const participants: APIParticipant[] = registrations.map((reg) => {
+    registrations: RegistrationData[]
+  ): IndividualRegistationI => {
+    const participants: APIParticipant[] = registrations.map((reg) => {
+      const participant: APIParticipant = {
+        firstName: reg.firstName,
+        middleName: reg.middleName || undefined,
+        lastName: reg.lastName,
+        email: reg.email || undefined,
+        phone: toE164(reg.phone),
+        dateOfBirth: reg.dateOfBirth || "",
+        gender: reg.gender,
+        country: reg.country,
+        region: reg.region || undefined,
+        pace: reg.pace || undefined,
+        emergencyPhone: toE164(reg.emergencyPhone),
+        selectedTickets: {
+          [reg.selectedTicket]: 1,
+        },
+      };
+
+      // ✅ FIXED: use map instead of reduce
+      if (
+        reg.selectedMerchandise &&
+        Array.isArray(reg.selectedMerchandise) &&
+        reg.selectedMerchandise.length > 0
+      ) {
+        participant.selectedMerchandise = reg.selectedMerchandise.map(
+          (merch) => ({
+            varietyId: merch.variantId || merch.merchandiseId,
+            quantity: merch.quantity,
+            collectionCenterId: merch.collectionCenterId || "0", // ✅ string
+          })
+        );
+      }
+
+      return participant;
+    });
+
+    return {
+      eventId: eventId,
+      participants,
+    };
+  };
+
+  const handleSimpleRegistrationSubmit = async (values: RegistrationData) => {
+    if (Object.keys(selectedTickets).length === 0) {
+      formik.setFieldTouched("selectedTicket", true);
+
+      setTimeout(() => {
+        const ticketSection =
+          document.querySelector('[class*="MultiTicketSelector"]') ||
+          document.querySelector('h4:has-text("Select Ticket")') ||
+          Array.from(document.querySelectorAll("h4, h3")).find(
+            (el) =>
+              el.textContent?.includes("Ticket") ||
+              el.textContent?.includes("ticket")
+          );
+
+        if (ticketSection) {
+          ticketSection.scrollIntoView({
+            behavior: "smooth",
+            block: "center",
+            inline: "nearest",
+          });
+        } else {
+          const errorElements = Array.from(
+            document.querySelectorAll(
+              ".text-red-600, .text-red-400, .text-destructive"
+            )
+          );
+
+          const ticketError = errorElements.find((el) =>
+            el.textContent?.toLowerCase().includes("ticket")
+          );
+
+          if (ticketError) {
+            ticketError.scrollIntoView({
+              behavior: "smooth",
+              block: "center",
+            });
+          }
+        }
+      }, 100);
+
+      return;
+    }
+
     const participant: APIParticipant = {
-      firstName: reg.firstName,
-      middleName: reg.middleName || undefined,
-      lastName: reg.lastName,
-      email: reg.email || undefined,
-      phone: toE164(reg.phone),
-      dateOfBirth: reg.dateOfBirth || "",
-      gender: reg.gender,
-      country: reg.country,
-      region: reg.region || undefined,
-      pace: reg.pace || undefined,
-      emergencyPhone: toE164(reg.emergencyPhone),
-      selectedTickets: {
-        [reg.selectedTicket]: 1,
-      },
+      firstName: values.firstName,
+      middleName: values.middleName || undefined,
+      lastName: values.lastName,
+      email: values.email || undefined,
+      phone: toE164(values.phone),
+      dateOfBirth: values.dateOfBirth || "",
+      gender: values.gender,
+      country: values.country,
+      region: values.region || undefined,
+      pace: values.pace || undefined,
+      emergencyPhone: toE164(values.emergencyPhone),
+      selectedTickets: selectedTickets,
     };
 
-    // ✅ FIXED: use map instead of reduce
+    // ✅ FIXED: use map instead of reduce (array expected)
     if (
-      reg.selectedMerchandise &&
-      Array.isArray(reg.selectedMerchandise) &&
-      reg.selectedMerchandise.length > 0
+      values.selectedMerchandise &&
+      Array.isArray(values.selectedMerchandise) &&
+      values.selectedMerchandise.length > 0
     ) {
-      participant.selectedMerchandise = reg.selectedMerchandise.map(
+      participant.selectedMerchandise = values.selectedMerchandise.map(
         (merch) => ({
           varietyId: merch.variantId || merch.merchandiseId,
           quantity: merch.quantity,
@@ -814,102 +897,20 @@ const RegistrationForm = () => {
       );
     }
 
-    return participant;
-  });
-
-  return {
-    eventId: eventId,
-    participants,
-  };
-};
-
-const handleSimpleRegistrationSubmit = async (values: RegistrationData) => {
-  if (Object.keys(selectedTickets).length === 0) {
-    formik.setFieldTouched("selectedTicket", true);
-
-    setTimeout(() => {
-      const ticketSection =
-        document.querySelector('[class*="MultiTicketSelector"]') ||
-        document.querySelector('h4:has-text("Select Ticket")') ||
-        Array.from(document.querySelectorAll("h4, h3")).find(
-          (el) =>
-            el.textContent?.includes("Ticket") ||
-            el.textContent?.includes("ticket")
-        );
-
-      if (ticketSection) {
-        ticketSection.scrollIntoView({
-          behavior: "smooth",
-          block: "center",
-          inline: "nearest",
-        });
-      } else {
-        const errorElements = Array.from(
-          document.querySelectorAll(
-            ".text-red-600, .text-red-400, .text-destructive"
-          )
-        );
-
-        const ticketError = errorElements.find((el) =>
-          el.textContent?.toLowerCase().includes("ticket")
-        );
-
-        if (ticketError) {
-          ticketError.scrollIntoView({
-            behavior: "smooth",
-            block: "center",
-          });
-        }
-      }
-    }, 100);
-
-    return;
-  }
+    const apiData: IndividualRegistationI = {
+      eventId: eventId,
+      participants: [participant],
+    };
 
-  const participant: APIParticipant = {
-    firstName: values.firstName,
-    middleName: values.middleName || undefined,
-    lastName: values.lastName,
-    email: values.email || undefined,
-    phone: toE164(values.phone),
-    dateOfBirth: values.dateOfBirth || "",
-    gender: values.gender,
-    country: values.country,
-    region: values.region || undefined,
-    pace: values.pace || undefined,
-    emergencyPhone: toE164(values.emergencyPhone),
-    selectedTickets: selectedTickets,
-  };
+    const response: ApiResponseI<RegistrationResponse> =
+      await completeIndividualRegistration(apiData);
 
-  // ✅ FIXED: use map instead of reduce (array expected)
-  if (
-    values.selectedMerchandise &&
-    Array.isArray(values.selectedMerchandise) &&
-    values.selectedMerchandise.length > 0
-  ) {
-    participant.selectedMerchandise = values.selectedMerchandise.map(
-      (merch) => ({
-        varietyId: merch.variantId || merch.merchandiseId,
-        quantity: merch.quantity,
-        collectionCenterId: merch.collectionCenterId || "0", // ✅ string
-      })
+    setIsNavigating(true);
+    router.push(
+      `registration/${response.data?.registrationId}/ticket-checkout`
     );
-  }
-
-  const apiData: IndividualRegistationI = {
-    eventId: eventId,
-    participants: [participant],
   };
 
-  const response: ApiResponseI<RegistrationResponse> =
-    await completeIndividualRegistration(apiData);
-
-  setIsNavigating(true);
-  router.push(
-    `registration/${response.data?.registrationId}/ticket-checkout`
-  );
-};
-
   const handleFinalSubmit = async () => {
     // Update final registration data
     const finalRegistrations = [...registrations];
@@ -1201,7 +1202,7 @@ const handleSimpleRegistrationSubmit = async (values: RegistrationData) => {
                 }
                 ticketSelectionError={
                   formik.submitCount > 0 &&
-                  Object.keys(selectedTickets).length === 0
+                    Object.keys(selectedTickets).length === 0
                     ? "Please select at least one ticket"
                     : undefined
                 }
@@ -1211,13 +1212,12 @@ const handleSimpleRegistrationSubmit = async (values: RegistrationData) => {
 
               {/* Navigation Buttons */}
               <div
-                className={`flex flex-col sm:flex-row items-stretch sm:items-center gap-3 pt-4 sm:pt-6 border-t mt-4 sm:mt-8 ${
-                  !supportsGroupRegistration ||
-                  ticketQuantity === 1 ||
-                  currentStep === 1
+                className={`flex flex-col sm:flex-row items-stretch sm:items-center gap-3 pt-4 sm:pt-6 border-t mt-4 sm:mt-8 ${!supportsGroupRegistration ||
+                    ticketQuantity === 1 ||
+                    currentStep === 1
                     ? "sm:justify-end"
                     : "sm:justify-between"
-                }`}
+                  }`}
               >
                 {supportsGroupRegistration && currentStep > 1 && (
                   <Button
@@ -1247,8 +1247,8 @@ const handleSimpleRegistrationSubmit = async (values: RegistrationData) => {
 
                   {/* Next/Payment Button */}
                   {!supportsGroupRegistration ||
-                  ticketQuantity === 1 ||
-                  isLastStep ? (
+                    ticketQuantity === 1 ||
+                    isLastStep ? (
                     <Button
                       type="button"
                       onClick={(e) => {
@@ -1262,8 +1262,8 @@ const handleSimpleRegistrationSubmit = async (values: RegistrationData) => {
                       {isNavigating
                         ? "Redirecting..."
                         : isCreateLoading
-                        ? "Processing..."
-                        : "Proceed To Payment"}
+                          ? "Processing..."
+                          : "Proceed To Payment"}
                     </Button>
                   ) : (
                     <Button
diff --git a/src/contexts/event-registration.context.tsx b/src/contexts/event-registration.context.tsx
index d77a014..f887bef 100644
--- a/src/contexts/event-registration.context.tsx
+++ b/src/contexts/event-registration.context.tsx
@@ -94,6 +94,10 @@ interface EventRegistrationContextI {
     attendeeId: string,
     data: UpdateParticipantI,
   ) => Promise<void>;
+  updateGroupMember: (
+    memberId: string,
+    data: Partial<import("@/types/group").GroupMemberI>,
+  ) => Promise<void>;
 
   // REGISTRATION HISTORY
   getUserRegistrations: () => Promise<ApiResponseI<RegistrationListItemI[]>>;
diff --git a/src/data/DashboardRoutes.ts b/src/data/DashboardRoutes.ts
index 623bb1d..a9d5bc0 100644
--- a/src/data/DashboardRoutes.ts
+++ b/src/data/DashboardRoutes.ts
@@ -42,31 +42,7 @@ export const dashboardRoutes: NavigationItemData[] = [
     href: "/dashboard/galleries",
     roles: ["ROLE_USER", "ROLE_ADMIN", "ROLE_PARTNER_CONTACT"],
   },
-  // {
-  //   label: "Market Place",
-  //   iconName: "ShoppingBag",
-  //   roles: ["ROLE_USER", "ROLE_ADMIN"],
-  //   subItems: [
-  //     {
-  //       label: "Products",
-  //       iconName: "ShoppingBasket",
-  //       href: "/dashboard/nbc-sokoni",
-  //       roles: ["ROLE_USER", "ROLE_ADMIN"],
-  //     },
-  //     {
-  //       label: "Orders",
-  //       iconName: "BaggageClaim",
-  //       href: "/dashboard/nbc-sokoni/orders",
-  //       roles: ["ROLE_USER", "ROLE_ADMIN"],
-  //     },
-  //   ],
-  // },
-  {
-    label: "Insurance",
-    iconName: "Shield",
-    href: "/dashboard/insurance",
-    roles: ["ROLE_USER", "ROLE_ADMIN"],
-  },
+
   {
     label: "Groups",
     iconName: "Users",
@@ -85,26 +61,8 @@ export const dashboardRoutes: NavigationItemData[] = [
     href: "/dashboard/registrations",
     roles: ["ROLE_USER", "ROLE_ADMIN", "ROLE_PARTNER_CONTACT"],
   },
-  // {
-  //   label: "Bookings",
-  //   iconName: "BookUser",
-  //   href: "/dashboard/bookings",
-  //   roles: ["ROLE_USER", "ROLE_ADMIN"],
-  // }
 
-  // Admin-only routes
-  {
-    label: "User Management",
-    iconName: "Users",
-    href: "/dashboard/users",
-    roles: ["ROLE_ADMIN"],
-  },
-  //   {
-  //   label: "Products Schema",
-  //   iconName: "Palette",
-  //   href: "/dashboard/schema",
-  //   roles: ["ROLE_ADMIN"],
-  // },
+
   {
     label: "Ticket Management",
     iconName: "Tickets",
@@ -164,22 +122,12 @@ export const dashboardRoutes: NavigationItemData[] = [
     href: "/dashboard/profile",
     roles: ["ROLE_USER", "ROLE_ADMIN", "ROLE_PARTNER_CONTACT"],
   },
-  // {
-  //   label: "Activity Logs",
-  //   iconName: "Activity",
-  //   href: "/dashboard/activity-logs",
-  //   roles: ["ROLE_USER", "ROLE_ADMIN"],
-  // },
+
   {
     label: "Settings",
     iconName: "Settings",
     href: "/dashboard/settings",
     roles: ["ROLE_ADMIN"], // Only admins can access settings
   },
-  // {
-  //   label: "Reports",
-  //   iconName: "BarChart3",
-  //   href: "/dashboard/admin/reports",
-  //   roles: ["ROLE_USER"],
-  // },
+
 ];
diff --git a/src/lib/schema-to-yup.ts b/src/lib/schema-to-yup.ts
index f63792c..d27fa04 100644
--- a/src/lib/schema-to-yup.ts
+++ b/src/lib/schema-to-yup.ts
@@ -38,7 +38,7 @@ const createYupField = (property: JSONSchemaProperty, isRequired: boolean, field
 
         // Handle formats
         if (property.format === "email") {
-          stringField = stringField.email("Invalid email address");
+          stringField = stringField.email("Please enter a valid email address").matches(/^[^\s@]+@[^\s@]+\.[^\s@]+$/, "Please enter a valid email address");
         }
         if (property.format === "uri" || property.pattern?.includes("https?://")) {
           // For URI fields, we accept any string (since image URLs will be uploaded)
diff --git a/src/locales/TRANSLATION_KEYS.md b/src/locales/TRANSLATION_KEYS.md
index 600272a..e55f053 100644
--- a/src/locales/TRANSLATION_KEYS.md
+++ b/src/locales/TRANSLATION_KEYS.md
@@ -264,7 +264,7 @@ All validation messages support dynamic parameters like `{min}`, `{max}`, `{age}
 
 ### validation.email
 - `required` - Email is required / Barua pepe inahitajika
-- `invalid` - Invalid email address / Barua pepe si sahihi
+- `invalid` - Please enter a valid email address / Barua pepe si sahihi
 - `validEmail` - Please enter a valid email / Tafadhali ingiza barua pepe sahihi
 
 ### validation.password
diff --git a/src/locales/en/validation.json b/src/locales/en/validation.json
index 203568f..61a9314 100644
--- a/src/locales/en/validation.json
+++ b/src/locales/en/validation.json
@@ -15,7 +15,7 @@
   },
   "email": {
     "required": "Email is required",
-    "invalid": "Invalid email address",
+    "invalid": "Please enter a valid email address",
     "validEmail": "Please enter a valid email"
   },
   "password": {
diff --git a/src/providers/event-registration.provider.tsx b/src/providers/event-registration.provider.tsx
index ffcd251..5098895 100644
--- a/src/providers/event-registration.provider.tsx
+++ b/src/providers/event-registration.provider.tsx
@@ -63,15 +63,12 @@ export const EventRegistrationProvider: React.FC<{
     registrationId: string,
   ): Promise<void> => {
     try {
-      setIsLoading(true);
       await eventRegistrationService.eventRegistrationOrderDetails(
         registrationId,
         setOrderDetails,
       );
     } catch (error) {
       throw error;
-    } finally {
-      setIsLoading(false);
     }
   };
 
@@ -331,6 +328,20 @@ export const EventRegistrationProvider: React.FC<{
     }
   };
 
+  const updateGroupMember = async (
+    memberId: string,
+    data: Partial<import("@/types/group").GroupMemberI>,
+  ): Promise<void> => {
+    try {
+      setIsCreateLoading(true);
+      await eventRegistrationService.updateGroupMember(memberId, data);
+    } catch (error) {
+      throw error;
+    } finally {
+      setIsCreateLoading(false);
+    }
+  };
+
   // REGISTRATION HISTORY
   const getUserRegistrations = async (): Promise<
     ApiResponseI<RegistrationListItemI[]>
@@ -426,6 +437,7 @@ export const EventRegistrationProvider: React.FC<{
         // PARTICIPANT MANAGEMENT
         deleteParticipant,
         updateParticipant,
+        updateGroupMember,
 
         // REGISTRATION HISTORY
         getUserRegistrations,
diff --git a/src/services/event-registration.service.ts b/src/services/event-registration.service.ts
index 2cf20ad..610b2af 100644
--- a/src/services/event-registration.service.ts
+++ b/src/services/event-registration.service.ts
@@ -383,13 +383,13 @@ class EventRegistrationService extends BaseApiService {
     data: UpdateParticipantI
   ) {
     try {
-      const response = await this.put(
+      const response = await this.patch(
         `${API_PATHS.EVENT}event-attendees/${attendeeId}`,
-       {
-        id:attendeeId,
-        eventRegistrationId:registrationId,
-        ...data
-       },
+        {
+          id: attendeeId,
+          eventRegistrationId: registrationId,
+          ...data,
+        },
         "Successfully updated participant"
       );
       return response;
@@ -398,6 +398,22 @@ class EventRegistrationService extends BaseApiService {
     }
   }
 
+  async updateGroupMember(
+    memberId: string,
+    data: Partial<import("@/types/group").GroupMemberI>
+  ) {
+    try {
+      const response = await this.put(
+        `${API_PATHS.EVENT}group-members/${memberId}`,
+        { id: memberId, ...data },
+        "Successfully updated group member"
+      );
+      return response;
+    } catch (error) {
+      throw error;
+    }
+  }
+
   // Get user's own registrations
   async getUserRegistrations(): Promise<ApiResponseI<RegistrationListItemI[]>> {
     try {
diff --git a/src/types/user.ts b/src/types/user.ts
index 2ea2119..6316d20 100644
--- a/src/types/user.ts
+++ b/src/types/user.ts
@@ -25,7 +25,7 @@ export interface UserI {
   email: string;
   imageUrl?: string;
   activated: boolean;
-  langKey?: string;
+  langKey: string;
   createdBy: string;
   createdDate: string;
   lastModifiedBy: string;
